% ====================================================================
% MulVAL Interaction Rules Knowledge Base
% ====================================================================

% Primitive Predicates
primitive(accepts_hash(_0, _1)).
primitive(app_installed(_0, _1, _2)).
primitive(attacker_at(_0)).
primitive(auth_possible(_0, _1, _2)).
primitive(auth_usable_cred(_0, _1, _2)).
primitive(c2_channel(_0, _1)).
primitive(cloud_account(_0, _1)).
primitive(cloud_cred(_0, _1)).
primitive(compromise(_0, _1)).
primitive(compromise_from_exec(_0, _1)).
primitive(compromise_from_pe(_0, _1)).
primitive(connects(_0, _1, _2)).
primitive(cred(_0, _1, _2)).
primitive(defense_disabled(_0, _1)).
primitive(device(_0)).
primitive(event(_0, _1, _2, _3)).
primitive(exec(_0, _1)).
primitive(execCode(_0, _1)).
primitive(exec_possible_remote(_0, _1)).
primitive(exec_possible_user(_0, _1)).
primitive(exfiltrated(_0, _1, _2)).
primitive(exploit_possible(_0)).
primitive(exposed(_0, _1)).
primitive(has_data(_0, _1)).
primitive(host(_0)).
primitive(iam_policy(_0, _1, _2, _3)).
primitive(iam_principal(_0, _1)).
primitive(ics_endpoint(_0, _1)).
primitive(ics_exposed_endpoint(_0, _1)).
primitive(ladder_logic(_0, _1)).
primitive(local_pe_vulnerable(_0, _1)).
primitive(local_vuln(_0, _1)).
primitive(loot(_0, _1, _2)).
primitive(mdm_enrolled(_0, _1)).
primitive(metadata_access(_0, _1)).
primitive(misconfig(_0, _1)).
primitive(mobile_perm(_0, _1)).
primitive(networkServiceInfo(_0, _1, _2, _3, _4)).
primitive(nonvar(_0, _1)).
primitive(os(_0, _1)).
primitive(perm(_0, _1, _2)).
primitive(persistence(_0, _1)).
primitive(persistence_enables_reentry(_0)).
primitive(pkg(_0, _1, _2)).
primitive(plc(_0)).
primitive(priv_escalation(_0, _1, _2)).
primitive(protocol(_0, _1, _2)).
primitive(pth_feasible(_0, _1)).
primitive(reachable(_0, _1, _2)).
primitive(remote_service_vulnerable(_0, _1, _2, _3)).
primitive(role(_0, _1)).
primitive(safety_instrumented(_0)).
primitive(sensor(_0, _1)).
primitive(service(_0, _1, _2, _3, _4)).
primitive(service_matches(_0, _1)).
primitive(setting(_0, _1, _2)).
primitive(trust(_0, _1, _2)).
primitive(user_on_host(_0, _1)).
primitive(user_phishes_success(_0)).
primitive(valid_account(_0, _1)).
primitive(vulExists(_0, _1, _2, _3)).
primitive(vuln(_0, _1, _2)).
primitive(weakened_defense(_0)).
primitive(block_reporting_message(_0)).
primitive(compromised_device(_0)).
primitive(removable_media_infection(_0)).
primitive(connected_removable_media(_0, _1)).
primitive(misleading_name(_0)).
primitive(modify_malware(_0, _1, _2)).
primitive(user_accesses_content(_0, _1)).
primitive(modify_account(_0, _1)).
primitive(bypasses_app_control(_0)).
primitive(actual_file_type(_0, _1)).
primitive(direct_installation(_0)).
primitive(dll_side_loading(_0, _1)).
primitive(dll_search_order_hijacking(_0, _1)).
primitive(signal_packet_sequence_detected(_0, _1)).
primitive(encrypted_traffic(_0, _1)).
primitive(malicious_app_installed(_0, _1)).
primitive(analysis_env_detected(_0)).
primitive(jailbroken(_0)).
primitive(receives_mobile_data(_0, _1)).
primitive(manipulate_app_store_rankings_or_ratings(_0)).
primitive(vulnerable_browser(_0)).
primitive(user_accesses_content(_0, _1)).
primitive(vulnerability_exploit(_0)).
primitive(jam_signal(_0, _1)).
primitive(signal_jammer_in_range(_0, _1)).
primitive(needs_compilation(_0)).
primitive(encrypt_data(_0, _1)).
primitive(creates_instance_in_region(_0, _1, _2)).
primitive(monitored_region(_0, _1)).
primitive(compress_data(_0, _1)).
primitive(encrypt_data(_0, _1)).
primitive(file_transfer_tool_installed(_0, _1)).
primitive(decompiled_and_rebuilt(_0, _1)).
primitive(attacker_modifies_app(_0, _1)).
primitive(used_in_phase(_0, _1)).
primitive(theft(_0, _1, _2)).
primitive(gather_victim_org_info(_0, _1)).
primitive(allows_traffic(_0, _1)).
primitive(modify_system_image(_0)).
primitive(rooted(_0)).
primitive(paas_service(_0)).
primitive(matches_environment(_0, _1)).
primitive(dynamic_resolution(_0)).
primitive(dynamic_parameters(_0, _1, _2, _3)).
primitive(prolonged_information_gathering(_0)).
primitive(grants_permission(_0, _1, _2)).
primitive(sends_mass_emails(_0)).
primitive(open_wifi(_0, _1)).
primitive(obfuscates_traffic(_0)).
primitive(protocol_contains_junk(_0)).
primitive(file_contains_embedded_payload(_0, _1)).
primitive(benign_file(_0)).
primitive(contains_malicious_payload(_0)).
primitive(hook_procedure_installed(_0)).
primitive(iat_hook_installed(_0)).
primitive(inline_hook_installed(_0)).
primitive(ld_preload_hook_installed(_0)).
primitive(dyld_insert_libraries_hook_installed(_0)).
primitive(clear_command_history(_0, _1)).
primitive(command_executed(_0, _1, _2)).
primitive(uses_legitimate_service(_0, _1)).
primitive(replace_compiled_release(_0, _1)).
primitive(packet_matches_filter(_0)).
primitive(attacker_sends_packet(_0, _1, _2)).
primitive(filter_criteria_met(_0, _1)).
primitive(abuse_accessibility_features(_0)).
primitive(security_software_discovery(_0)).
primitive(security_app(_0)).
primitive(decrypts_successfully(_0, _1)).
primitive(mutex_present(_0, _1)).
primitive(mutex_exists(_0, _1)).
primitive(mutex_associated_with_malware(_0)).
primitive(sms_pumping_attack(_0)).
primitive(user_activity_check(_0)).
primitive(mouse_movement_detected(_0)).
primitive(browser_history_detected(_0)).
primitive(file_count_detected(_0)).
primitive(user_interaction_required(_0)).
primitive(delay_execution(_0)).
primitive(uses_sleep_command(_0)).
primitive(uses_scheduled_tasks(_0)).
primitive(uses_api_hammering(_0)).
primitive(uses_benign_commands(_0)).
primitive(spoofed_packet(_0, _1)).
primitive(protocol_amplifies(_0)).
primitive(user_accesses_content(_0, _1)).
primitive(disable_security_tool(_0)).
primitive(spoof_security_alerting(_0)).
primitive(spoofed_status(_0, _1)).
primitive(api_call(_0, _1, _2)).
primitive(known_exclusion_path(_0)).
primitive(file_dropped(_0, _1)).
primitive(email_account_created(_0)).
primitive(infrastructure_acquired(_0)).
primitive(persona_cultivated(_0)).
primitive(disposable_email_used(_0)).
primitive(compromised_social_media_account(_0)).
primitive(steal_cert(_0, _1)).
primitive(nat_traversal(_0)).
primitive(network_device(_0)).
primitive(nat_configurable(_0)).
primitive(uri_points_to_malicious_content(_0)).
primitive(abuse_accessibility_features(Host)).
primitive(actual_file_type(File, MaliciousType)).
primitive(allows_traffic(Device, Arg1)).
primitive(analysis_env_detected(Host)).
primitive(api_call(Host, Arg1, _)).
primitive(attacker_modifies_app(App, Host)).
primitive(attacker_sends_packet(Reflector, Target, _Packet)).
primitive(benign_file(File)).
primitive(block_reporting_message(Host)).
primitive(browser_history_detected(Host)).
primitive(bypasses_app_control(Host)).
primitive(clear_command_history(Host, User)).
primitive(command_executed(Host, User, User)).
primitive(compress_data(Host, DataKind)).
primitive(compromised_device(Host)).
primitive(compromised_social_media_account(User)).
primitive(connected_removable_media(Host, Media)).
primitive(contains_malicious_payload(File)).
primitive(creates_instance_in_region(User, Provider, Region)).
primitive(decompiled_and_rebuilt(App, Host)).
primitive(decrypts_successfully(Payload, Host)).
primitive(delay_execution(Host)).
primitive(direct_installation(Host)).
primitive(disable_security_tool(Host)).
primitive(disposable_email_used(EmailAccount)).
primitive(dll_search_order_hijacking(Host, Arg1)).
primitive(dll_side_loading(Host, Arg1)).
primitive(dyld_insert_libraries_hook_installed(Host)).
primitive(dynamic_parameters(Host, Domain, IP, Port)).
primitive(dynamic_resolution(Host)).
primitive(email_account_created(EmailAccount)).
primitive(encrypt_data(Host, DataKind)).
primitive(encrypted_traffic(Host, Protocol)).
primitive(file_contains_embedded_payload(File, Host)).
primitive(file_count_detected(Host)).
primitive(file_dropped(Host, FilePath)).
primitive(file_transfer_tool_installed(Host, Tool)).
primitive(filter_criteria_met(Host, _Packet)).
primitive(gather_victim_org_info(Attacker, Victim)).
primitive(grants_permission(Host, User, Permission)).
primitive(hook_procedure_installed(Host)).
primitive(iat_hook_installed(Host)).
primitive(infrastructure_acquired(Host)).
primitive(inline_hook_installed(Host)).
primitive(jailbroken(Host)).
primitive(jam_signal(Network, Host)).
primitive(known_exclusion_path(FilePath)).
primitive(ld_preload_hook_installed(Host)).
primitive(malicious_app_installed(Host, App)).
primitive(manipulate_app_store_rankings_or_ratings(Host)).
primitive(matches_environment(Host, Environment)).
primitive(misleading_name(Host)).
primitive(modify_account(User, Host)).
primitive(modify_malware(Host, Malware, NewMalware)).
primitive(modify_system_image(Host)).
primitive(monitored_region(Arg0, Arg1)).
primitive(mouse_movement_detected(Host)).
primitive(mutex_associated_with_malware(MutexName)).
primitive(mutex_exists(Host, MutexName)).
primitive(mutex_present(Host, MutexName)).
primitive(nat_configurable(Host)).
primitive(nat_traversal(Host)).
primitive(needs_compilation(Host)).
primitive(network_device(Host)).
primitive(obfuscates_traffic(Host)).
primitive(open_wifi(Network, SSID)).
primitive(paas_service(Host)).
primitive(packet_matches_filter(Host)).
primitive(persona_cultivated(EmailAccount)).
primitive(prolonged_information_gathering(Host)).
primitive(protocol_amplifies(Protocol)).
primitive(protocol_contains_junk(Protocol)).
primitive(receives_mobile_data(MobileCarrier, User)).
primitive(removable_media_infection(Host)).
primitive(replace_compiled_release(App, Host)).
primitive(rooted(Host)).
primitive(security_app(App)).
primitive(security_software_discovery(Host)).
primitive(sends_mass_emails(Host)).
primitive(signal_jammer_in_range(Network, Host)).
primitive(signal_packet_sequence_detected(Host, Port)).
primitive(sms_pumping_attack(Host)).
primitive(spoof_security_alerting(Host)).
primitive(spoofed_packet(Network, Target)).
primitive(spoofed_status(Host, Arg1)).
primitive(steal_cert(Host, Arg1)).
primitive(theft(Attacker, Party, Capability)).
primitive(uri_points_to_malicious_content(Uri)).
primitive(used_in_phase(Capability, Phase)).
primitive(user_accesses_content(User, Host)).
primitive(user_activity_check(Host)).
primitive(user_interaction_required(Host)).
primitive(uses_api_hammering(Host)).
primitive(uses_benign_commands(Host)).
primitive(uses_legitimate_service(Host, Service)).
primitive(uses_scheduled_tasks(Host)).
primitive(uses_sleep_command(Host)).
primitive(vulnerability_exploit(Host)).
primitive(vulnerable_browser(Host)).
primitive(member(_elem,_list)).
primitive(file_extension(_file,_ext)).
primitive(alert_triggered(_host,_alert_type)).
primitive(commonly_used_port(_host,_port)).
primitive(encrypted_c2_traffic(_host)).
primitive(dga_used_for_c2(_host)).

% Derived Predicates
% Derived predicates are inferred from interaction rules
derived(exec(_0, _1)).
derived(compromise(_0, _1)).
derived(priv_escalation(_0, _1, _2)).
derived(loot(_0, _1, _2)).
derived(exfiltrated(_0, _1, _2)).
derived(persistence(_0, _1)).
derived(c2_channel(_0, _1)).
derived(access(_0, _1)).
derived(access_calendar_entries(_0)).
derived(access_contact_list(_0)).
derived(access_protected_user_data(_0)).
derived(access_sensitive_data_in_device_logs(_0)).
derived(access_token_manipulation(_0, _1)).
derived(accesses_website(_0, _1)).
derived(account_access_removal(_0, _1)).
derived(account_discovery(_0)).
derived(account_manipulation(_0, _1)).
derived(acquire_infrastructure(_0)).
derived(acquired_access(_0)).
derived(active_scanning(_0)).
derived(adversary_in_the_middle(_0)).
derived(alarm_suppression(_0)).
derived(app_has_permission(_0, _1)).
derived(app_shimming_possible(_0)).
derived(app_update_malicious(_0, _1)).
derived(application_window_discovery(_0)).
derived(archive_collected_data(_0)).
derived(audio_capture_possible(_0)).
derived(automated_collection(_0)).
derived(automated_exfiltration(_0)).
derived(autorun_vulnerability(_0)).
derived(bandwidth_exhaustion(_0, _1)).
derived(bash_history_access(_0, _1)).
derived(binary_padding_evasion(_0)).
derived(biometric_spoofing(_0)).
derived(biometric_spoofing_possible(_0)).
derived(bits_job_abuse(_0)).
derived(blackmarket_listing(_0, _1)).
derived(block_serial_com(_0)).
derived(blocked_command_message(_0)).
derived(bootkit_persistence(_0)).
derived(botnet_traffic(_0, _1)).
derived(broadcast_receiver_malware(_0)).
derived(browser_info_discovery(_0)).
derived(browser_session_hijacking(_0)).
derived(brute_force_io(_0)).
derived(brute_force_possible(_0, _1)).
derived(build_image_on_host(_0)).
derived(builds_solution(_0, _1, _2)).
derived(bypass_policy_enforcement(_0)).
derived(bypass_uac(_0)).
derived(call_control_compromise(_0)).
derived(can_revert_instance(_0)).
derived(capability_developed(_0, _1)).
derived(carrier_billing_fraud(_0)).
derived(change_credential(_0)).
derived(change_operating_mode(_0)).
derived(change_program_state(_0)).
derived(clipboard_data_access(_0)).
derived(clipboard_data_access(_0, _1)).
derived(clipboard_modification(_0)).
derived(closed_source(_0)).
derived(cloud_dashboard_access(_0, _1)).
derived(cloud_data_accessible(_0)).
derived(cloud_infrastructure_discovery(_0)).
derived(cloud_service_discovery(_0)).
derived(cloud_storage_object_discovery(_0, _1)).
derived(cmstp_exploit(_0)).
derived(code_injection_possible(_0)).
derived(collected_point_tag_values(_0)).
derived(com_hijack_persistence(_0)).
derived(commonly_used_port(_0)).
derived(compile_after_delivery(_0)).
derived(compromise_infrastructure(_0)).
derived(compromised_access_token(_0, _1)).
derived(compromised_binary(_0, _1)).
derived(compromised_certificate(_0)).
derived(compromised_data(_0, _1)).
derived(compromised_firmware(_0)).
derived(compromised_infrastructure(_0)).
derived(compromised_marketplace(_0)).
derived(concealment(_0, _1)).
derived(connection_proxy(_0)).
derived(container_admin_command_exec(_0)).
derived(container_discovery(_0)).
derived(content_injection_possible(_0)).
derived(control_device_identification(_0)).
derived(create_account(_0, _1)).
derived(credential_dumping(_0, _1)).
derived(credentials_from_password_store(_0, _1)).
derived(credentials_from_password_stores(_0, _1)).
derived(credentials_in_files(_0, _1)).
derived(critical_data_overwritten(_0)).
derived(critical_service(_0)).
derived(cybercrime_blackmarket(_0)).
derived(damage_to_property(_0)).
derived(dark_web(_0)).
derived(data_collected(_0, _1)).
derived(data_collected_from_repo(_0, _1)).
derived(data_collection(_0)).
derived(data_compressed(_0)).
derived(data_destruction(_0)).
derived(data_encoded_c2(_0)).
derived(data_encrypted_for_impact(_0)).
derived(data_from_config_repo(_0)).
derived(data_from_local_system(_0)).
derived(data_from_network_shared_drive(_0)).
derived(data_manipulation(_0)).
derived(data_staged(_0)).
derived(dde_execution_possible(_0)).
derived(debugger_evasion_detected(_0)).
derived(defacement_possible(_0)).
derived(default_credentials_exploitable(_0)).
derived(delete_device_data(_0)).
derived(delete_logs(_0, _1)).
derived(denial_of_control(_0)).
derived(denial_of_service(_0)).
derived(denial_of_view(_0)).
derived(deny_functionality(_0)).
derived(deobfuscate_decode_files_or_information(_0)).
derived(deploy_container(_0)).
derived(detect_app_analysis_env(_0)).
derived(detect_operating_mode(_0)).
derived(detected_malware(_0, _1)).
derived(device_admin_privilege_escalation(_0)).
derived(device_driver_discovery(_0)).
derived(device_lockout(_0)).
derived(device_restart_shutdown(_0)).
derived(device_unlock_code_guessing(_0)).
derived(dga_c2_channel(_0)).
derived(dga_used_for_c2(_0)).
derived(direct_volume_access(_0)).
derived(disable_vulnerable_service(_0)).
derived(disguise_root_jailbreak_indicators(_0)).
derived(disk_content_wipe(_0)).
derived(disk_structure_wipe(_0)).
derived(disk_wipe(_0)).
derived(distributed_attack(_0, _1)).
derived(dll_search_order_hijacking(_0)).
derived(dll_side_loading_vulnerable(_0)).
derived(domain_fronting_possible(_0)).
derived(domain_policy_modified(_0)).
derived(domain_trust_discovery(_0)).
derived(downgrade_to_insecure_protocols(_0)).
derived(downloaded_capability(_0, _1)).
derived(drive_by_compromise(_0)).
derived(dual_homed(_0)).
derived(dylib_hijacking_possible(_0)).
derived(dynamic_code_downloaded(_0)).
derived(eavesdrop_possible(_0)).
derived(email_bombing(_0)).
derived(email_collection(_0, _1)).
derived(email_spoofing_possible(_0)).
derived(encoding_used(_0, _1)).
derived(encrypted_c2_channel(_0)).
derived(encrypted_c2_traffic(_0)).
derived(encrypted_exfiltration(_0, _1, _2)).
derived(endpoint_dos(_0)).
derived(escape_to_host(_0)).
derived(establish_account(_0, _1)).
derived(esxi_admin_command_execution(_0)).
derived(evasion(_0, _1)).
derived(evasion_attempt(_0)).
derived(event_triggered_execution(_0)).
derived(evidence(_0, _1)).
derived(ewm_injection_possible(_0)).
derived(exclusive_control(_0)).
derived(execution_guardrails(_0)).
derived(exfiltration_over_alternative_protocol(_0, _1, _2)).
derived(exfiltration_over_c2(_0, _1, _2)).
derived(exfiltration_over_c2_channel(_0, _1)).
derived(exfiltration_over_other_medium(_0, _1, _2)).
derived(exfiltration_over_physical_medium(_0, _1)).
derived(exfiltration_over_web_service(_0)).
derived(exfiltration_possible(_0)).
derived(exfiltration_possible(_0, _1)).
derived(exfiltration_preparation(_0)).
derived(expected_app(_0)).
derived(expected_conditions_met(_0)).
derived(expected_environment(_0, _1)).
derived(exploit_credential_access(_0, _1)).
derived(exploit_defense_evasion(_0)).
derived(exploit_for_evasion(_0)).
derived(exploit_remote_service(_0)).
derived(exploit_ss7_tracking(_0)).
derived(exploit_tee_vulnerability(_0)).
derived(exploit_via_radio_interface(_0)).
derived(exploit_vulnerability(_0)).
derived(external_remote_service_access(_0)).
derived(fake_developer_account_compromise(_0)).
derived(fake_input_prompt(_0, _1)).
derived(file_accessible(_0, _1, _2)).
derived(file_and_directory_discovery(_0)).
derived(file_deletion(_0)).
derived(file_directory_discovery(_0)).
derived(file_modified(_0, _1)).
derived(file_permission_modified(_0, _1)).
derived(file_system_permissions_weakness(_0)).
derived(financial_theft(_0)).
derived(firmware_accessible(_0)).
derived(firmware_compromise(_0)).
derived(firmware_update_mode_activated(_0)).
derived(forged_web_credential(_0, _1)).
derived(fraudulent_ad_revenue(_0)).
derived(free_resource(_0)).
derived(gatekeeper_bypass(_0)).
derived(gather_process_state_info(_0)).
derived(gather_program_state_info(_0)).
derived(gather_victim_host_info(_0)).
derived(gather_victim_identity_info(_0)).
derived(gather_victim_network_info(_0)).
derived(generate_traffic_from_victim(_0)).
derived(geofencing_exploit(_0)).
derived(group_policy_discovery(_0)).
derived(gui_access_possible(_0)).
derived(hardware_addition_access(_0)).
derived(hidden_files_and_directories(_0)).
derived(hidden_user_account(_0, _1)).
derived(hidden_window_exploit(_0)).
derived(hide_artifacts(_0)).
derived(hide_infrastructure(_0)).
derived(high_traffic_volume(_0, _1)).
derived(hijack_entry_point(_0)).
derived(hijack_execution_flow(_0)).
derived(hooking_attack(_0)).
derived(hooking_detected(_0)).
derived(hooking_possible(_0)).
derived(hosts(_0, _1)).
derived(hypervisor_persistence(_0)).
derived(identifies_development_requirements(_0, _1)).
derived(ifeo_injection(_0)).
derived(impact(_0)).
derived(impact_on_operations(_0)).
derived(impact_possible(_0)).
derived(impersonates(_0, _1)).
derived(impersonates_usb(_0, _1)).
derived(impersonation_attack(_0, _1)).
derived(implant_internal_image(_0)).
derived(indicator_blocking(_0)).
derived(indicator_known(_0, _1)).
derived(indicator_removal(_0)).
derived(indicator_removal_from_tools(_0)).
derived(indicator_removal_on_host(_0)).
derived(indirect_command_execution(_0)).
derived(infected_pou(_0)).
derived(infected_project_file(_0)).
derived(information_available(_0, _1)).
derived(ingress_tool_transfer(_0)).
derived(inhibit_response_function(_0)).
derived(inhibit_system_recovery(_0)).
derived(input_capture(_0)).
derived(input_capture(_0, _1)).
derived(input_injection(_0)).
derived(input_injection_possible(_0)).
derived(insecure_permissions(_0, _1)).
derived(insecure_third_party_library(_0, _1, _2)).
derived(installs_root_certificate(_0)).
derived(intercept_communication(_0)).
derived(intercept_traffic(_0)).
derived(internal_spearphishing(_0, _1)).
derived(io_module_discovery(_0)).
derived(ip_spoofing(_0, _1)).
derived(ipc_abuse(_0)).
derived(job_scheduling_persistence(_0)).
derived(kerberoasting_possible(_0, _1)).
derived(kerberos_ticket_stolen_or_forged(_0, _1)).
derived(kernel_extension_loaded(_0)).
derived(kernel_module_loaded(_0)).
derived(keychain_access(_0, _1)).
derived(lateral_movement_possible(_0, _1)).
derived(lateral_tool_transfer(_0)).
derived(launch_daemon_persistence(_0)).
derived(llmnr_nbt_ns_poisoning_and_relay(_0)).
derived(location_identification_possible(_0)).
derived(location_tracking(_0)).
derived(lockscreen_bypass(_0)).
derived(log_enumeration(_0)).
derived(loss_of_availability(_0)).
derived(loss_of_control(_0)).
derived(loss_of_productivity_and_revenue(_0)).
derived(loss_of_safety(_0)).
derived(loss_of_view(_0)).
derived(malicious_broadcast_receiver(_0)).
derived(malicious_component_installed(_0)).
derived(malicious_dll(_0)).
derived(malicious_extension_installed(_0)).
derived(malicious_media_content(_0)).
derived(malicious_password_filter_installed(_0)).
derived(malicious_prompt(_0)).
derived(malicious_software_development_tools(_0)).
derived(malicious_traffic(_0, _1)).
derived(manipulate_device_communication(_0)).
derived(manipulate_io_image(_0)).
derived(manipulated_metadata(_0)).
derived(manipulated_view(_0)).
derived(manipulation_of_control(_0)).
derived(marketplace(_0)).
derived(masquerading_detected(_0)).
derived(mbr_accessible(_0)).
derived(member(_0, _1, _2)).
derived(member(_0, _1, _2, _3)).
derived(member(_0, _1, _2, _3, _4)).
derived(member(_0, _1, _2, _3, _4, _5)).
derived(member(_0, _1, _2, _3, _4, _5, _6, _7)).
derived(member(_0, _1, _2, _3, _4, _5, _6, _7, _8)).
derived(mfa_interception_possible(_0, _1)).
derived(mfa_request_generation_attack(_0, _1)).
derived(modified_auth_process(_0)).
derived(modify_alarm_settings(_0)).
derived(modify_cloud_hierarchy(_0, _1)).
derived(modify_cloud_infrastructure(_0)).
derived(modify_control_logic(_0)).
derived(modify_controller_tasking(_0)).
derived(modify_parameter(_0)).
derived(modify_program(_0)).
derived(modify_registry(_0)).
derived(modify_system_partition(_0)).
derived(modify_traffic(_0)).
derived(multi_hop_proxy_detected(_0)).
derived(multiband_communication(_0)).
derived(multiple_sources(_0, _1)).
derived(native_api_access(_0)).
derived(native_api_execution(_0)).
derived(network_config_discovery(_0)).
derived(network_connection_enumeration(_0)).
derived(network_discovery(_0)).
derived(network_dos(_0)).
derived(network_dos_attack(_0)).
derived(network_info_discovery(_0)).
derived(network_service_discovery(_0)).
derived(network_service_scanning(_0)).
derived(network_share_connection_removed(_0)).
derived(network_share_discovery(_0)).
derived(network_sniffing(_0)).
derived(network_sniffing_possible(_0)).
derived(network_traffic_capture_or_redirection(_0)).
derived(new_service_installed(_0)).
derived(non_application_layer_protocol(_0)).
derived(non_standard_port_usage(_0, _1)).
derived(non_standard_port_usage(_0, _1, _2)).
derived(notification_data_collected(_0)).
derived(ntfs_file_attribute_attack(_0)).
derived(obfuscated_payload_detected(_0)).
derived(obtained_capabilities(_0)).
derived(online_geolocation_service(_0, _1)).
derived(open_technical_database(_0)).
derived(out_of_band_channel(_0)).
derived(overload_inbox(_0)).
derived(overwrite_mbr(_0)).
derived(overwrite_partition_table(_0)).
derived(paas_staging(_0)).
derived(packed_software_detected(_0)).
derived(partition_table_accessible(_0)).
derived(pass_the_ticket_feasible(_0, _1)).
derived(password_policy_discovery(_0)).
derived(patch_vulnerable_system(_0)).
derived(path_interception_vulnerable(_0)).
derived(phishing_attempt(_0, _1)).
derived(phishing_for_information(_0)).
derived(physical_access(_0)).
derived(physical_access_possible(_0)).
derived(plist_file_modified(_0)).
derived(plist_modification(_0)).
derived(possesses(_0, _1)).
derived(potential_drive_by_compromise(_0)).
derived(potential_t0822(_0)).
derived(potential_t1223(_0)).
derived(potential_xsl_script_processing(_0)).
derived(ppid_spoofing_possible(_0)).
derived(privilege_escalation(_0, _1)).
derived(privilege_escalation(_0, _1, _2)).
derived(privileged_access(_0)).
derived(process_discovery(_0)).
derived(process_disruption(_0)).
derived(process_doppelgaenging(_0)).
derived(process_hollowing_detected(_0)).
derived(process_injection_possible(_0)).
derived(program_download_possible(_0)).
derived(program_upload_possible(_0)).
derived(protocol(_0, _1)).
derived(protocol_in_use(_0)).
derived(protocol_tunneling(_0)).
derived(proxy_execution_possible(_0)).
derived(proxy_through_victim(_0)).
derived(proxy_used(_0)).
derived(purchase_information(_0, _1, _2)).
derived(purchased_capability(_0, _1)).
derived(purchased_infrastructure(_0)).
derived(query_registry(_0)).
derived(rdp_accessible(_0)).
derived(rdp_compromise(_0, _1)).
derived(rdp_session_hijack(_0, _1)).
derived(reconnaissance(_0)).
derived(reconnaissance(_0, _1)).
derived(reconnaissance_opportunity(_0)).
derived(reconnaissance_tactic(_0, _1)).
derived(redundant_access(_0)).
derived(reflective_code_loading(_0)).
derived(registers_uri(_0, _1)).
derived(registry_key_modified(_0, _1, _2)).
derived(remote_access_possible(_0, _1)).
derived(remote_access_tool_installed(_0)).
derived(remote_service_accessible(_0, _1)).
derived(remote_system_discovery(_0)).
derived(remotely_install_app(_0)).
derived(removable_media_access(_0)).
derived(remove_other_malware(_0)).
derived(renamed_utility(_0)).
derived(reputable_private_source(_0)).
derived(resource_hijacking(_0)).
derived(revert_cloud_instance(_0)).
derived(rogue_access_point(_0)).
derived(rogue_base_station_detected(_0)).
derived(rogue_domain_controller(_0)).
derived(rogue_master_setup(_0)).
derived(rogue_wifi_access_point(_0)).
derived(role_identification(_0)).
derived(root_access(_0)).
derived(rootkit_installed(_0)).
derived(runtime_data_manipulation(_0)).
derived(scheduled_exfiltration(_0, _1, _2)).
derived(scheduled_task_abuse(_0, _1)).
derived(screen_capture(_0)).
derived(screen_capture_possible(_0)).
derived(scripting_executed(_0, _1)).
derived(scripting_execution(_0, _1)).
derived(sells(_0, _1)).
derived(serverless_execution(_0, _1)).
derived(service_execution_possible(_0)).
derived(service_stopped(_0, _1)).
derived(session_cookie_stolen(_0, _1)).
derived(session_hijack_possible(_0, _1)).
derived(setuid_setgid_exploit(_0, _1)).
derived(shared_storage(_0, _1)).
derived(shared_webroot_attack(_0)).
derived(signed_binary(_0)).
derived(signed_by_trusted_cert(_0)).
derived(signed_malware(_0)).
derived(signed_with_legit_cert(_0)).
derived(sim_card_swap_attack(_0, _1)).
derived(sim_swap_request(_0, _1)).
derived(sms_control_attack(_0)).
derived(social_engineering(_0)).
derived(software_discovery(_0)).
derived(source_code_payload(_0)).
derived(spearphishing_attachment(_0, _1)).
derived(spearphishing_link(_0, _1)).
derived(spearphishing_via_service(_0, _1)).
derived(specialized_software_access(_0, _1)).
derived(specialized_tool(_0)).
derived(spoof_reporting_message(_0)).
derived(spoofed_ip_traffic(_0, _1)).
derived(ss7_exploit_possible(_0)).
derived(stage_capabilities(_0)).
derived(standard_port(_0, _1)).
derived(steal_application_access_token(_0, _1)).
derived(steal_or_forge_certificates(_0, _1)).
derived(steal_web_session_cookie(_0, _1)).
derived(stolen_auth_material(_0, _1)).
derived(stolen_capability(_0, _1)).
derived(stolen_token(_0, _1)).
derived(stored_app_data_accessible(_0)).
derived(strip_privileges(_0)).
derived(subscription_feed(_0, _1)).
derived(subvert_trust_controls(_0)).
derived(sudo_caching_vulnerable(_0)).
derived(sufficient_permissions(_0)).
derived(supply_chain_compromise(_0)).
derived(supports_operations(_0, _1)).
derived(system_info_discovery(_0)).
derived(system_location_discovery(_0)).
derived(system_service_discovery(_0)).
derived(system_shutdown_reboot(_0)).
derived(system_time_discovery(_0)).
derived(taint_shared_content(_0)).
derived(tainted_content(_0)).
derived(template_injection_possible(_0)).
derived(theft_of_operational_info(_0)).
derived(third_party(_0)).
derived(traffic_signaling_detected(_0)).
derived(transaction(_0, _1, _2)).
derived(transient_asset_compromise(_0)).
derived(trap_execution(_0, _1)).
derived(trojanized(_0, _1)).
derived(trusted_app_store(_0)).
derived(trusted_binary(_0)).
derived(trusted_dev_util_proxy_exec(_0)).
derived(trusted_relationship_access(_0)).
derived(trusted_third_party_access(_0)).
derived(unauthorized_command_message(_0)).
derived(unauthorized_tracking(_0)).
derived(unauthorized_wipe(_0)).
derived(unencrypted_protocol(_0)).
derived(uninstall_malicious_app(_0)).
derived(unlock_code_bypass(_0)).
derived(unlocked_bootloader(_0)).
derived(unprotected_storage(_0, _1)).
derived(unsecured_credentials(_0, _1)).
derived(unused_cloud_region_exploitation(_0, _1)).
derived(uri_hijacking_possible(_0, _1)).
derived(user_discovery(_0, _1)).
derived(user_evasion(_0)).
derived(user_execution_possible(_0, _1)).
derived(user_id(_0, _1)).
derived(user_mfa_fatigue(_0)).
derived(uses_common_port(_0, _1)).
derived(uses_signed_code(_0, _1)).
derived(valid_account_access(_0, _1)).
derived(valid_account_compromise(_0, _1)).
derived(valid_credentials(_0, _1)).
derived(version(_0, _1)).
derived(video_capture_possible(_0)).
derived(virtual_machine_discovery(_0)).
derived(virtualization_solution_exploit(_0)).
derived(vulnerable_app(_0, _1)).
derived(vulnerable_baseband(_0)).
derived(vulnerable_dll_search_order(_0)).
derived(watering_hole_attack(_0)).
derived(weaken_defense(_0)).
derived(web_service(_0)).
derived(web_service_staging(_0)).
derived(web_shell_installed(_0)).
derived(wifi_access(_0)).
derived(windows_admin_shares_accessible(_0, _1)).
derived(wireless_compromise(_0)).
derived(wireless_sniffing_possible(_0)).
derived(within_jammer_range(_0, _1)).
derived(wmi_execution_possible(_0)).

% Tabling Predicates
% Table directives must appear before any clauses that use them
:- table exec/2.
:- table compromise/2.
:- table priv_escalation/3.
:- table loot/3.
:- table exfiltrated/3.
:- table persistence/2.
:- table c2_channel/2.
:- table access/2.
:- table access_calendar_entries/1.
:- table access_contact_list/1.
:- table access_protected_user_data/1.
:- table access_sensitive_data_in_device_logs/1.
:- table access_token_manipulation/2.
:- table accesses_website/2.
:- table account_access_removal/2.
:- table account_discovery/1.
:- table account_manipulation/2.
:- table acquire_infrastructure/1.
:- table acquired_access/1.
:- table active_scanning/1.
:- table adversary_in_the_middle/1.
:- table alarm_suppression/1.
:- table app_has_permission/2.
:- table app_shimming_possible/1.
:- table app_update_malicious/2.
:- table application_window_discovery/1.
:- table archive_collected_data/1.
:- table audio_capture_possible/1.
:- table automated_collection/1.
:- table automated_exfiltration/1.
:- table autorun_vulnerability/1.
:- table bandwidth_exhaustion/2.
:- table bash_history_access/2.
:- table binary_padding_evasion/1.
:- table biometric_spoofing/1.
:- table biometric_spoofing_possible/1.
:- table bits_job_abuse/1.
:- table blackmarket_listing/2.
:- table block_serial_com/1.
:- table blocked_command_message/1.
:- table bootkit_persistence/1.
:- table botnet_traffic/2.
:- table broadcast_receiver_malware/1.
:- table browser_info_discovery/1.
:- table browser_session_hijacking/1.
:- table brute_force_io/1.
:- table brute_force_possible/2.
:- table build_image_on_host/1.
:- table builds_solution/3.
:- table bypass_policy_enforcement/1.
:- table bypass_uac/1.
:- table call_control_compromise/1.
:- table can_revert_instance/1.
:- table capability_developed/2.
:- table carrier_billing_fraud/1.
:- table change_credential/1.
:- table change_operating_mode/1.
:- table change_program_state/1.
:- table clipboard_data_access/1.
:- table clipboard_data_access/2.
:- table clipboard_modification/1.
:- table closed_source/1.
:- table cloud_dashboard_access/2.
:- table cloud_data_accessible/1.
:- table cloud_infrastructure_discovery/1.
:- table cloud_service_discovery/1.
:- table cloud_storage_object_discovery/2.
:- table cmstp_exploit/1.
:- table code_injection_possible/1.
:- table collected_point_tag_values/1.
:- table com_hijack_persistence/1.
:- table commonly_used_port/1.
:- table compile_after_delivery/1.
:- table compromise_infrastructure/1.
:- table compromised_access_token/2.
:- table compromised_binary/2.
:- table compromised_certificate/1.
:- table compromised_data/2.
:- table compromised_firmware/1.
:- table compromised_infrastructure/1.
:- table compromised_marketplace/1.
:- table concealment/2.
:- table connection_proxy/1.
:- table container_admin_command_exec/1.
:- table container_discovery/1.
:- table content_injection_possible/1.
:- table control_device_identification/1.
:- table create_account/2.
:- table credential_dumping/2.
:- table credentials_from_password_store/2.
:- table credentials_from_password_stores/2.
:- table credentials_in_files/2.
:- table critical_data_overwritten/1.
:- table critical_service/1.
:- table cybercrime_blackmarket/1.
:- table damage_to_property/1.
:- table dark_web/1.
:- table data_collected/2.
:- table data_collected_from_repo/2.
:- table data_collection/1.
:- table data_compressed/1.
:- table data_destruction/1.
:- table data_encoded_c2/1.
:- table data_encrypted_for_impact/1.
:- table data_from_config_repo/1.
:- table data_from_local_system/1.
:- table data_from_network_shared_drive/1.
:- table data_manipulation/1.
:- table data_staged/1.
:- table dde_execution_possible/1.
:- table debugger_evasion_detected/1.
:- table defacement_possible/1.
:- table default_credentials_exploitable/1.
:- table delete_device_data/1.
:- table delete_logs/2.
:- table denial_of_control/1.
:- table denial_of_service/1.
:- table denial_of_view/1.
:- table deny_functionality/1.
:- table deobfuscate_decode_files_or_information/1.
:- table deploy_container/1.
:- table detect_app_analysis_env/1.
:- table detect_operating_mode/1.
:- table detected_malware/2.
:- table device_admin_privilege_escalation/1.
:- table device_driver_discovery/1.
:- table device_lockout/1.
:- table device_restart_shutdown/1.
:- table device_unlock_code_guessing/1.
:- table dga_c2_channel/1.
:- table dga_used_for_c2/1.
:- table direct_volume_access/1.
:- table disable_vulnerable_service/1.
:- table disguise_root_jailbreak_indicators/1.
:- table disk_content_wipe/1.
:- table disk_structure_wipe/1.
:- table disk_wipe/1.
:- table distributed_attack/2.
:- table dll_search_order_hijacking/1.
:- table dll_side_loading_vulnerable/1.
:- table domain_fronting_possible/1.
:- table domain_policy_modified/1.
:- table domain_trust_discovery/1.
:- table downgrade_to_insecure_protocols/1.
:- table downloaded_capability/2.
:- table drive_by_compromise/1.
:- table dual_homed/1.
:- table dylib_hijacking_possible/1.
:- table dynamic_code_downloaded/1.
:- table eavesdrop_possible/1.
:- table email_bombing/1.
:- table email_collection/2.
:- table email_spoofing_possible/1.
:- table encoding_used/2.
:- table encrypted_c2_channel/1.
:- table encrypted_c2_traffic/1.
:- table encrypted_exfiltration/3.
:- table endpoint_dos/1.
:- table escape_to_host/1.
:- table establish_account/2.
:- table esxi_admin_command_execution/1.
:- table evasion/2.
:- table evasion_attempt/1.
:- table event_triggered_execution/1.
:- table evidence/2.
:- table ewm_injection_possible/1.
:- table exclusive_control/1.
:- table execution_guardrails/1.
:- table exfiltration_over_alternative_protocol/3.
:- table exfiltration_over_c2/3.
:- table exfiltration_over_c2_channel/2.
:- table exfiltration_over_other_medium/3.
:- table exfiltration_over_physical_medium/2.
:- table exfiltration_over_web_service/1.
:- table exfiltration_possible/1.
:- table exfiltration_possible/2.
:- table exfiltration_preparation/1.
:- table expected_app/1.
:- table expected_conditions_met/1.
:- table expected_environment/2.
:- table exploit_credential_access/2.
:- table exploit_defense_evasion/1.
:- table exploit_for_evasion/1.
:- table exploit_remote_service/1.
:- table exploit_ss7_tracking/1.
:- table exploit_tee_vulnerability/1.
:- table exploit_via_radio_interface/1.
:- table exploit_vulnerability/1.
:- table external_remote_service_access/1.
:- table fake_developer_account_compromise/1.
:- table fake_input_prompt/2.
:- table file_accessible/3.
:- table file_and_directory_discovery/1.
:- table file_deletion/1.
:- table file_directory_discovery/1.
:- table file_modified/2.
:- table file_permission_modified/2.
:- table file_system_permissions_weakness/1.
:- table financial_theft/1.
:- table firmware_accessible/1.
:- table firmware_compromise/1.
:- table firmware_update_mode_activated/1.
:- table forged_web_credential/2.
:- table fraudulent_ad_revenue/1.
:- table free_resource/1.
:- table gatekeeper_bypass/1.
:- table gather_process_state_info/1.
:- table gather_program_state_info/1.
:- table gather_victim_host_info/1.
:- table gather_victim_identity_info/1.
:- table gather_victim_network_info/1.
:- table generate_traffic_from_victim/1.
:- table geofencing_exploit/1.
:- table group_policy_discovery/1.
:- table gui_access_possible/1.
:- table hardware_addition_access/1.
:- table hidden_files_and_directories/1.
:- table hidden_user_account/2.
:- table hidden_window_exploit/1.
:- table hide_artifacts/1.
:- table hide_infrastructure/1.
:- table high_traffic_volume/2.
:- table hijack_entry_point/1.
:- table hijack_execution_flow/1.
:- table hooking_attack/1.
:- table hooking_detected/1.
:- table hooking_possible/1.
:- table hosts/2.
:- table hypervisor_persistence/1.
:- table identifies_development_requirements/2.
:- table ifeo_injection/1.
:- table impact/1.
:- table impact_on_operations/1.
:- table impact_possible/1.
:- table impersonates/2.
:- table impersonates_usb/2.
:- table impersonation_attack/2.
:- table implant_internal_image/1.
:- table indicator_blocking/1.
:- table indicator_known/2.
:- table indicator_removal/1.
:- table indicator_removal_from_tools/1.
:- table indicator_removal_on_host/1.
:- table indirect_command_execution/1.
:- table infected_pou/1.
:- table infected_project_file/1.
:- table information_available/2.
:- table ingress_tool_transfer/1.
:- table inhibit_response_function/1.
:- table inhibit_system_recovery/1.
:- table input_capture/1.
:- table input_capture/2.
:- table input_injection/1.
:- table input_injection_possible/1.
:- table insecure_permissions/2.
:- table insecure_third_party_library/3.
:- table installs_root_certificate/1.
:- table intercept_communication/1.
:- table intercept_traffic/1.
:- table internal_spearphishing/2.
:- table io_module_discovery/1.
:- table ip_spoofing/2.
:- table ipc_abuse/1.
:- table job_scheduling_persistence/1.
:- table kerberoasting_possible/2.
:- table kerberos_ticket_stolen_or_forged/2.
:- table kernel_extension_loaded/1.
:- table kernel_module_loaded/1.
:- table keychain_access/2.
:- table lateral_movement_possible/2.
:- table lateral_tool_transfer/1.
:- table launch_daemon_persistence/1.
:- table llmnr_nbt_ns_poisoning_and_relay/1.
:- table location_identification_possible/1.
:- table location_tracking/1.
:- table lockscreen_bypass/1.
:- table log_enumeration/1.
:- table loss_of_availability/1.
:- table loss_of_control/1.
:- table loss_of_productivity_and_revenue/1.
:- table loss_of_safety/1.
:- table loss_of_view/1.
:- table malicious_broadcast_receiver/1.
:- table malicious_component_installed/1.
:- table malicious_dll/1.
:- table malicious_extension_installed/1.
:- table malicious_media_content/1.
:- table malicious_password_filter_installed/1.
:- table malicious_prompt/1.
:- table malicious_software_development_tools/1.
:- table malicious_traffic/2.
:- table manipulate_device_communication/1.
:- table manipulate_io_image/1.
:- table manipulated_metadata/1.
:- table manipulated_view/1.
:- table manipulation_of_control/1.
:- table marketplace/1.
:- table masquerading_detected/1.
:- table mbr_accessible/1.
:- table member/3.
:- table member/4.
:- table member/5.
:- table member/6.
:- table member/8.
:- table member/9.
:- table mfa_interception_possible/2.
:- table mfa_request_generation_attack/2.
:- table modified_auth_process/1.
:- table modify_alarm_settings/1.
:- table modify_cloud_hierarchy/2.
:- table modify_cloud_infrastructure/1.
:- table modify_control_logic/1.
:- table modify_controller_tasking/1.
:- table modify_parameter/1.
:- table modify_program/1.
:- table modify_registry/1.
:- table modify_system_partition/1.
:- table modify_traffic/1.
:- table multi_hop_proxy_detected/1.
:- table multiband_communication/1.
:- table multiple_sources/2.
:- table native_api_access/1.
:- table native_api_execution/1.
:- table network_config_discovery/1.
:- table network_connection_enumeration/1.
:- table network_discovery/1.
:- table network_dos/1.
:- table network_dos_attack/1.
:- table network_info_discovery/1.
:- table network_service_discovery/1.
:- table network_service_scanning/1.
:- table network_share_connection_removed/1.
:- table network_share_discovery/1.
:- table network_sniffing/1.
:- table network_sniffing_possible/1.
:- table network_traffic_capture_or_redirection/1.
:- table new_service_installed/1.
:- table non_application_layer_protocol/1.
:- table non_standard_port_usage/2.
:- table non_standard_port_usage/3.
:- table notification_data_collected/1.
:- table ntfs_file_attribute_attack/1.
:- table obfuscated_payload_detected/1.
:- table obtained_capabilities/1.
:- table online_geolocation_service/2.
:- table open_technical_database/1.
:- table out_of_band_channel/1.
:- table overload_inbox/1.
:- table overwrite_mbr/1.
:- table overwrite_partition_table/1.
:- table paas_staging/1.
:- table packed_software_detected/1.
:- table partition_table_accessible/1.
:- table pass_the_ticket_feasible/2.
:- table password_policy_discovery/1.
:- table patch_vulnerable_system/1.
:- table path_interception_vulnerable/1.
:- table phishing_attempt/2.
:- table phishing_for_information/1.
:- table physical_access/1.
:- table physical_access_possible/1.
:- table plist_file_modified/1.
:- table plist_modification/1.
:- table possesses/2.
:- table potential_drive_by_compromise/1.
:- table potential_t0822/1.
:- table potential_t1223/1.
:- table potential_xsl_script_processing/1.
:- table ppid_spoofing_possible/1.
:- table privilege_escalation/2.
:- table privilege_escalation/3.
:- table privileged_access/1.
:- table process_discovery/1.
:- table process_disruption/1.
:- table process_doppelgaenging/1.
:- table process_hollowing_detected/1.
:- table process_injection_possible/1.
:- table program_download_possible/1.
:- table program_upload_possible/1.
:- table protocol/2.
:- table protocol_in_use/1.
:- table protocol_tunneling/1.
:- table proxy_execution_possible/1.
:- table proxy_through_victim/1.
:- table proxy_used/1.
:- table purchase_information/3.
:- table purchased_capability/2.
:- table purchased_infrastructure/1.
:- table query_registry/1.
:- table rdp_accessible/1.
:- table rdp_compromise/2.
:- table rdp_session_hijack/2.
:- table reconnaissance/1.
:- table reconnaissance/2.
:- table reconnaissance_opportunity/1.
:- table reconnaissance_tactic/2.
:- table redundant_access/1.
:- table reflective_code_loading/1.
:- table registers_uri/2.
:- table registry_key_modified/3.
:- table remote_access_possible/2.
:- table remote_access_tool_installed/1.
:- table remote_service_accessible/2.
:- table remote_system_discovery/1.
:- table remotely_install_app/1.
:- table removable_media_access/1.
:- table remove_other_malware/1.
:- table renamed_utility/1.
:- table reputable_private_source/1.
:- table resource_hijacking/1.
:- table revert_cloud_instance/1.
:- table rogue_access_point/1.
:- table rogue_base_station_detected/1.
:- table rogue_domain_controller/1.
:- table rogue_master_setup/1.
:- table rogue_wifi_access_point/1.
:- table role_identification/1.
:- table root_access/1.
:- table rootkit_installed/1.
:- table runtime_data_manipulation/1.
:- table scheduled_exfiltration/3.
:- table scheduled_task_abuse/2.
:- table screen_capture/1.
:- table screen_capture_possible/1.
:- table scripting_executed/2.
:- table scripting_execution/2.
:- table sells/2.
:- table serverless_execution/2.
:- table service_execution_possible/1.
:- table service_stopped/2.
:- table session_cookie_stolen/2.
:- table session_hijack_possible/2.
:- table setuid_setgid_exploit/2.
:- table shared_storage/2.
:- table shared_webroot_attack/1.
:- table signed_binary/1.
:- table signed_by_trusted_cert/1.
:- table signed_malware/1.
:- table signed_with_legit_cert/1.
:- table sim_card_swap_attack/2.
:- table sim_swap_request/2.
:- table sms_control_attack/1.
:- table social_engineering/1.
:- table software_discovery/1.
:- table source_code_payload/1.
:- table spearphishing_attachment/2.
:- table spearphishing_link/2.
:- table spearphishing_via_service/2.
:- table specialized_software_access/2.
:- table specialized_tool/1.
:- table spoof_reporting_message/1.
:- table spoofed_ip_traffic/2.
:- table ss7_exploit_possible/1.
:- table stage_capabilities/1.
:- table standard_port/2.
:- table steal_application_access_token/2.
:- table steal_or_forge_certificates/2.
:- table steal_web_session_cookie/2.
:- table stolen_auth_material/2.
:- table stolen_capability/2.
:- table stolen_token/2.
:- table stored_app_data_accessible/1.
:- table strip_privileges/1.
:- table subscription_feed/2.
:- table subvert_trust_controls/1.
:- table sudo_caching_vulnerable/1.
:- table sufficient_permissions/1.
:- table supply_chain_compromise/1.
:- table supports_operations/2.
:- table system_info_discovery/1.
:- table system_location_discovery/1.
:- table system_service_discovery/1.
:- table system_shutdown_reboot/1.
:- table system_time_discovery/1.
:- table taint_shared_content/1.
:- table tainted_content/1.
:- table template_injection_possible/1.
:- table theft_of_operational_info/1.
:- table third_party/1.
:- table traffic_signaling_detected/1.
:- table transaction/3.
:- table transient_asset_compromise/1.
:- table trap_execution/2.
:- table trojanized/2.
:- table trusted_app_store/1.
:- table trusted_binary/1.
:- table trusted_dev_util_proxy_exec/1.
:- table trusted_relationship_access/1.
:- table trusted_third_party_access/1.
:- table unauthorized_command_message/1.
:- table unauthorized_tracking/1.
:- table unauthorized_wipe/1.
:- table unencrypted_protocol/1.
:- table uninstall_malicious_app/1.
:- table unlock_code_bypass/1.
:- table unlocked_bootloader/1.
:- table unprotected_storage/2.
:- table unsecured_credentials/2.
:- table unused_cloud_region_exploitation/2.
:- table uri_hijacking_possible/2.
:- table user_discovery/2.
:- table user_evasion/1.
:- table user_execution_possible/2.
:- table user_id/2.
:- table user_mfa_fatigue/1.
:- table uses_common_port/2.
:- table uses_signed_code/2.
:- table valid_account_access/2.
:- table valid_account_compromise/2.
:- table valid_credentials/2.
:- table version/2.
:- table video_capture_possible/1.
:- table virtual_machine_discovery/1.
:- table virtualization_solution_exploit/1.
:- table vulnerable_app/2.
:- table vulnerable_baseband/1.
:- table vulnerable_dll_search_order/1.
:- table watering_hole_attack/1.
:- table weaken_defense/1.
:- table web_service/1.
:- table web_service_staging/1.
:- table web_shell_installed/1.
:- table wifi_access/1.
:- table windows_admin_shares_accessible/2.
:- table wireless_compromise/1.
:- table wireless_sniffing_possible/1.
:- table within_jammer_range/2.
:- table wmi_execution_possible/1.

% Tabling for recursive predicates (prevents infinite loops)
:- table analysis_env_detected/1.
:- table app_installed/3.
:- table attacker_modifies_app/2.
:- table block_reporting_message/1.
:- table clear_command_history/2.
:- table compress_data/2.
:- table compromise_from_exec/2.
:- table compromise_from_pe/2.
:- table compromised_device/1.
:- table connected_removable_media/2.
:- table decompiled_and_rebuilt/2.
:- table defense_disabled/2.
:- table dynamic_resolution/1.
:- table encrypt_data/2.
:- table encrypted_traffic/2.
:- table event/4.
:- table execCode/2.
:- table exec_possible_remote/2.
:- table exec_possible_user/2.
:- table exposed/2.
:- table gather_victim_org_info/2.
:- table infrastructure_acquired/1.
:- table jam_signal/2.
:- table malicious_app_installed/2.
:- table manipulate_app_store_rankings_or_ratings/1.
:- table metadata_access/2.
:- table misconfig/2.
:- table misleading_name/1.
:- table modify_account/2.
:- table modify_system_image/1.
:- table paas_service/1.
:- table persistence_enables_reentry/1.
:- table pth_feasible/2.
:- table remote_service_vulnerable/4.
:- table removable_media_infection/1.
:- table security_software_discovery/1.
:- table sends_mass_emails/1.
:- table signal_jammer_in_range/2.
:- table user_accesses_content/2.
:- table vulnerable_browser/1.
:- table weakened_defense/1.
:- dynamic interaction_rule/2.

% Utility Predicates
member(X,[X|_]).
member(X,[_|T]) :- member(X,T).

append([],L,L).
append([H|T],L,[H|R]) :- append(T,L,R).

reverse(L,R) :- reverse_acc(L,[],R).
reverse_acc([],Acc,Acc).
reverse_acc([H|T],Acc,R) :- reverse_acc(T,[H|Acc],R).

file_extension(File,Ext) :- atom(File), atom_codes(File,Cs), reverse(Cs,RCs), append(ExtCodes,[46|_],RCs), !, reverse(ExtCodes,ExtRev), atom_codes(Ext,ExtRev).

protocol(Host,Proto) :- protocol(Host,Proto,_).

protocol_in_use(Proto) :- protocol(_,Proto,_).
uses_common_port(Host,Port) :- commonly_used_port(Host,Port).
encrypted_c2_channel(Host) :- encrypted_c2_traffic(Host).
dga_c2_channel(Host) :- dga_used_for_c2(Host).

% Interaction Rules
% --- T0800_activate_firmware_update_mode ---
    firmware_update_mode_activated(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        role(Host, protection_relay).

    deny_functionality(Host) :-
        firmware_update_mode_activated(Host),
        defense_disabled(Host, process_monitoring).

% --- T0801_monitor_process_state ---
    gather_process_state_info(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        gather_process_state_info(Host).

% --- T0802_automated_collection ---
    automated_collection(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        protocol(Host, opc, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        automated_collection(Host).

% --- T0803_block_command_message ---
    blocked_command_message(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    inhibit_response_function(Host) :-
        blocked_command_message(Host).

% --- T0804_block_reporting_message ---
    block_reporting_message(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port),
        sensor(Host, telemetry),
        compromised_device(Host).

compromised_device(Host) :-
    block_reporting_message(Host).

% --- T0805_block_serial_com ---
    block_serial_com(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        protocol(Host, 'telnet', Port),
        Port >= 20001,
        Port =< 20003.

% --- T0806_brute_force_i_o ---
    brute_force_io(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        brute_force_io(Host).

% --- T0807_command-line_interface ---
    exec(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        auth_possible(Host, User, Method),
        auth_usable_cred(User, Host, Method).

    compromise_from_exec(Host, user) :-
        exec(Host, User).

% --- T0808_control_device_identification ---
    control_device_identification(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        device(Host).

    compromise(Host, user) :-
        control_device_identification(Host).

% --- T0809_data_destruction ---
    data_destruction(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        has_data(Host, sensitive),
        compromised_device(Host).

    compromise(Host, user) :-
        data_destruction(Host).

% --- T0811_data_from_information_repositories ---
    data_collected_from_repo(Host, DataKind) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, DataKind).

    compromise(Host, user) :-
        data_collected_from_repo(Host, _DataKind).

% --- T0812_default_credentials ---
    default_credentials_exploitable(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        auth_usable_cred(User, Host, default),
        perm(Host, User, admin).

    compromise(Host, admin) :-
        default_credentials_exploitable(Host).

% --- T0813_denial_of_control ---
    denial_of_control(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        role(Host, control_device).

    compromise(Host, user) :-
        denial_of_control(Host).

% --- T0814_denial_of_service ---
    denial_of_service(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, Proto, Svc, Version),
        remote_service_vulnerable(Host, Port, CVE, Svc),
        exec_possible_remote(Host, User).

    compromise(Host, user) :-
        denial_of_service(Host).

% --- T0815_denial_of_view ---
    denial_of_view(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        block_reporting_message(Host).

    compromise(Host, user) :-
        denial_of_view(Host).

% --- T0816_device_restart_shutdown ---
    device_restart_shutdown(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        device_restart_shutdown(Host).

% --- T0817_drive-by_compromise ---
    watering_hole_attack(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        exec_possible_user(Host, User).

    compromise(Host, user) :-
        watering_hole_attack(Host).

% --- T0818_engineering_workstation_compromise ---
    compromised_device(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        role(Host, engineering_workstation),
        (valid_account(User, Host); removable_media_infection(Host)),
        dual_homed(Host).

    dual_homed(Host) :-
        connects(Network1, Host, _),
        connects(Network2, Host, _),
        Network1 \= Network2.

% --- T0819_exploit_public-facing_application ---
    exposed(Host, Port) :-
        attacker_at(net),
        connects(net, Host, Port).

    remote_service_vulnerable(Host, Port, CVE, Service) :-
        exposed(Host, Port),
        service(Host, Port, _Proto, Service, _Version),
        service_matches(CVE, Service).

    compromise(Host, user) :-
        remote_service_vulnerable(Host, _Port, _CVE, _Service).

% --- T0820_exploitation_for_evasion ---
    exploit_for_evasion(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        remote_service_vulnerable(Host, Port, CVE, Service),
        defense_disabled(Host, SecurityFeature),
        exec_possible_remote(Host, User).

    compromise(Host, user) :-
        exploit_for_evasion(Host).

% --- T0821_modify_controller_tasking ---
    modify_controller_tasking(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port),
        exec_possible_remote(Host, User).

    compromise(Host, user) :-
        modify_controller_tasking(Host).

% --- T0822_external_remote_services ---
    potential_t0822(Host) :-
        attacker_at(net),
        connects(net, Host, Port),
        exposed(Host, Port),
        valid_account(User, Host).

    compromise(Host, user) :-
        potential_t0822(Host).

% --- T0823_graphical_user_interface ---
    gui_access_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, Protocol, Service, Version),
        (Protocol = 'VNC'; Protocol = 'RDP').

compromise(Host, user) :-
    gui_access_possible(Host).

% --- T0824_i_o_module_discovery ---
    io_module_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        role(Host, plc),
        exposed(Host, Port).

    compromise(Host, user) :-
        io_module_discovery(Host).

% --- T0825_location_identification ---
    location_identification_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (has_data(Host, gps_coordinates);
         has_data(Host, timezone_info);
         has_data(Host, serial_connection_info)).

    compromise(Host, user) :-
        location_identification_possible(Host).

% --- T0826_loss_of_availability ---
    loss_of_availability(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        has_data(Host, critical),
        exec_possible_remote(Host, user),
        compromise(Host, user).

    compromise(Host, user) :-
        loss_of_availability(Host).

% --- T0827_loss_of_control ---
    loss_of_control(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        compromise(Host, control_system).

    compromise(Host, control_system) :-
        loss_of_control(Host).

% --- T0828_loss_of_productivity_and_revenue ---
    loss_of_productivity_and_revenue(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        compromise(Host, user).

    impact_on_operations(Host) :-
        loss_of_productivity_and_revenue(Host),
        event(Host, impact, 'T0828', _EvidenceId).

% --- T0829_loss_of_view ---
    loss_of_view(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        block_reporting_message(Host).

    compromise(Host, user) :-
        loss_of_view(Host).

% --- T0830_adversary-in-the-middle ---
    adversary_in_the_middle(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        privileged_access(Network).

    intercept_traffic(Host) :-
        adversary_in_the_middle(Host),
        connects(_, Host, _).

    modify_traffic(Host) :-
        intercept_traffic(Host).

    block_reporting_message(Host) :-
        modify_traffic(Host).

    spoof_reporting_message(Host) :-
        modify_traffic(Host).

    modify_parameter(Host) :-
        modify_traffic(Host).

    unauthorized_command_message(Host) :-
        modify_traffic(Host).

% --- T0831_manipulation_of_control ---
    manipulation_of_control(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        compromised_device(Host).

    compromise(Host, user) :-
        manipulation_of_control(Host).

% --- T0832_manipulation_of_view ---
    manipulated_view(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        manipulated_view(Host).

% --- T0833_modify_control_logic ---
    modify_control_logic(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port),
        ladder_logic(Host, Program),
        exec_possible_remote(Host, user),
        compromise(Host, user).

    compromise(Host, user) :-
        modify_control_logic(Host).

% --- T0834_native_api ---
    native_api_access(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        exec(Host, user),
        compromise(Host, user).

    compromise(Host, user) :-
        native_api_access(Host).

% --- T0835_manipulate_i_o_image ---
    manipulate_io_image(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        plc(Host),
        compromised_device(Host).

compromise(Host, user) :-
    manipulate_io_image(Host).

% --- T0836_modify_parameter ---
    modify_parameter(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        ics_endpoint(Host, _Port).

    compromise(Host, user) :-
        modify_parameter(Host).

% --- T0837_loss_of_protection ---
    defense_disabled(Host, protection_system) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port).

    process_disruption(Host) :-
        defense_disabled(Host, protection_system).

% --- T0838_modify_alarm_settings ---
    modify_alarm_settings(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        compromise(Host, user),
        defense_disabled(Host, alarm_settings).

    impact_possible(Host) :-
        modify_alarm_settings(Host),
        block_reporting_message(Host).

% --- T0839_module_firmware ---
    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, Port),
        exposed(Host, Port),
        device(Host),
        persistence(Host, 'malicious_firmware').

    persistence_enables_reentry(Host) :-
        compromised_device(Host).

% --- T0840_network_connection_enumeration ---
    network_connection_enumeration(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        network_connection_enumeration(Host).

% --- T0841_network_service_scanning ---
    network_service_scanning(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        network_service_scanning(Host).

% --- T0842_network_sniffing ---
    network_sniffing_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        protocol(Host, Proto, Port),
        unencrypted_protocol(Proto).

    compromise(Host, user) :-
        network_sniffing_possible(Host),
        has_data(Host, credentials),
        exfiltrated(Host, credentials, network).

% --- T0843_program_download ---
    program_download_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        program_download_possible(Host).

% --- T0844_program_organization_units ---
    infected_pou(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        plc(Host),
        ladder_logic(Host, Program),
        execCode(Host, user),
        compromise(Host, user).

    compromise(Host, user) :-
        infected_pou(Host).

% --- T0845_program_upload ---
    program_upload_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        app_installed(Host, 'VendorSoftware', _Version),
        role(Host, 'plc').

    compromise(Host, user) :-
        program_upload_possible(Host).

% --- T0846_remote_system_discovery ---
    remote_system_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, _Port),
        exposed(Host, _Port).

    compromise(Host, user) :-
        remote_system_discovery(Host).

% --- T0847_replication_through_removable_media ---
    removable_media_infection(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        physical_access(Host),
        trusted_third_party_access(Host).

    compromise(Host, user) :-
        removable_media_infection(Host).

% --- T0848_rogue_master ---
    rogue_master_setup(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        rogue_master_setup(Host).

% --- T0849_masquerading ---
    masquerading_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, Version),
        expected_app(App),
        not(malicious_app(App)).

    compromise(Host, user) :-
        masquerading_detected(Host).

% --- T0850_role_identification ---
    role_identification(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        role(Host, Role),
        exfiltrated(Host, role_data, Channel).

    compromise(Host, user) :-
        role_identification(Host).

% --- T0851_rootkit ---
    rootkit_installed(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        compromise(Host, kernel).

    compromise(Host, kernel) :-
        rootkit_installed(Host).

% --- T0852_screen_capture ---
    screen_capture_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        role(Host, hmi).

    compromise(Host, user) :-
        screen_capture_possible(Host).

% --- T0853_scripting ---
    scripting_executed(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'scripting_interpreter', _Version),
        exec(Host, User).

    compromise(Host, user) :-
        scripting_executed(Host, user).

% --- T0855_unauthorized_command_message ---
    unauthorized_command_message(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        unauthorized_command_message(Host).

% --- T0856_spoof_reporting_message ---
    spoof_reporting_message(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port).

    compromise(Host, user) :-
        spoof_reporting_message(Host).

% --- T0858_change_operating_mode ---
    change_operating_mode(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        change_operating_mode(Host).

% --- T0859_valid_accounts ---
    valid_account_access(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        valid_account(User, Host),
        auth_usable_cred(User, Host, _Method).

    compromise(Host, user) :-
        valid_account_access(Host, User).

% --- T0860_wireless_compromise ---
    wireless_compromise(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        compromised_device(Device),
        device(Device),
        protocol(Device, wireless, Port).

    compromise(Host, user) :-
        wireless_compromise(Host).

% --- T0861_point_tag_identification ---
    collected_point_tag_values(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        collected_point_tag_values(Host).

% --- T0862_supply_chain_compromise ---
    supply_chain_compromise(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        trojanized(App, Version).

    compromise(Host, user) :-
        supply_chain_compromise(Host).

% --- T0863_user_execution ---
    user_execution_possible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        exec_possible_user(Host, User).

compromise(Host, user) :-
    user_execution_possible(Host, User).

% --- T0864_transient_cyber_asset ---
    transient_asset_compromise(Host) :-
        attacker_at(ExternalNet),
        connects(ExternalNet, Host, _Port),
        misconfig(Host, _Issue),
        app_installed(Host, _App, _Version).

    compromise(Host, user) :-
        transient_asset_compromise(Host).

% --- T0865_spearphishing_attachment ---
    spearphishing_attachment(User, Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        user_phishes_success(User).

    compromise(Host, user) :-
        spearphishing_attachment(User, Host),
        exec_possible_user(Host, User).

% --- T0866_exploitation_of_remote_services ---
    exploit_remote_service(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        remote_service_vulnerable(Host, Port, 'CVE-2017-0144', 'SMBv1').

    compromise(Host, user) :-
        exploit_remote_service(Host).

% --- T0867_lateral_tool_transfer ---
    lateral_tool_transfer(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, "smb", _, _).

    compromise(Host, user) :-
        lateral_tool_transfer(Host).

% --- T0868_detect_operating_mode ---
    detect_operating_mode(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        plc(Host),
        exposed(Host, Port).

    compromise(Host, user) :-
        detect_operating_mode(Host).

% --- T0869_standard_application_layer_protocol ---
    c2_channel(Host, Proto) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        protocol(Proto, Port),
        member(Proto, [http, https, opc, rdp, telnet, dnp3, modbus]).

    compromise(Host, user) :-
        c2_channel(Host, _Proto).

% --- T0870_detect_program_state ---
    gather_program_state_info(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        plc(Host),
        exposed(Host, Port).

    compromise(Host, user) :-
        gather_program_state_info(Host).

% --- T0871_execution_through_api ---
    exec_possible_remote(Host, user) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).
    compromise_from_exec(Host, user) :-
        exec_possible_remote(Host, user).

% --- T0872_indicator_removal_on_host ---
    indicator_removal_on_host(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        compromised_device(Host),
        weaken_defense(Host).

    compromised_device(Host) :-
        exec(Host, User),
        delete_logs(Host, User).

    weaken_defense(Host) :-
        defense_disabled(Host, logging).

% --- T0873_project_file_infection ---
    infected_project_file(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        plc(PLC),
        role(Host, engineering_workstation),
        exec(PLC, malicious_code),
        persistence(PLC, project_file_infection).

    compromise(Host, user) :-
        infected_project_file(Host).

% --- T0874_hooking ---
    hooking_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'Windows', _Version),
        exposed(Host, _Port).

    compromise(Host, user) :-
        hooking_possible(Host).

% --- T0875_change_program_state ---
    change_program_state(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_endpoint(Host, Port),
        exec_possible_remote(Host, user),
        compromise_from_exec(Host, user).

    compromise(Host, user) :-
        change_program_state(Host).

% --- T0878_alarm_suppression ---
    alarm_suppression(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        defense_disabled(Host, alarm_system).

    compromise(Host, user) :-
        alarm_suppression(Host).

% --- T0879_damage_to_property ---
    damage_to_property(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port),
        compromise(Host, user).

    event(Host, impact, 'T0879', EvidenceId) :-
        damage_to_property(Host),
        evidence(EvidenceId, 'Damage to Property observed').

% --- T0880_loss_of_safety ---
    loss_of_safety(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        safety_instrumented(Host),
        defense_disabled(Host, safety_system).

    compromise(Host, user) :-
        loss_of_safety(Host).

% --- T0881_service_stop ---
    service_stopped(Host, Service) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, _Proto, Service, _Version),
        compromised_device(Host).

    compromise(Host, user) :-
        service_stopped(Host, _Service).

% --- T0882_theft_of_operational_information ---
    theft_of_operational_info(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, operational_information),
        exfiltrated(Host, operational_information, Network).

    compromise(Host, user) :-
        theft_of_operational_info(Host).

% --- T0883_internet_accessible_device ---
    exposed(Host, Port) :-
        attacker_at(net),
        connects(net, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        exposed(Host, Port),
        auth_possible(Host, user, method),
        auth_usable_cred(user, Host, method).

% --- T0884_connection_proxy ---
    connection_proxy(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        trust(Host, TrustedHost, Mechanism),
        c2_channel(Host, Protocol).

    compromise(Host, user) :-
        connection_proxy(Host).

% --- T0885_commonly_used_port ---
    c2_channel(Host, Port) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        commonly_used_port(Port).

    compromise(Host, user) :-
        c2_channel(Host, _Port).

    commonly_used_port(80).
    commonly_used_port(443).
    commonly_used_port(53).
    commonly_used_port(23).
    commonly_used_port(161).
    commonly_used_port(502).
    commonly_used_port(102).
    commonly_used_port(20000).
    commonly_used_port(44818).
    commonly_used_port(Port) :-
        Port >= 1024,
        Port =< 4999.
    commonly_used_port(Port) :-
        Port >= 49152,
        Port =< 65535.

% --- T0886_remote_services ---
    remote_service_accessible(Host, Service) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _, Service, _).

    compromise(Host, user) :-
        remote_service_accessible(Host, _),
        valid_account(User, Host).

% --- T0887_wireless_sniffing ---
    wireless_sniffing_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        protocol(Host, Protocol, Port),
        member(Protocol, [wirelesshart, zigbee, wiafa, public_safety_spectrum]),
        not(encrypted_communication(Host, Protocol)).

    compromise(Host, user) :-
        wireless_sniffing_possible(Host).

% --- T0889_modify_program ---
    modify_program(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        ics_exposed_endpoint(Host, Port).

    compromise(Host, user) :-
        modify_program(Host).

% --- T0890_exploitation_for_privilege_escalation ---
    priv_escalation(Host, user, root) :-
        attacker_at(net),
        connects(net, Host, Port),
        local_pe_vulnerable(Host, CVE),
        exec(Host, user),
        compromise_from_pe(Host, root).

% --- T0892_change_credential ---
    change_credential(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        auth_usable_cred(User, Host, method),
        compromised_device(Host).

    compromised_device(Host) :-
        change_credential(Host).

% --- T0893_data_from_local_system ---
    data_collected(Host, DataKind) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        exec(Host, User).

    compromise(Host, user) :-
        data_collected(Host, _).

% --- T0894_system_binary_proxy_execution ---
    proxy_execution_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version),
        signed_binary(App),
        trusted_binary(App).

    compromise(Host, user) :-
        proxy_execution_possible(Host).

% --- T0895_autorun_image ---
    autorun_vulnerability(Host) :-
        setting(Host, autorun, enabled),
        removable_media_infection(Media),
        connects(net, Host, _Port).

    exec(Host, user) :-
        autorun_vulnerability(Host).

% --- T1002_data_compressed ---
    data_compressed(Host) :-
        has_data(Host, sensitive),
        app_installed(Host, compression_tool, _Version),
        attacker_at(net),
        connects(net, Host, _Port).

    exfiltration_possible(Host) :-
        data_compressed(Host),
        exfiltrated(Host, compressed_data, network_channel).

% --- T1003_os_credential_dumping ---
    credential_dumping(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        exposed(Host, Port),
        exec_possible_remote(Host, User).

    compromise(Host, user) :-
        credential_dumping(Host, User).

% --- T1004_winlogon_helper_dll ---
    persistence(Host, winlogon_helper_dll) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        setting(Host, 'HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\Notify', _),
        setting(Host, 'HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\Userinit', _),
        setting(Host, 'HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\Shell', _).

    compromise(Host, user) :-
        persistence(Host, winlogon_helper_dll).

% --- T1005_data_from_local_system ---
    data_collection(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        has_data(Host, sensitive).

    compromise(Host, user) :-
        data_collection(Host).

% --- T1006_direct_volume_access ---
    direct_volume_access(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        app_installed(Host, 'NinjaCopy', _Version).

    compromise(Host, user) :-
        direct_volume_access(Host).

% --- T1007_system_service_discovery ---
    system_service_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, OS),
        member(OS, [windows, macos, linux]),
        exposed(Host, Port).

    compromise(Host, user) :-
        system_service_discovery(Host).

% --- T1009_binary_padding ---
    binary_padding_evasion(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        defense_disabled(Host, antivirus),
        weakened_defense(Host).

    compromise(Host, user) :-
        binary_padding_evasion(Host).

% --- T1010_application_window_discovery ---
    application_window_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, OS),
        member(OS, [macOS, Windows, Linux]),
        exec(Host, User),
        compromise(Host, user).

    compromise(Host, user) :-
        application_window_discovery(Host).

% --- T1011_exfiltration_over_other_network_medium ---
    exfiltration_over_other_medium(Host, DataKind, Channel) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        exfiltrated(Host, DataKind, Channel),
        Channel \= Network.

% --- T1012_query_registry ---
    query_registry(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        compromise(Host, user).

compromise(Host, user) :-
    query_registry(Host).

% --- T1013_port_monitors ---
    persistence(Host, 'port_monitor') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, 'Windows'),
        setting(Host, 'HKLM\\SYSTEM\\CurrentControlSet\\Control\\Print\\Monitors', _),
        exec(Host, 'SYSTEM').

    compromise(Host, 'SYSTEM') :-
        persistence(Host, 'port_monitor').

% --- T1014_rootkit ---
    rootkit_installed(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, OS),
        member(OS, [linux, macos, windows]).

    compromise(Host, kernel) :-
        rootkit_installed(Host).

% --- T1016_system_network_configuration_discovery ---
    network_config_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _, "network_util", _).

    compromise(Host, user) :-
        network_config_discovery(Host).

% --- T1017_application_deployment_software ---
    exec_possible_remote(Host, admin) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        role(Host, deployment_server),
        auth_usable_cred(User, Host, domain),
        perm(Host, User, admin).

    compromise(Host, user) :-
        exec_possible_remote(Host, admin).

% --- T1018_remote_system_discovery ---
    remote_system_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, Service, _),
        member(Service, [ping, net_view, esxcli, arp, cdp]).
    compromise(Host, user) :-
        remote_system_discovery(Host).

% --- T1019_system_firmware ---
    firmware_compromise(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        persistence(Host, system_firmware).

    compromise(Host, user) :-
        firmware_compromise(Host).

% --- T1020_automated_exfiltration ---
    automated_exfiltration(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, sensitive),
        exfiltrated(Host, sensitive, automated).

    compromise(Host, user) :-
        automated_exfiltration(Host).

% --- T1021_remote_services ---
    remote_access_possible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        auth_possible(Host, User, Method),
        member(Method, [ssh, rdp, vnc]).

    compromise(Host, user) :-
        remote_access_possible(Host, User).

% --- T1022_data_encrypted ---
    encrypted_exfiltration(Host, DataKind, Channel) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        exfiltrated(Host, DataKind, Channel),
        Channel \= 'unencrypted'.

    compromise(Host, user) :-
        encrypted_exfiltration(Host, _, _).

% --- T1024_custom_cryptographic_protocol ---
    c2_channel(Host, custom_crypto) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _, _, _),
        compromised_device(Host).

% --- T1025_data_from_removable_media ---
    removable_media_access(Host) :-
        compromised_device(Host),
        connected_removable_media(Host, Media),
        has_data(Media, sensitive).

    connected_removable_media(Host, Media) :-
        device(Media),
        connects(Host, Media, _Port).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port).

% --- T1026_multiband_communication ---
    multiband_communication(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port1),
        connects(Network, Host, Port2),
        Port1 \= Port2,
        c2_channel(Host, Protocol1),
        c2_channel(Host, Protocol2),
        Protocol1 \= Protocol2.

    compromise(Host, user) :-
        multiband_communication(Host).

% --- T1027_obfuscated_files_or_information ---
    obfuscated_payload_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        defense_disabled(Host, obfuscation_detection).

    compromise(Host, user) :-
        obfuscated_payload_detected(Host).

% --- T1028_windows_remote_management ---
    exec_possible_remote(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, tcp, winrm, _Version),
        auth_possible(Host, User, winrm).

    compromise(Host, user) :-
        exec_possible_remote(Host, User).

% --- T1029_scheduled_transfer ---
    scheduled_exfiltration(Host, DataKind, Channel) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        exfiltrated(Host, DataKind, Channel),
        (c2_channel(Host, Channel); protocol(Channel, _, _)).

% --- T1030_data_transfer_size_limits ---
    exfiltrated(Host, data_chunk, network_channel) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, sensitive),
        not(alert_triggered(Host, data_transfer)).

% --- T1032_standard_cryptographic_protocol ---
    encrypted_c2_traffic(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        protocol(Protocol, 'encrypted', Port).

    compromise(Host, user) :-
        encrypted_c2_traffic(Host).

% --- T1033_system_owner_user_discovery ---
    user_discovery(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host).

    compromise(Host, user) :-
        user_discovery(Host, _User).

% --- T1034_path_interception ---
    path_interception_vulnerable(Host) :-
        os(Host, windows),
        misconfig(Host, unquoted_path),
        attacker_at(net),
        connects(net, Host, _Port).

    path_interception_vulnerable(Host) :-
        os(Host, windows),
        misconfig(Host, path_env_var),
        attacker_at(net),
        connects(net, Host, _Port).

    path_interception_vulnerable(Host) :-
        os(Host, windows),
        misconfig(Host, search_order_hijacking),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        path_interception_vulnerable(Host).

% --- T1035_service_execution ---
    service_execution_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _Proto, "Service Control Manager", _Version).

    compromise_from_exec(Host, user) :-
        service_execution_possible(Host).

% --- T1036_masquerading ---
    masquerading_detected(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (manipulated_metadata(Host);
         renamed_utility(Host);
         misleading_name(Host)).

    compromise(Host, user) :-
        masquerading_detected(Host).

% --- T1037_boot_or_logon_initialization_scripts ---
    persistence(Host, 'boot_or_logon_initialization_scripts') :-
        attacker_at(net),
        connects(net, Host, _Port),
        (valid_account(User, Host); perm(Host, User, admin)),
        exec(Host, User).

    priv_escalation(Host, user, admin) :-
        persistence(Host, 'boot_or_logon_initialization_scripts'),
        perm(Host, user, user).

% --- T1038_dll_search_order_hijacking ---
    dll_search_order_hijacking(Host) :-
        os(Host, windows),
        app_installed(Host, App, _Version),
        vulnerable_dll_search_order(App),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, admin) :-
        dll_search_order_hijacking(Host),
        perm(Host, user, admin).

% --- T1039_data_from_network_shared_drive ---
    data_from_network_shared_drive(Host) :-
        compromised_device(Host),
        connects(Network, Host, Port),
        has_data(Host, sensitive),
        exposed(Host, Port).

    compromise(Host, user) :-
        data_from_network_shared_drive(Host).

% --- T1040_network_sniffing ---
    network_sniffing(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, Proto, _, _),
        Proto \= 'https',
        Proto \= 'tls'.

    compromise(Host, user) :-
        network_sniffing(Host).

% --- T1041_exfiltration_over_c2_channel ---
    exfiltration_over_c2(Host, DataKind, Proto) :-
        attacker_at(net),
        connects(net, Host, _Port),
        c2_channel(Host, Proto),
        has_data(Host, DataKind),
        exfiltrated(Host, DataKind, Proto).

% --- T1042_change_default_file_association ---
    registry_key_modified(Host, 'HKEY_CLASSES_ROOT', Extension) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, 'Windows'),
        setting(Host, 'HKEY_CLASSES_ROOT\\' + Extension, _Value).

    persistence(Host, 'Change Default File Association') :-
        registry_key_modified(Host, 'HKEY_CLASSES_ROOT', _Extension).

% --- T1043_commonly_used_port ---
    c2_channel(Host, Proto) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        member(Port, [80, 443, 25, 53, 135, 22, 3389]),
        protocol(Proto, _, Port).

% --- T1044_file_system_permissions_weakness ---
    file_system_permissions_weakness(Host) :-
        misconfig(Host, 'file_permissions'),
        exec(Host, User),
        perm(Host, User, low),
        priv_escalation(Host, low, high).

    compromise(Host, high) :-
        file_system_permissions_weakness(Host).

% --- T1045_software_packing ---
    packed_software_detected(Host) :-
        app_installed(Host, 'packer', _Version),
        os(Host, 'Windows'),
        defense_disabled(Host, 'signature-based detection').

    packed_software_detected(Host) :-
        app_installed(Host, 'packer', _Version),
        os(Host, 'macOS'),
        defense_disabled(Host, 'signature-based detection').

    compromise(Host, user) :-
        packed_software_detected(Host).

% --- T1046_network_service_discovery ---
    network_service_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        network_service_discovery(Host).

% --- T1047_windows_management_instrumentation ---
    wmi_execution_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, tcp, 'WMI', _),
        (Port = 135; Port = 5985; Port = 5986).

    compromise(Host, user) :-
        wmi_execution_possible(Host).

% --- T1049_system_network_connections_discovery ---
    network_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromised_device(Host).

compromise(Host, user) :-
    network_discovery(Host).

% --- T1050_new_service ---
    new_service_installed(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        perm(Host, User, administrator),
        exec(Host, User).

    priv_escalation(Host, administrator, system) :-
        new_service_installed(Host).

    persistence(Host, new_service) :-
        new_service_installed(Host).

% --- T1051_shared_webroot ---
    shared_webroot_attack(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, 'web_server', _),
        misconfig(Host, 'open_network_share'),
        exec_possible_remote(Host, 'web_server_user').

    compromise(Host, 'system') :-
        shared_webroot_attack(Host),
        priv_escalation(Host, 'web_server_user', 'system').

% --- T1052_exfiltration_over_physical_medium ---
    exfiltration_over_physical_medium(Host, Device) :-
        attacker_at(net),
        connects(net, Host, _Port),
        connected_removable_media(Host, Device),
        has_data(Host, sensitive),
        exfiltrated(Host, sensitive, Device).

% --- T1053_scheduled_task_job ---
    scheduled_task_abuse(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        perm(Host, User, admin),
        auth_possible(Host, User, rpc),
        exec_possible_remote(Host, User).

    compromise(Host, user) :-
        scheduled_task_abuse(Host, User).

% --- T1054_indicator_blocking ---
    indicator_blocking(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        (defense_disabled(Host, etw) ;
         defense_disabled(Host, firewall)),
        compromised_device(Host).

    compromised_device(Host) :-
        defense_disabled(Host, etw).

    compromised_device(Host) :-
        defense_disabled(Host, firewall).

% --- T1056_input_capture ---
    input_capture(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        exposed(Host, Port),
        valid_account(User, Host).

    compromise(Host, user) :-
        input_capture(Host, User).

% --- T1057_process_discovery ---
    process_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        perm(Host, user, admin).

    compromise(Host, user) :-
        process_discovery(Host).

% --- T1058_service_registry_permissions_weakness ---
    misconfig(Host, 'ServiceRegistryPermissions') :-
        os(Host, 'Windows'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise_from_pe(Host, 'SYSTEM') :-
        misconfig(Host, 'ServiceRegistryPermissions'),
        exec(Host, 'attacker_controlled_program').

    persistence(Host, 'ServiceRegistryPermissions') :-
        misconfig(Host, 'ServiceRegistryPermissions'),
        exec(Host, 'attacker_controlled_program').

% --- T1059_command_and_scripting_interpreter ---
    exec(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'CommandInterpreter', _Version),
        user_on_host(User, Host).

    compromise(Host, user) :-
        exec(Host, User).

% --- T1060_registry_run_keys_startup_folder ---
    persistence(Host, 'Registry Run Keys / Startup Folder') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        user_on_host(User, Host),
        exec(Host, User).

    compromise(Host, user) :-
        persistence(Host, 'Registry Run Keys / Startup Folder').

% --- T1062_hypervisor ---
    hypervisor_persistence(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        persistence(Host, hypervisor_rootkit).

    compromise(Host, user) :-
        hypervisor_persistence(Host).

% --- T1063_security_software_discovery ---
    security_software_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, security_software, _Version).

    compromise(Host, user) :-
        security_software_discovery(Host).

% --- T1064_scripting ---
    scripting_execution(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, User).

compromise(Host, user) :-
    scripting_execution(Host, user).

% --- T1065_uncommonly_used_port ---
    c2_channel(Host, proto) :-
        attacker_at(net),
        connects(net, Host, Port),
        exposed(Host, Port),
        misconfig(Host, 'improperly configured firewall or proxy').

% --- T1066_indicator_removal_from_tools ---
    indicator_removal_from_tools(Host) :-
        detected_malware(Host, Malware),
        indicator_known(Malware, Indicator),
        attacker_at(net),
        connects(net, Host, _Port),
        modify_malware(Malware, Indicator, ModifiedMalware),
        not(detected_malware(Host, ModifiedMalware)).

    compromise(Host, user) :-
        indicator_removal_from_tools(Host).

% --- T1067_bootkit ---
    bootkit_persistence(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, linux),
        persistence(Host, bootkit).

    bootkit_persistence(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        persistence(Host, bootkit).

    compromise(Host, kernel) :-
        bootkit_persistence(Host).

% --- T1068_exploitation_for_privilege_escalation ---
    priv_escalation(Host, low_priv, high_priv) :-
        attacker_at(net),
        connects(net, Host, _Port),
        local_pe_vulnerable(Host, _CVE),
        exec(Host, low_priv),
        perm(Host, low_priv, user),
        perm(Host, high_priv, root).

    compromise(Host, root) :-
        priv_escalation(Host, low_priv, high_priv).

% --- T1070_indicator_removal ---
    indicator_removal(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromised_device(Host),
        defense_disabled(Host, logging),
        weakened_defense(Host).

    compromised_device(Host) :-
        indicator_removal(Host).

% --- T1071_application_layer_protocol ---
    c2_channel(Host, Protocol) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        protocol(Host, Protocol, Port).

    compromise(Host, user) :-
        c2_channel(Host, _).

% --- T1072_software_deployment_tools ---
    exec_possible_remote(Host, user) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'SoftwareDeploymentTool', _Version),
        perm(Host, user, admin).

    compromise(Host, admin) :-
        exec_possible_remote(Host, user).

% --- T1073_dll_side-loading ---
    dll_side_loading_vulnerable(Host) :-
        app_installed(Host, App, Version),
        misconfig(Host, 'dll_loading'),
        os(Host, 'Windows').

    compromise(Host, user) :-
        dll_side_loading_vulnerable(Host),
        attacker_at(net),
        connects(net, Host, _Port).

% --- T1074_data_staged ---
    data_staged(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, sensitive),
        compromise(Host, user).

    exfiltration_preparation(Host) :-
        data_staged(Host),
        c2_channel(Host, Protocol).

% --- T1075_pass_the_hash ---
    pth_feasible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        auth_usable_cred(User, Host, hash),
        accepts_hash(Host, User).

    compromise(Host, user) :-
        pth_feasible(Host, User).

% --- T1076_remote_desktop_protocol ---
    rdp_accessible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, tcp, rdp, _Version).

    valid_credentials(User, Host) :-
        auth_usable_cred(User, Host, rdp).

    rdp_compromise(Host, User) :-
        rdp_accessible(Host),
        valid_credentials(User, Host),
        exec(Host, User).

    rdp_session_hijack(Host, User) :-
        rdp_accessible(Host),
        perm(Host, User, system),
        exec(Host, User).

    compromise(Host, user) :-
        rdp_compromise(Host, _User).

    compromise(Host, admin) :-
        rdp_session_hijack(Host, _User).

% --- T1077_windows_admin_shares ---
    windows_admin_shares_accessible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        auth_possible(Host, User, smb),
        perm(Host, User, admin).

    compromise(Host, admin) :-
        windows_admin_shares_accessible(Host, User).

% --- T1078_valid_accounts ---
    valid_account_compromise(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        auth_usable_cred(User, Host, Method),
        auth_possible(Host, User, Method).

    compromise(Host, user) :-
        valid_account_compromise(Host, User).

% --- T1079_multilayer_encryption ---
    c2_channel(Host, Protocol) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, Protocol, _, _),
        (Protocol = https; Protocol = smtps),
        compromised_device(Host).

% --- T1080_taint_shared_content ---
    taint_shared_content(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        shared_storage(Host, Storage),
        tainted_content(Storage),
        user_accesses_content(User, Storage),
        exec(Host, User).

    compromise(Host, user) :-
        taint_shared_content(Host).

% --- T1081_credentials_in_files ---
    credentials_in_files(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, credentials),
        user_on_host(User, Host).

    compromise(Host, user) :-
        credentials_in_files(Host, User),
        auth_usable_cred(User, Host, password).

% --- T1082_system_information_discovery ---
    system_info_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        perm(Host, User, Privilege),
        Privilege \= none.

% --- T1083_file_and_directory_discovery ---
    file_and_directory_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, User),
        perm(Host, User, Privilege),
        Privilege \= none.

    compromise(Host, user) :-
        file_and_directory_discovery(Host).

% --- T1084_windows_management_instrumentation_event_subscription ---
    persistence(Host, wmi_event_subscription) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        exec(Host, User),
        user_on_host(User, Host).
    compromise(Host, user) :-
        persistence(Host, wmi_event_subscription).

% --- T1085_rundll32 ---
    exec_possible_user(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        app_installed(Host, rundll32, _Version).

    compromise_from_exec(Host, user) :-
        exec_possible_user(Host, User).

% --- T1086_powershell ---
    exec_possible_remote(Host, user) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        app_installed(Host, powershell, _Version),
        perm(Host, user, admin).

    compromise_from_exec(Host, user) :-
        exec_possible_remote(Host, user).

% --- T1087_account_discovery ---
    account_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        user_on_host(User, Host).

    compromise(Host, user) :-
        account_discovery(Host).

% --- T1088_bypass_user_account_control ---
    bypass_uac(Host) :-
        os(Host, windows),
        setting(Host, uac_level, low),
        app_installed(Host, rundll32, _Version),
        connects(net, Host, _Port).

    priv_escalation(Host, user, admin) :-
        bypass_uac(Host).

% --- T1089_disabling_security_tools ---
    defense_disabled(Host, Control) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        (Control = 'security_software'; Control = 'event_logging'; Control = 'registry_keys').

    % compromise(Host, user) :-
    %     defense_disabled(Host, _Control).

% --- T1090_proxy ---
    proxy_used(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        service(Host, Port, Protocol, proxy, _Version).

    compromise(Host, user) :-
        proxy_used(Host).

% --- T1092_communication_through_removable_media ---
    compromised_device(Host1) :-
        attacker_at(Internet),
        connects(Internet, Host1, _Port),
        compromised_device(Host2),
        connected_removable_media(Host1, Host2).

    compromised_device(Host2) :-
        removable_media_infection(Host2),
        connected_removable_media(Host1, Host2).

% --- T1093_process_hollowing ---
    process_hollowing_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        defense_disabled(Host, 'process_monitoring').

    compromise(Host, user) :-
        process_hollowing_detected(Host).

% --- T1095_non-application_layer_protocol ---
    c2_channel(Host, Protocol) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        protocol(Host, Protocol, Port),
        non_application_layer_protocol(Protocol).

    compromise(Host, user) :-
        c2_channel(Host, Protocol).

% --- T1096_ntfs_file_attributes ---
    ntfs_file_attribute_attack(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        defense_disabled(Host, static_indicator_scanning),
        defense_disabled(Host, anti_virus).

    compromise(Host, user) :-
        ntfs_file_attribute_attack(Host).

% --- T1097_pass_the_ticket ---
    pass_the_ticket_feasible(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        valid_account(User, Host),
        loot(Host, 'kerberos_ticket', TicketId),
        auth_usable_cred(User, Host, 'kerberos_ticket').

    compromise(Host, user) :-
        pass_the_ticket_feasible(Host, User).

% --- T1098_account_manipulation ---
    account_manipulation(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        perm(Host, User, Privilege),
        sufficient_permissions(Privilege),
        modify_account(Host, User).

compromise(Host, user) :-
    account_manipulation(Host, User).

sufficient_permissions(admin).
sufficient_permissions(root).

modify_account(Host, User) :-
    user_on_host(User, Host),
    valid_account(User, Host).

% --- T1100_web_shell ---
    web_shell_installed(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, "web_server", _).

    persistence(Host, "web_shell") :-
        web_shell_installed(Host).

    compromise(Host, user) :-
        web_shell_installed(Host).

% --- T1101_security_support_provider ---
    persistence(Host, 'Security Support Provider') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, 'Windows'),
        setting(Host, 'HKLM\\SYSTEM\\CurrentControlSet\\Control\\Lsa\\Security Packages', _),
        setting(Host, 'HKLM\\SYSTEM\\CurrentControlSet\\Control\\Lsa\\OSConfig\\Security Packages', _).

    compromise(Host, user) :-
        persistence(Host, 'Security Support Provider').

% --- T1102_web_service ---
    c2_channel(Host, web_service) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, https, web_service, _Version).

    compromise(Host, user) :-
        c2_channel(Host, web_service).

% --- T1103_appinit_dlls ---
    persistence(Host, appinit_dlls) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        setting(Host, 'HKEY_LOCAL_MACHINE\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Windows', AppInitDLLs),
        setting(Host, 'HKEY_LOCAL_MACHINE\\Software\\Wow6432Node\\Microsoft\\Windows NT\\CurrentVersion\\Windows', AppInitDLLs),
        exec(Host, user),
        not(defense_disabled(Host, secure_boot)).

    priv_escalation(Host, user, admin) :-
        persistence(Host, appinit_dlls).

% --- T1105_ingress_tool_transfer ---
    ingress_tool_transfer(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        c2_channel(Host, _Proto).

    compromise(Host, user) :-
        ingress_tool_transfer(Host).

% --- T1106_native_api ---
    native_api_execution(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        exec(Host, user).

    compromise(Host, user) :-
        native_api_execution(Host).

% --- T1107_file_deletion ---
    file_deletion(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        compromise(Host, user),
        os(Host, OS),
        member(OS, [linux, macos, windows]).

    compromise(Host, user) :-
        file_deletion(Host).

% --- T1108_redundant_access ---
    redundant_access(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        auth_usable_cred(User, Host, Method),
        persistence(Host, Method).

    compromise(Host, user) :-
        redundant_access(Host).

% --- T1109_component_firmware ---
    compromised_firmware(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        defense_disabled(Host, firmware_integrity_check).

    persistence(Host, firmware) :-
        compromised_firmware(Host).

    evasion(Host, software_defense) :-
        compromised_firmware(Host).

% --- T1110_brute_force ---
    brute_force_possible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        user_on_host(User, Host),
        \+ auth_usable_cred(User, Host, _Method).

    compromise(Host, user) :-
        brute_force_possible(Host, User).

% --- T1111_multi-factor_authentication_interception ---
    mfa_interception_possible(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        auth_possible(Host, User, mfa),
        exposed(Host, _Port).

    compromise(Host, user) :-
        mfa_interception_possible(Host, User),
        auth_usable_cred(User, Host, mfa).

% --- T1112_modify_registry ---
    modify_registry(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        valid_account(User, Host),
        perm(Host, User, admin),
        service(Host, Port, tcp, "RemoteRegistry", _),
        exec(Host, User).

    compromise(Host, user) :-
        modify_registry(Host).

% --- T1113_screen_capture ---
    screen_capture(Host) :-
        compromise(Host, user),
        app_installed(Host, 'remote_access_tool', _Version).

    compromise(Host, user) :-
        attacker_at(net),
        connects(net, Host, _Port).

% --- T1114_email_collection ---
    email_collection(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        has_data(Host, email),
        exfiltrated(Host, email, Network).

    compromise(Host, user) :-
        email_collection(Host, User).

% --- T1115_clipboard_data ---
    clipboard_data_access(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'clip.exe', _Version).

    compromise(Host, user) :-
        clipboard_data_access(Host).

% --- T1116_code_signing ---
    signed_malware(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, OS),
        member(OS, [macOS, windows]),
        compromised_certificate(Cert),
        uses_signed_code(Host, Cert).

compromise(Host, user) :-
    signed_malware(Host).

% --- T1117_regsvr32 ---
    exec(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'regsvr32.exe', _Version),
        os(Host, 'Windows'),
        user_on_host(User, Host),
        exposed(Host, Port).

    compromise(Host, user) :-
        exec(Host, User).

% --- T1118_installutil ---
    exec(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'InstallUtil', _Version),
        os(Host, 'Windows'),
        user_on_host(User, Host).

    compromise_from_exec(Host, user) :-
        exec(Host, User).

% --- T1119_automated_collection ---
    automated_collection(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        compromised_device(Host),
        has_data(Host, data_kind),
        exec(Host, User).

    compromise(Host, user) :-
        automated_collection(Host).

% --- T1121_regsvcs_regasm ---
    exec(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'Regsvcs', _Version),
        perm(Host, User, _Privilege).

    compromise_from_exec(Host, user) :-
        exec(Host, User).

% --- T1122_component_object_model_hijacking ---
    com_hijack_persistence(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        setting(Host, 'HKCU\\Software\\Classes\\CLSID', _MaliciousReference).

    compromise(Host, user) :-
        com_hijack_persistence(Host).

% --- T1123_audio_capture ---
    audio_capture_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'audio_capture_app', _Version).

    compromise(Host, user) :-
        audio_capture_possible(Host).

    exfiltrated(Host, 'audio_files', 'network') :-
        audio_capture_possible(Host).

% --- T1124_system_time_discovery ---
    system_time_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _, "time_service", _).

    compromise(Host, user) :-
        system_time_discovery(Host).

% --- T1125_video_capture ---
    video_capture_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, "camera_app", _Version).

    compromise(Host, user) :-
        video_capture_possible(Host).

% --- T1126_network_share_connection_removal ---
    network_share_connection_removed(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        exec(Host, user),
        app_installed(Host, net, _Version).

    compromise(Host, user) :-
        network_share_connection_removed(Host).

% --- T1127_trusted_developer_utilities_proxy_execution ---
    trusted_dev_util_proxy_exec(Host) :-
        app_installed(Host, dev_util, _Version),
        signed_with_legit_cert(dev_util),
        exec(Host, user),
        bypasses_app_control(dev_util).

    compromise(Host, user) :-
        trusted_dev_util_proxy_exec(Host).

% --- T1128_netsh_helper_dll ---
    persistence(Host, netsh_helper_dll) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        app_installed(Host, netsh, _Version),
        setting(Host, 'HKLM\\SOFTWARE\\Microsoft\\Netsh', _Value).

    compromise(Host, user) :-
        persistence(Host, netsh_helper_dll).

% --- T1129_shared_modules ---
    exec_possible_remote(Host, user) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        os(Host, OS),
        member(OS, [windows, macos, linux]).

    compromise_from_exec(Host, user) :-
        exec_possible_remote(Host, user).

% --- T1130_install_root_certificate ---
    compromised_certificate(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        installs_root_certificate(Host).

compromise(Host, user) :-
    compromised_certificate(Host).

% --- T1132_data_encoding ---
    data_encoded_c2(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        encoding_used(Protocol, EncodingType).

    compromise(Host, user) :-
        data_encoded_c2(Host).

% --- T1133_external_remote_services ---
    external_remote_service_access(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, Service, _),
        member(Service, [vpn, citrix, windows_remote_management, vnc, docker_api, kubernetes_api, kubelet, kubernetes_dashboard]).

    compromise(Host, user) :-
        external_remote_service_access(Host),
        valid_account(User, Host).

% --- T1134_access_token_manipulation ---
    access_token_manipulation(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        perm(Host, User, admin),
        exec(Host, User).

    privilege_escalation(Host, User) :-
        access_token_manipulation(Host, User),
        perm(Host, User, system).

    compromise(Host, system) :-
        privilege_escalation(Host, User).

% --- T1135_network_share_discovery ---
    network_share_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, tcp, smb, _Version).

    compromise(Host, user) :-
        network_share_discovery(Host).

% --- T1136_create_account ---
    create_account(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        valid_account(User, Host).

    persistence(Host, create_account) :-
        create_account(Host, _User).

% --- T1137_office_application_startup ---
    persistence(Host, 'Office Application Startup') :-
        app_installed(Host, 'Microsoft Office', _Version),
        os(Host, 'Windows'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        persistence(Host, 'Office Application Startup').

% --- T1138_application_shimming ---
    app_shimming_possible(Host) :-
        os(Host, windows),
        attacker_at(net),
        connects(net, Host, _Port).

    priv_escalation(Host, user, admin) :-
        app_shimming_possible(Host),
        exec(Host, user).

    persistence(Host, app_shimming) :-
        app_shimming_possible(Host).

    defense_disabled(Host, windows_defender) :-
        app_shimming_possible(Host).

% --- T1139_bash_history ---
    bash_history_access(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        has_data(Host, bash_history).

    compromise(Host, user) :-
        bash_history_access(Host, User),
        loot(Host, credentials, bash_history).

% --- T1140_deobfuscate_decode_files_or_information ---
    deobfuscate_decode_files_or_information(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, certutil, _Version).

    compromise(Host, user) :-
        deobfuscate_decode_files_or_information(Host).

% --- T1141_input_prompt ---
    fake_input_prompt(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        (exec_possible_user(Host, User); exec_possible_remote(Host, User)).

compromise(Host, user) :-
    fake_input_prompt(Host, User).

% --- T1142_keychain ---
    keychain_access(Host, User) :-
        os(Host, macOS),
        cred(User, Host, login),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        keychain_access(Host, User).

% --- T1143_hidden_window ---
    hidden_window_exploit(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        app_installed(Host, powershell, _Version),
        exec_possible_remote(Host, User).

    hidden_window_exploit(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, macos),
        setting(Host, "apple.awt.UIElement", "true"),
        exec_possible_remote(Host, User).

    compromise(Host, user) :-
        hidden_window_exploit(Host).

% --- T1144_gatekeeper_bypass ---
    gatekeeper_bypass(Host) :-
        os(Host, macOS),
        (   connected_removable_media(Host, _Media)
        ;   exec_possible_user(Host, User),
            perm(Host, User, elevated)
        ),
        defense_disabled(Host, gatekeeper).

    compromise(Host, user) :-
        gatekeeper_bypass(Host).

% --- T1146_clear_command_history ---
    clear_command_history(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        exec(Host, User),
        os(Host, linux),
        os(Host, macos).

    concealment(Host, User) :-
        clear_command_history(Host, User).

% --- T1147_hidden_users ---
    hidden_user_account(Host, User) :-
        os(Host, macOS),
        user_on_host(User, Host),
        setting(Host, 'Hide500Users', 'Yes'),
        user_id(User, UserID),
        UserID < 500.

compromise(Host, user) :-
    hidden_user_account(Host, User).

% --- T1148_histcontrol ---
    defense_disabled(Host, histcontrol) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, linux),
        setting(Host, histcontrol, ignorespace).

    defense_disabled(Host, histcontrol) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macos),
        setting(Host, histcontrol, ignorespace).

    compromise(Host, user) :-
        defense_disabled(Host, histcontrol).

% --- T1149_lc_main_hijacking ---
    hijack_entry_point(Host) :-
        os(Host, macOS),
        attacker_at(net),
        connects(net, Host, _Port),
        bypasses_app_control(Host).

    compromise(Host, user) :-
        hijack_entry_point(Host).

% --- T1150_plist_modification ---
    plist_modification(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macOS),
        user_on_host(User, Host),
        exec(Host, User),
        persistence(Host, plist_modification),
        priv_escalation(Host, user, elevated_privileges).

    compromise(Host, elevated_privileges) :-
        plist_modification(Host).

% --- T1151_space_after_filename ---
    misleading_name(File) :-
        file_extension(File, "txt "),
        actual_file_type(File, "executable").

    exec_possible_user(Host, User) :-
        misleading_name(File),
        user_accesses_content(User, File),
        user_on_host(User, Host).

    compromise_from_exec(Host, user) :-
        exec_possible_user(Host, User).

% --- T1152_launchctl ---
    exec(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macOS),
        app_installed(Host, launchctl, _Version),
        perm(Host, User, admin).

    persistence(Host, launchctl) :-
        exec(Host, User).

% --- T1154_trap ---
    trap_execution(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        os(Host, linux),
        exec(Host, User).

    persistence(Host, 'trap_command') :-
        trap_execution(Host, User).

% --- T1155_applescript ---
    exec_possible_remote(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macOS),
        app_installed(Host, "osascript", _Version).

    compromise_from_exec(Host, user) :-
        exec_possible_remote(Host, User).

% --- T1156_malicious_shell_modification ---
    persistence(Host, 'malicious_shell_modification') :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, 'Linux'),
        compromised_device(Host).

    persistence(Host, 'malicious_shell_modification') :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, 'macOS'),
        compromised_device(Host).

    compromised_device(Host) :-
        exec(Host, User),
        user_on_host(User, Host),
        (perm(Host, User, 'root'); perm(Host, User, 'user')).

% --- T1157_dylib_hijacking ---
    dylib_hijacking_possible(Host) :-
        os(Host, macOS),
        app_installed(Host, App, Version),
        attacker_at(net),
        connects(net, Host, _Port).

    privilege_escalation(Host, user, elevated) :-
        dylib_hijacking_possible(Host),
        exec(Host, user).

    persistence(Host, dylib_hijacking) :-
        dylib_hijacking_possible(Host).

% --- T1158_hidden_files_and_directories ---
    hidden_files_and_directories(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, OS),
        member(OS, [linux, macos, windows]).

persistence(Host, hidden_files_and_directories) :-
    hidden_files_and_directories(Host).

% --- T1159_launch_agent ---
    persistence(Host, launch_agent) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macos),
        user_on_host(User, Host),
        exec(Host, User),
        perm(Host, User, user).

    compromise(Host, user) :-
        persistence(Host, launch_agent).

% --- T1160_launch_daemon ---
    launch_daemon_persistence(Host) :-
        os(Host, macOS),
        attacker_at(net),
        connects(net, Host, _Port),
        misconfig(Host, launch_daemon),
        compromise(Host, admin).

    persistence(Host, launch_daemon) :-
        launch_daemon_persistence(Host).

    priv_escalation(Host, admin, root) :-
        launch_daemon_persistence(Host).

% --- T1161_lc_load_dylib_addition ---
    persistence(Host, 'LC_LOAD_DYLIB Addition') :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, 'macOS'),
        compromised_device(Host).

    compromised_device(Host) :-
        exec(Host, User),
        modify_malware(Host, 'LC_LOAD_DYLIB', 'Mach-O binary').

% --- T1162_login_item ---
    persistence(Host, 'login_item') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, 'macOS'),
        user_on_host(User, Host),
        exec(Host, User).
    compromise(Host, user) :-
        persistence(Host, 'login_item').

% --- T1164_re-opened_applications ---
    persistence(Host, 'reopened_applications') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, 'macOS'),
        file_modified(Host, '~/Library/Preferences/com.apple.loginwindow.plist'),
        file_modified(Host, '~/Library/Preferences/ByHost/com.apple.loginwindow.*.plist').

compromise(Host, user) :-
    persistence(Host, 'reopened_applications').

% --- T1165_startup_items ---
    persistence(Host, startup_items) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macos),
        exec(Host, root).

    priv_escalation(Host, user, root) :-
        persistence(Host, startup_items).

% --- T1166_setuid_and_setgid ---
    setuid_setgid_exploit(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        exec(Host, User),
        perm(Host, User, elevated),
        os(Host, OS),
        (OS = linux; OS = macos).

    compromise_from_pe(Host, elevated) :-
        setuid_setgid_exploit(Host, User).

% --- T1167_securityd_memory ---
    compromise(Host, root) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macOS),
        perm(Host, User, root),
        user_on_host(User, Host).

    loot(Host, keychain_passwords, T1167) :-
        compromise(Host, root),
        os(Host, macOS).

% --- T1168_local_job_scheduling ---
    job_scheduling_persistence(Host) :-
        os(Host, linux),
        app_installed(Host, cron, _Version),
        attacker_at(net),
        connects(net, Host, _Port).

    job_scheduling_persistence(Host) :-
        os(Host, macos),
        app_installed(Host, launchd, _Version),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        job_scheduling_persistence(Host).

% --- T1170_mshta ---
    app_installed(Host, 'mshta.exe', _Version) :-
        os(Host, 'Windows').

    exec_possible_user(Host, User) :-
        app_installed(Host, 'mshta.exe', _Version),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise_from_exec(Host, user) :-
        exec_possible_user(Host, User).

% --- T1171_llmnr_nbt-ns_poisoning_and_relay ---
    llmnr_nbt_ns_poisoning_and_relay(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, udp, llmnr, _),
        service(Host, Port, udp, nbt_ns, _).

    compromise(Host, user) :-
        llmnr_nbt_ns_poisoning_and_relay(Host),
        auth_possible(Host, User, ntlmv2),
        accepts_hash(Host, User).

% --- T1172_domain_fronting ---
    domain_fronting_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, https),
        service(Host, Port, https, cdn, _Version).

    compromise(Host, user) :-
        domain_fronting_possible(Host).

% --- T1173_dynamic_data_exchange ---
    dde_execution_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'Microsoft Office', _Version),
        os(Host, 'Windows'),
        setting(Host, 'DDEEnabled', true).

    compromise(Host, user) :-
        dde_execution_possible(Host).

% --- T1174_password_filter_dll ---
    malicious_password_filter_installed(Host) :-
        app_installed(Host, 'PasswordFilterDLL', _Version),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        malicious_password_filter_installed(Host).

% --- T1175_component_object_model_and_distributed_com ---
    exec_possible_remote(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        perm(Host, User, admin).

    compromise(Host, user) :-
        exec_possible_remote(Host, user).

% --- T1176_software_extensions ---
    malicious_extension_installed(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'extension', _Version),
        (social_engineering(Host); compromised_marketplace(Host); direct_installation(Host)).

    persistence(Host, 'software_extension') :-
        malicious_extension_installed(Host).

% --- T1177_lsass_driver ---
    execCode(Host, priv) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        service(Host, _Port, _, lsass, _),
        (dll_side_loading(Host, lsass); dll_search_order_hijacking(Host, lsass)).

    compromise(Host, user) :-
        execCode(Host, priv).

% --- T1179_hooking ---
    hooking_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        exec(Host, user),
        persistence(Host, hooking),
        priv_escalation(Host, user, elevated),
        loot(Host, credentials, _).

    compromise(Host, user) :-
        hooking_detected(Host).

% --- T1180_screensaver ---
    persistence(Host, screensaver) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        setting(Host, 'HKCU\\Control Panel\\Desktop\\SCRNSAVE.exe', MaliciousPEPath),
        setting(Host, 'HKCU\\Control Panel\\Desktop\\ScreenSaveActive', '1'),
        setting(Host, 'HKCU\\Control Panel\\Desktop\\ScreenSaverIsSecure', '0'),
        setting(Host, 'HKCU\\Control Panel\\Desktop\\ScreenSaveTimeout', _Timeout).

% --- T1181_extra_window_memory_injection ---
    ewm_injection_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        defense_disabled(Host, dep).

    compromise(Host, elevated_privileges) :-
        ewm_injection_possible(Host).

% --- T1182_appcert_dlls ---
    persistence(Host, 'AppCertDLLs') :-
        setting(Host, 'HKEY_LOCAL_MACHINE\\System\\CurrentControlSet\\Control\\Session Manager\\AppCertDLLs', DLL),
        malicious_dll(DLL).

    priv_escalation(Host, user, admin) :-
        persistence(Host, 'AppCertDLLs'),
        exec(Host, user).

% --- T1183_image_file_execution_options_injection ---
    ifeo_injection(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        setting(Host, "HKLM\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\Image File Execution Options\\<executable>", "Debugger"),
        setting(Host, "HKLM\\SOFTWARE\\Microsoft\\Windows NT\\CurrentVersion\\SilentProcessExit\\<executable>", "MonitorProcess").

    persistence(Host, "IFEO") :-
        ifeo_injection(Host).

    priv_escalation(Host, user, admin) :-
        ifeo_injection(Host).

    defense_disabled(Host, "Security Applications") :-
        ifeo_injection(Host).

% --- T1185_browser_session_hijacking ---
    browser_session_hijacking(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, browser, Version),
        exposed(Host, Port),
        perm(Host, User, admin),
        exec(Host, User).

    compromise(Host, user) :-
        browser_session_hijacking(Host).

% --- T1186_process_doppelgänging ---
    process_doppelgaenging(Host) :-
        os(Host, windows),
        attacker_at(Network),
        connects(Network, Host, Port),
        exec_possible_remote(Host, User),
        defense_disabled(Host, 'API Monitoring'),
        compromise_from_exec(Host, user).

    compromise(Host, user) :-
        process_doppelgaenging(Host).

% --- T1188_multi-hop_proxy ---
    multi_hop_proxy_detected(Host) :-
        attacker_at(Network),
        connects(Network, Proxy, _Port),
        connects(Proxy, Host, _Port),
        c2_channel(Host, _Proto).

    compromise(Host, user) :-
        multi_hop_proxy_detected(Host).

% --- T1189_drive-by_compromise ---
    drive_by_compromise(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_accesses_content(User, Host),
        vulnerable_browser(Host),
        exec(Host, User).

    vulnerable_browser(Host) :-
        app_installed(Host, browser, Version),
        local_vuln(Host, CVE),
        service_matches(CVE, browser).

    user_accesses_content(User, Host) :-
        user_on_host(User, Host),
        accesses_website(User, malicious_website).

% --- T1190_exploit_public-facing_application ---
    exposed(Host, Port) :-
        attacker_at(net),
        connects(net, Host, Port).

    remote_service_vulnerable(Host, Port, CVE, Service) :-
        exposed(Host, Port),
        service(Host, Port, _Proto, Service, _Version),
        service_matches(CVE, Service).

    compromise(Host, user) :-
        remote_service_vulnerable(Host, _Port, _CVE, _Service).

% --- T1191_cmstp ---
    cmstp_exploit(Host) :-
        app_installed(Host, 'CMSTP.exe', _Version),
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        exec_possible_user(Host, User).

    compromise(Host, user) :-
        cmstp_exploit(Host).

% --- T1192_spearphishing_link ---
    spearphishing_link(User, Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        user_phishes_success(User).

    compromise(Host, user) :-
        spearphishing_link(User, Host).

% --- T1193_spearphishing_attachment ---
    spearphishing_attachment(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        user_phishes_success(User),
        exec_possible_user(Host, User).

    compromise(Host, user) :-
        spearphishing_attachment(Host, User).

% --- T1194_spearphishing_via_service ---
    spearphishing_via_service(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        user_phishes_success(User).

    compromise(Host, user) :-
        spearphishing_via_service(Host, User).

% --- T1195_supply_chain_compromise ---
    supply_chain_compromise(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromised_device(Host).

    compromise(Host, user) :-
        supply_chain_compromise(Host).

% --- T1196_control_panel_items ---
    exec_possible_user(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        user_on_host(User, Host).

    compromise_from_exec(Host, user) :-
        exec_possible_user(Host, User).

% --- T1197_bits_jobs ---
    bits_job_abuse(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        service(Host, Port, tcp, bits, _Version).

    persistence(Host, bits_job) :-
        bits_job_abuse(Host).

    compromise(Host, user) :-
        bits_job_abuse(Host).

% --- T1199_trusted_relationship ---
    trusted_relationship_access(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        trust(ThirdParty, Host, _Mechanism),
        valid_account(User, Host),
        auth_usable_cred(User, Host, _Method).

    compromise(Host, user) :-
        trusted_relationship_access(Host).

% --- T1200_hardware_additions ---
    hardware_addition_access(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        physical_access_possible(Host).

compromise(Host, user) :-
    hardware_addition_access(Host).

% --- T1201_password_policy_discovery ---
    password_policy_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, "ssh", _).

    compromise(Host, user) :-
        password_policy_discovery(Host).

% --- T1202_indirect_command_execution ---
    indirect_command_execution(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'Forfiles', _Version);
        app_installed(Host, 'pcalua.exe', _Version);
        app_installed(Host, 'WSL', _Version);
        app_installed(Host, 'Scriptrunner.exe', _Version);
        app_installed(Host, 'ssh.exe', _Version).

    compromise(Host, user) :-
        indirect_command_execution(Host).

% --- T1203_exploitation_for_client_execution ---
    exec_possible_remote(Host, user) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, Version),
        vulExists(Host, App, CVE, App),
        exploit_possible(CVE).

    compromise(Host, user) :-
        exec_possible_remote(Host, user).

% --- T1205_traffic_signaling ---
    traffic_signaling_detected(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        signal_packet_sequence_detected(Host, Port).

compromise(Host, user) :-
    traffic_signaling_detected(Host).

% --- T1206_sudo_caching ---
    sudo_caching_vulnerable(Host) :-
        os(Host, linux),
        setting(Host, 'timestamp_timeout', Timeout),
        Timeout > 0,
        setting(Host, 'tty_tickets', disabled).

    priv_escalation(Host, user, root) :-
        sudo_caching_vulnerable(Host),
        attacker_at(net),
        connects(net, Host, _Port).

% --- T1207_rogue_domain_controller ---
    rogue_domain_controller(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        role(Host, dc),
        perm(Host, User, admin),
        auth_usable_cred(User, Host, krbtgt_hash).

    compromise(Host, domain) :-
        rogue_domain_controller(Host).

% --- T1208_kerberoasting ---
    kerberoasting_possible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        role(Host, dc),
        valid_account(User, Host),
        auth_usable_cred(User, Host, kerberos),
        exposed(Host, kerberos_port).

    compromise(Host, user) :-
        kerberoasting_possible(Host, User).

% --- T1209_time_providers ---
    persistence(Host, 'T1209_Time_Providers') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        perm(Host, admin, _),
        exec(Host, 'LocalService').

% --- T1210_exploitation_of_remote_services ---
    remote_service_vulnerable(Host, Port, CVE, Service) :-
        attacker_at(net),
        connects(net, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _Proto, Service, _Version),
        vulExists(Host, Service, CVE, Service).

    exec_possible_remote(Host, user) :-
        remote_service_vulnerable(Host, Port, _CVE, _Service).

    compromise(Host, user) :-
        exec_possible_remote(Host, user).

% --- T1211_exploitation_for_defense_evasion ---
    exploit_defense_evasion(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        vulExists(Host, app, cve, app),
        defense_disabled(Host, control).

    compromise(Host, user) :-
        exploit_defense_evasion(Host).

% --- T1212_exploitation_for_credential_access ---
    exploit_credential_access(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        remote_service_vulnerable(Host, _Port, CVE, Service),
        exec_possible_remote(Host, User),
        auth_usable_cred(User, Host, _Method).

    compromise(Host, user) :-
        exploit_credential_access(Host, user).

% --- T1213_data_from_information_repositories ---
    compromised_data(Host, DataKind) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, DataKind),
        misconfig(Host, 'overly-broad access').

    compromise(Host, user) :-
        compromised_data(Host, _).

% --- T1215_kernel_modules_and_extensions ---
    kernel_module_loaded(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, linux),
        compromise(Host, root).

    kernel_extension_loaded(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macos),
        compromise(Host, root).

    persistence(Host, kernel_module) :-
        kernel_module_loaded(Host).

    persistence(Host, kernel_extension) :-
        kernel_extension_loaded(Host).

% --- T1216_system_script_proxy_execution ---
    proxy_execution_possible(Host) :-
        os(Host, windows),
        app_installed(Host, 'Microsoft Signed Script', _Version),
        bypasses_app_control(Host).

    compromise(Host, user) :-
        proxy_execution_possible(Host).

% --- T1217_browser_information_discovery ---
    browser_info_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, browser, _Version),
        has_data(Host, browser_data).

    compromise(Host, user) :-
        browser_info_discovery(Host).

% --- T1218_system_binary_proxy_execution ---
    proxy_execution_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, binary, version),
        signed_by_trusted_cert(binary).

    compromise(Host, user) :-
        proxy_execution_possible(Host).

% --- T1219_remote_access_tools ---
    remote_access_tool_installed(Host) :-
        app_installed(Host, remote_access_tool, _Version).

    c2_channel(Host, protocol) :-
        remote_access_tool_installed(Host),
        attacker_at(Network),
        connects(Network, Host, Port).

    compromise(Host, user) :-
        c2_channel(Host, protocol).

% --- T1220_xsl_script_processing ---
    % Rule for detecting potential XSL Script Processing attack
    potential_xsl_script_processing(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'msxsl.exe', _Version).

    % If potential XSL Script Processing is detected, compromise the host
    compromise(Host, user) :-
        potential_xsl_script_processing(Host).

% --- T1221_template_injection ---
    template_injection_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        app_installed(Host, "Office", _Version).

    compromise(Host, user) :-
        template_injection_possible(Host).

% --- T1222_file_and_directory_permissions_modification ---
    file_permission_modified(Host, File) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exposed(Host, _Port),
        user_on_host(User, Host),
        perm(Host, User, elevated),
        file_accessible(Host, File, User).

    compromise(Host, user) :-
        file_permission_modified(Host, _File).

% --- T1223_compiled_html_file ---
    potential_t1223(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'hh.exe', _Version),
        os(Host, 'Windows').

    compromise(Host, user) :-
        potential_t1223(Host),
        exec_possible_user(Host, user).

% --- T1398_boot_or_logon_initialization_scripts ---
    persistence(Host, 'boot_or_logon_initialization_scripts') :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (rooted(Host) ; jailbroken(Host)).

    compromise(Host, user) :-
        persistence(Host, 'boot_or_logon_initialization_scripts').

% --- T1400_modify_system_partition ---
    modify_system_partition(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        (unlocked_bootloader(Host) ; priv_escalation(Host, user, root)),
        persistence(Host, system_partition).

    persistence(Host, system_partition) :-
        modify_system_partition(Host).

% --- T1401_device_administrator_permissions ---
    device_admin_privilege_escalation(Host) :-
        app_installed(Host, App, Version),
        mobile_perm(App, 'DEVICE_ADMIN'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, admin) :-
        device_admin_privilege_escalation(Host).

% --- T1402_broadcast_receivers ---
    broadcast_receiver_malware(Host) :-
        app_installed(Host, App, Version),
        os(Host, android),
        attacker_at(net),
        connects(net, Host, _Port),
        malicious_broadcast_receiver(App).

compromise(Host, user) :-
    broadcast_receiver_malware(Host).

% --- T1403_modify_cached_executable_code ---
    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        priv_escalation(Host, user, root),
        persistence(Host, 'modify_cached_executable_code').

% --- T1405_exploit_tee_vulnerability ---
    exploit_tee_vulnerability(Host) :-
        app_installed(Host, 'malicious_app', _Version),
        os(Host, 'Android'),
        device(Host),
        priv_escalation(Host, user, tee_privileges),
        has_data(Host, 'cryptographic_keys'),
        compromise(Host, tee_privileges).

    compromise(Host, user) :-
        exploit_tee_vulnerability(Host).

% --- T1406_obfuscated_files_or_information ---
    obfuscated_payload_detected(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        os(Host, OS),
        (OS = android; OS = ios),
        defense_disabled(Host, obfuscation_detection).

    compromise(Host, user) :-
        obfuscated_payload_detected(Host).

% --- T1407_download_new_code_at_runtime ---
    dynamic_code_downloaded(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        os(Host, OS),
        (OS = android; OS = ios),
        defense_disabled(Host, static_analysis).

    compromise(Host, user) :-
        dynamic_code_downloaded(Host).

% --- T1408_disguise_root_jailbreak_indicators ---
    disguise_root_jailbreak_indicators(Host) :-
        device(Host),
        compromised_device(Host),
        misleading_name(Host).

    compromise(Host, user) :-
        disguise_root_jailbreak_indicators(Host).

% --- T1409_stored_application_data ---
    stored_app_data_accessible(Host) :-
        app_installed(Host, App, _Version),
        (unprotected_storage(Host, App);
         insecure_permissions(Host, App);
         root_access(Host)).

    compromise(Host, user) :-
        stored_app_data_accessible(Host).

% --- T1410_network_traffic_capture_or_redirection ---
    network_traffic_capture_or_redirection(Host) :-
        app_installed(Host, App, Version),
        (app_has_permission(App, vpn_client); priv_escalation(Host, user, root)),
        connects(net, Host, Port),
        attacker_at(net),
        has_data(Host, sensitive_data),
        not(encrypted_traffic(Host, Port)).

    compromise(Host, user) :-
        network_traffic_capture_or_redirection(Host).

% --- T1411_input_prompt ---
    malicious_prompt(Host) :-
        app_installed(Host, App, Version),
        mobile_perm(App, 'SYSTEM_ALERT_WINDOW'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        malicious_prompt(Host).

% --- T1413_access_sensitive_data_in_device_logs ---
    access_sensitive_data_in_device_logs(Host) :-
        app_installed(Host, App, Version),
        mobile_perm(App, 'READ_LOGS'),
        os(Host, 'Android'),
        Version @< '4.1'.

    access_sensitive_data_in_device_logs(Host) :-
        app_installed(Host, App, Version),
        os(Host, 'Android'),
        Version @>= '4.1',
        local_pe_vulnerable(Host, CVE),
        priv_escalation(Host, user, root).

    compromise(Host, user) :-
        access_sensitive_data_in_device_logs(Host).

% --- T1414_clipboard_data ---
    clipboard_data_access(Host, App) :-
        app_installed(Host, App, _Version),
        mobile_perm(App, clipboard_access),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        clipboard_data_access(Host, _App).

% --- T1416_uri_hijacking ---
    uri_hijacking_possible(Host, Uri) :-
        app_installed(Host, App, _Version),
        registers_uri(App, Uri),
        malicious_app_installed(Host, MaliciousApp),
        registers_uri(MaliciousApp, Uri).

    compromise(Host, user) :-
        uri_hijacking_possible(Host, Uri),
        has_data(Host, oauth_tokens),
        exfiltrated(Host, oauth_tokens, MaliciousApp).

% --- T1417_input_capture ---
    input_capture(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        mobile_perm(App, 'input_capture'),
        user_on_host(User, Host),
        exec_possible_user(Host, User).

    compromise(Host, user) :-
        input_capture(Host).

% --- T1418_software_discovery ---
    software_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        app_installed(Host, App, Version).

    compromise(Host, user) :-
        software_discovery(Host).

% --- T1420_file_and_directory_discovery ---
    file_directory_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, android),
        exposed(Host, Port),
        has_data(Host, sensitive),
        local_pe_vulnerable(Host, _CVE).

    file_directory_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, ios),
        exposed(Host, Port),
        priv_escalation(Host, user, root).

% --- T1421_system_network_connections_discovery ---
    network_discovery(Host) :-
        compromised_device(Host),
        app_installed(Host, 'network_discovery_app', _Version),
        mobile_perm('network_discovery_app', 'ACCESS_FINE_LOCATION').

compromise(Host, user) :-
    network_discovery(Host).

% --- T1422_system_network_configuration_discovery ---
    network_config_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'java.net.NetworkInterface', _Version).

    compromise(Host, user) :-
        network_config_discovery(Host).

% --- T1423_network_service_scanning ---
    network_service_scanning(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        network_service_scanning(Host).

% --- T1424_process_discovery ---
    process_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, android),
        version(Host, Version),
        Version < 7.

    process_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, ios),
        version(Host, Version),
        Version < 11.

    compromise(Host, user) :-
        process_discovery(Host).

% --- T1425_insecure_third-party_libraries ---
    insecure_third_party_library(Host, App, Version) :-
        app_installed(Host, App, Version),
        vulExists(Host, App, _, _).

    compromise(Host, user) :-
        insecure_third_party_library(Host, _, _).

% --- T1426_system_information_discovery ---
    system_info_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        os(Host, OS),
        (OS = 'Android'; OS = 'iOS'),
        app_installed(Host, 'android.os.Build', _Version).

    compromise(Host, user) :-
        system_info_discovery(Host).

% --- T1427_attack_pc_via_usb_connection ---
    compromised_device(Device) :-
        attacker_at(net),
        device(Device),
        os(Device, android),
        priv_escalation(Device, user, root),
        connected_removable_media(Device, Host),
        impersonates_usb(Device, Host).

    compromise(Host, user) :-
        compromised_device(Device),
        connected_removable_media(Device, Host).

% --- T1429_audio_capture ---
    audio_capture_possible(Host) :-
        app_installed(Host, App, _Version),
        mobile_perm(App, 'RECORD_AUDIO'),
        os(Host, 'Android').

    audio_capture_possible(Host) :-
        app_installed(Host, App, _Version),
        mobile_perm(App, 'NSMicrophoneUsageDescription'),
        os(Host, 'iOS').

    compromise(Host, user) :-
        audio_capture_possible(Host).

% --- T1430_location_tracking ---
    location_tracking(Host) :-
        compromised_device(Host),
        app_installed(Host, App, Version),
        mobile_perm(App, 'ACCESS_FINE_LOCATION'),
        os(Host, 'Android').

    location_tracking(Host) :-
        compromised_device(Host),
        app_installed(Host, App, Version),
        mobile_perm(App, 'NSLocationAlwaysUsageDescription'),
        os(Host, 'iOS').

    compromise(Host, user) :-
        location_tracking(Host).

% --- T1431_app_delivered_via_web_download ---
    malicious_app_installed(Host, App) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version).

compromise(Host, user) :-
    malicious_app_installed(Host, App).

% --- T1432_access_contact_list ---
    access_contact_list(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, app, version),
        mobile_perm(app, 'access_contacts'),
        exec(Host, user).

    compromise(Host, user) :-
        access_contact_list(Host).

% --- T1434_app_delivered_via_email_attachment ---
    malicious_app_installed(Host, App) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_phishes_success(User),
        user_on_host(User, Host),
        app_installed(Host, App, _Version).

    compromise(Host, user) :-
        malicious_app_installed(Host, _App).

% --- T1435_access_calendar_entries ---
    access_calendar_entries(Host) :-
        malicious_app_installed(Host, app),
        mobile_perm(app, 'calendar_access'),
        compromise(Host, user).

    compromise(Host, user) :-
        access_calendar_entries(Host).

% --- T1436_commonly_used_port ---
    uses_common_port(Host, Port) :-
        attacker_at(net),
        connects(net, Host, Port),
        member(Port, [80, 443, 25, 53]).

compromise(Host, user) :-
    uses_common_port(Host, _Port).

% --- T1437_application_layer_protocol ---
    c2_channel(Host, Protocol) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, Protocol, _, _),
        protocol_in_use(Protocol).

    compromise(Host, user) :-
        c2_channel(Host, _).

% --- T1439_eavesdrop_on_insecure_network_communication ---
    eavesdrop_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, Proto, Service, Version),
        (Proto = 'http'; Proto = 'ftp'; Proto = 'telnet'). % Example of insecure protocols

    compromise(Host, user) :-
        eavesdrop_possible(Host).

% --- T1440_detect_app_analysis_environment ---
    detect_app_analysis_env(Host) :-
        device(Host),
        app_installed(Host, App, Version),
        analysis_env_detected(App).

    compromise(Host, user) :-
        detect_app_analysis_env(Host).

% --- T1442_fake_developer_accounts ---
    fake_developer_account_compromise(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        valid_account(User, Host),
        modify_account(User, Host).

    compromise(Host, user) :-
        fake_developer_account_compromise(Host).

% --- T1443_remotely_install_application ---
    remotely_install_app(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exposed(Host, _Port),
        app_installed(Host, app, _Version).

    compromise(Host, user) :-
        remotely_install_app(Host).

% --- T1444_masquerade_as_legitimate_application ---
    malicious_app_installed(Host, App) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version),
        misleading_name(App).

    compromise(Host, user) :-
        malicious_app_installed(Host, App),
        mobile_perm(App, accessibility_service).

% --- T1445_abuse_of_ios_enterprise_app_signing_key ---
    malicious_app_installed(Device, App) :-
        attacker_at(Network),
        connects(Network, Device, Port),
        device(Device),
        app_installed(Device, App, _Version),
        bypasses_app_control(Device).

    compromise(Device, user) :-
        malicious_app_installed(Device, _App).

% --- T1446_device_lockout ---
    device_lockout(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, android),
        app_installed(Host, 'DeviceAdminApp', Version),
        Version < 7.

    device_lockout(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, ios),
        jailbroken(Host),
        malicious_app_installed(Host, 'LockoutMalware').

    compromise(Host, user) :-
        device_lockout(Host).

% --- T1447_delete_device_data ---
    delete_device_data(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        device(Host),
        app_installed(Host, app, _Version),
        mobile_perm(app, admin_access).

    compromise(Host, user) :-
        delete_device_data(Host).

% --- T1448_carrier_billing_fraud ---
    carrier_billing_fraud(Host) :-
        app_installed(Host, App, Version),
        mobile_perm(App, 'SEND_SMS'),
        os(Host, 'Android'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        carrier_billing_fraud(Host).

% --- T1449_exploit_ss7_to_redirect_phone_calls_sms ---
    ss7_exploit_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, ss7, _, _).

    intercept_communication(Host) :-
        ss7_exploit_possible(Host),
        compromise(Host, user).

% --- T1450_exploit_ss7_to_track_device_location ---
    exploit_ss7_tracking(Device) :-
        attacker_at(Network),
        connects(Network, Device, Port),
        device(Device),
        exposed(Device, Port),
        has_data(Device, location).

    compromise(Device, user) :-
        exploit_ss7_tracking(Device).

% --- T1451_sim_card_swap ---
    sim_card_swap_attack(User, MobileCarrier) :-
        attacker_at(net),
        user_phishes_success(User),
        impersonates(User, MobileCarrier),
        sim_swap_request(MobileCarrier, User).

compromise(User, mobile_data) :-
    sim_card_swap_attack(User, MobileCarrier),
    receives_mobile_data(MobileCarrier, User).

% --- T1452_manipulate_app_store_rankings_or_ratings ---
    manipulate_app_store_rankings_or_ratings(Host) :-
        compromised_device(Host),
        jailbroken(Host),
        app_installed(Host, App, Version),
        exec(Host, User),
        perm(Host, User, privileged).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        auth_usable_cred(User, Host, _Method).

% --- T1453_abuse_accessibility_features ---
    malicious_app_installed(Host, App) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version),
        mobile_perm(App, accessibility_features).

    compromise(Host, user) :-
        malicious_app_installed(Host, App),
        mobile_perm(App, accessibility_features).

    defense_disabled(Host, uninstallation_prevention) :-
        malicious_app_installed(Host, App),
        mobile_perm(App, accessibility_features).

% --- T1455_exploit_baseband_vulnerability ---
    vulnerable_baseband(Host) :-
        device(Host),
        exposed(Host, _Port),
        attacker_at(net).

    compromise(Host, user) :-
        vulnerable_baseband(Host).

% --- T1456_drive-by_compromise ---
    potential_drive_by_compromise(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_accesses_content(User, Host),
        vulnerable_browser(Host).

    compromise(Host, user) :-
        potential_drive_by_compromise(Host),
        exec(Host, user).

% --- T1457_malicious_media_content ---
    malicious_media_content(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_accesses_content(User, Host),
        exec(Host, User).

    compromise(Host, user) :-
        malicious_media_content(Host).

% --- T1458_replication_through_removable_media ---
    removable_media_infection(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        connected_removable_media(Host, Media),
        malicious_app_installed(Media, App).

    compromise(Host, user) :-
        removable_media_infection(Host).

% --- T1459_device_unlock_code_guessing_or_brute_force ---
    device_unlock_code_guessing(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        exposed(Host, Port).

    compromise(Host, user) :-
        device_unlock_code_guessing(Host).

% --- T1460_biometric_spoofing ---
    biometric_spoofing_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        device(Host).

compromise(Host, user) :-
    biometric_spoofing_possible(Host).

% --- T1461_lockscreen_bypass ---
    lockscreen_bypass(Host) :-
        device(Host),
        attacker_at(physical_access),
        (biometric_spoofing(Host);
         unlock_code_bypass(Host);
         vulnerability_exploit(Host)).

    compromise(Host, user) :-
        lockscreen_bypass(Host).

% --- T1462_malicious_software_development_tools ---
    malicious_software_development_tools(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'development_tool', Version),
        malicious_app_installed('development_tool', Version).

    compromise(Host, user) :-
        malicious_software_development_tools(Host).

% --- T1463_manipulate_device_communication ---
    manipulate_device_communication(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        misconfig(Host, 'TLS vulnerability').

    compromise(Host, user) :-
        manipulate_device_communication(Host).

% --- T1464_network_denial_of_service ---
    network_dos(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        jam_signal(Network, Host).

    jam_signal(Network, Host) :-
        attacker_at(Network),
        device(Host),
        signal_jammer_in_range(Network, Host).

    signal_jammer_in_range(Network, Host) :-
        attacker_at(Network),
        device(Host),
        within_jammer_range(Network, Host).

% --- T1465_rogue_wi-fi_access_points ---
    rogue_wifi_access_point(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        rogue_wifi_access_point(Host).

% --- T1466_downgrade_to_insecure_protocols ---
    downgrade_to_insecure_protocols(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        jam_signal(Network, 'LTE'),
        protocol(Host, 'GSM', Port).

    compromise(Host, user) :-
        downgrade_to_insecure_protocols(Host).

% --- T1467_rogue_cellular_base_station ---
    rogue_base_station_detected(Device) :-
        attacker_at(Network),
        connects(Network, Device, Port),
        device(Device),
        exposed(Device, Port).

    compromise(Device, user) :-
        rogue_base_station_detected(Device).

% --- T1468_remotely_track_device_without_authorization ---
    unauthorized_tracking(Device) :-
        attacker_at(Network),
        connects(Network, Device, Port),
        cloud_account(Account, Provider),
        cloud_cred(Principal, Type),
        mdm_enrolled(Device, Vendor),
        compromise(Device, user).

% --- T1469_remotely_wipe_data_without_authorization ---
    unauthorized_wipe(Device) :-
        attacker_at(net),
        cloud_account(Account, Provider),
        cloud_cred(Credential, type),
        mdm_enrolled(Device, Provider),
        compromise(Account, user).

    compromise(Device, user) :-
        unauthorized_wipe(Device).

% --- T1471_data_encrypted_for_impact ---
    data_encrypted_for_impact(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        os(Host, android),
        has_data(Host, sensitive),
        compromise(Host, user).

    compromise(Host, user) :-
        data_encrypted_for_impact(Host).

% --- T1472_generate_fraudulent_advertising_revenue ---
    fraudulent_ad_revenue(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'ad_clicker', _Version),
        exec(Host, 'ad_clicker').
    compromise(Host, user) :-
        fraudulent_ad_revenue(Host).

% --- T1474_supply_chain_compromise ---
    supply_chain_compromise(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        malicious_app_installed(App, Version).

    compromise(Host, user) :-
        supply_chain_compromise(Host).

% --- T1475_deliver_malicious_app_via_authorized_app_store ---
    malicious_app_installed(Device, App) :-
        attacker_at(net),
        connects(net, Device, _Port),
        app_installed(Device, App, _Version),
        device(Device),
        role(App, malicious).

    compromise(Device, user) :-
        malicious_app_installed(Device, _App).

% --- T1476_deliver_malicious_app_via_other_means ---
    malicious_app_installed(Device, App) :-
        attacker_at(net),
        connects(net, Device, _Port),
        app_installed(Device, App, _Version),
        not(authorized_app_store(App)).

    compromise(Device, user) :-
        malicious_app_installed(Device, _App).

% --- T1477_exploit_via_radio_interfaces ---
    exploit_via_radio_interface(Host) :-
        attacker_at(net),
        device(Host),
        connects(net, Host, _Port),
        exposed(Host, _Port),
        vulnerability_exploit(Host).

    compromise(Host, user) :-
        exploit_via_radio_interface(Host).

% --- T1480_execution_guardrails ---
    execution_guardrails(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        expected_conditions_met(Host).

compromise(Host, user) :-
    execution_guardrails(Host).

% --- T1481_web_service ---
    c2_channel(Host, web_service) :-
        attacker_at(net),
        connects(net, Host, _Port),
        encrypted_traffic(Host, ssl_tls).

    compromise(Host, user) :-
        c2_channel(Host, web_service).

% --- T1482_domain_trust_discovery ---
    domain_trust_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        app_installed(Host, nltest, _Version).

    compromise(Host, user) :-
        domain_trust_discovery(Host).

% --- T1483_domain_generation_algorithms ---
    dga_c2_channel(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, dga).

compromise(Host, user) :-
    dga_c2_channel(Host).

% --- T1484_domain_or_tenant_policy_modification ---
    domain_policy_modified(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        perm(Host, User, admin),
        setting(Host, domain_policy, modified).

    compromise(Host, admin) :-
        domain_policy_modified(Host).

% --- T1485_data_destruction ---
    data_destruction(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        compromise(Host, user),
        has_data(Host, sensitive),
        exec(Host, user).

% --- T1486_data_encrypted_for_impact ---
    data_encrypted_for_impact(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, sensitive),
        compromise(Host, user).

    compromise(Host, user) :-
        data_encrypted_for_impact(Host).

% --- T1487_disk_structure_wipe ---
    disk_structure_wipe(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        compromise(Host, admin),
        os(Host, OS),
        member(OS, [windows, macos, linux]),
        critical_data_overwritten(Host).

    critical_data_overwritten(Host) :-
        overwrite_mbr(Host);
        overwrite_partition_table(Host).

    overwrite_mbr(Host) :-
        exec(Host, User),
        compromise(Host, admin),
        mbr_accessible(Host).

    overwrite_partition_table(Host) :-
        exec(Host, User),
        compromise(Host, admin),
        partition_table_accessible(Host).

    mbr_accessible(Host) :-
        exposed(Host, Port),
        service(Host, Port, _, mbr_service, _).

    partition_table_accessible(Host) :-
        exposed(Host, Port),
        service(Host, Port, _, partition_service, _).

% --- T1488_disk_content_wipe ---
    disk_content_wipe(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        compromise(Host, root),
        os(Host, OS),
        member(OS, [linux, macos, windows]).

    compromise(Host, root) :-
        disk_content_wipe(Host).

% --- T1489_service_stop ---
    service_stopped(Host, Service) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, _Proto, Service, _Version),
        critical_service(Service).

compromise(Host, user) :-
    service_stopped(Host, _Service).

% --- T1490_inhibit_system_recovery ---
    inhibit_system_recovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromised_device(Host),
        defense_disabled(Host, 'system_recovery').

    compromised_device(Host) :-
        exec(Host, user),
        defense_disabled(Host, 'backup_catalog'),
        defense_disabled(Host, 'volume_shadow_copy'),
        defense_disabled(Host, 'automatic_repair').

% --- T1491_defacement ---
    defacement_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, "web", _).

    compromise(Host, user) :-
        defacement_possible(Host).

% --- T1492_stored_data_manipulation ---
    data_manipulation(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, _DataKind),
        compromise(Host, user).

    compromise(Host, user) :-
        data_manipulation(Host).

% --- T1493_transmitted_data_manipulation ---
    data_manipulation(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, Proto, Service, Version),
        specialized_tool(Service).

    compromise(Host, user) :-
        data_manipulation(Host).

% --- T1494_runtime_data_manipulation ---
    runtime_data_manipulation(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        exec(Host, User),
        compromise_from_exec(Host, user).

    compromise(Host, user) :-
        runtime_data_manipulation(Host).

% --- T1496_resource_hijacking ---
    resource_hijacking(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        exec(Host, User),
        compromise(Host, user).

    compromise(Host, user) :-
        resource_hijacking(Host).

% --- T1497_virtualization_sandbox_evasion ---
    analysis_env_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'Sysinternals', _Version).

    compromise(Host, user) :-
        analysis_env_detected(Host).

% --- T1498_network_denial_of_service ---
    network_dos_attack(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, _, _),
        bandwidth_exhaustion(Network, Host).

bandwidth_exhaustion(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    high_traffic_volume(Network, Host).

high_traffic_volume(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    malicious_traffic(Network, Host).

malicious_traffic(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    spoofed_ip_traffic(Network, Host).

spoofed_ip_traffic(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    ip_spoofing(Network, Host).

ip_spoofing(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    botnet_traffic(Network, Host).

botnet_traffic(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    distributed_attack(Network, Host).

distributed_attack(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    multiple_sources(Network, Host).

multiple_sources(Network, Host) :-
    attacker_at(Network),
    connects(Network, Host, Port),
    attacker_at(AnotherNetwork),
    connects(AnotherNetwork, Host, Port),
    Network \= AnotherNetwork.

% --- T1499_endpoint_denial_of_service ---
    endpoint_dos(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, Service, _),
        (Service = web_server; Service = dns_server; Service = email_service; Service = web_app),
        compromised_device(Host).

    compromised_device(Host) :-
        endpoint_dos(Host).

% --- T1500_compile_after_delivery ---
    compile_after_delivery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, OS),
        member(OS, [linux, macos, windows]),
        user_on_host(User, Host),
        spearphishing_attachment(User, Host),
        source_code_payload(Host),
        needs_compilation(Host).

compromise(Host, user) :-
    compile_after_delivery(Host).

% --- T1501_systemd_service ---
    persistence(Host, systemd_service) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, linux),
        (perm(Host, root, _); perm(Host, user, _)).
    compromise(Host, user) :-
        persistence(Host, systemd_service).

% --- T1502_parent_pid_spoofing ---
    ppid_spoofing_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, windows),
        exec(Host, User),
        perm(Host, User, admin).

    compromise(Host, system) :-
        ppid_spoofing_possible(Host),
        priv_escalation(Host, admin, system).

% --- T1504_powershell_profile ---
    persistence(Host, 'PowerShellProfile') :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, 'Windows'),
        app_installed(Host, 'PowerShell', _Version).

    priv_escalation(Host, user, admin) :-
        persistence(Host, 'PowerShellProfile'),
        exec(Host, admin).

% --- T1505_server_software_component ---
    malicious_component_installed(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        malicious_app_installed(App, Version).

    persistence(Host, 'Server Software Component') :-
        malicious_component_installed(Host).

% --- T1506_web_session_cookie ---
    session_cookie_stolen(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        auth_usable_cred(User, Host, session_cookie).

    compromise(Host, user) :-
        session_cookie_stolen(Host, user).

% --- T1507_network_information_discovery ---
    network_info_discovery(Device) :-
        attacker_at(Network),
        connects(Network, Device, _Port),
        device(Device),
        sensor(Device, wifi),
        sensor(Device, bluetooth).

    compromise(Device, user) :-
        network_info_discovery(Device).

% --- T1508_suppress_application_icon ---
    malicious_app_installed(Host, App) :-
        app_installed(Host, App, _Version),
        os(Host, android),
        defense_disabled(Host, app_icon_visibility).

    compromise(Host, user) :-
        malicious_app_installed(Host, _App).

% --- T1509_non-standard_port ---
    non_standard_port_usage(Host, Port) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, Proto, _, _),
        \+ protocol(Proto, _, Port).

    compromise(Host, user) :-
        non_standard_port_usage(Host, Port).

% --- T1510_clipboard_modification ---
    clipboard_modification(Host) :-
        device(Host),
        os(Host, android),
        malicious_app_installed(Host, 'Android/Clipper.C'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        clipboard_modification(Host).

% --- T1512_video_capture ---
    video_capture_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version),
        mobile_perm(App, 'android.permission.CAMERA'),
        os(Host, 'Android').

    video_capture_possible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version),
        mobile_perm(App, 'NSCameraUsageDescription'),
        os(Host, 'iOS').

    compromise(Host, user) :-
        video_capture_possible(Host),
        (jailbroken(Host); rooted(Host)).

% --- T1513_screen_capture ---
    screen_capture_possible(Host) :-
        app_installed(Host, 'MediaProjectionManager', _Version),
        mobile_perm('MediaProjectionManager', 'screen_capture'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        screen_capture_possible(Host).

% --- T1514_elevated_execution_with_prompt ---
    priv_escalation(Host, user, root) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macOS),
        user_on_host(User, Host),
        exec_possible_user(Host, User),
        auth_usable_cred(User, Host, prompt),
        app_installed(Host, 'AuthorizationExecuteWithPrivileges', _Version).
    compromise(Host, root) :-
        priv_escalation(Host, user, root).

% --- T1516_input_injection ---
    input_injection_possible(Host) :-
        app_installed(Host, 'malicious_app', _Version),
        mobile_perm('malicious_app', 'accessibility_api'),
        os(Host, 'Android').

    compromise(Host, user) :-
        input_injection_possible(Host).

% --- T1517_access_notifications ---
    notification_data_collected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, notification),
        os(Host, android).

    compromise(Host, user) :-
        notification_data_collected(Host).

% --- T1518_software_discovery ---
    software_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version).

    compromise(Host, user) :-
        software_discovery(Host),
        local_pe_vulnerable(Host, CVE).

% --- T1519_emond ---
    persistence(Host, emond) :-
        app_installed(Host, emond, _Version),
        os(Host, macos),
        attacker_at(net),
        connects(net, Host, _Port).

    priv_escalation(Host, admin, root) :-
        persistence(Host, emond).

% --- T1520_domain_generation_algorithms ---
    dga_used_for_c2(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, dga).

    compromise(Host, user) :-
        dga_used_for_c2(Host).

% --- T1521_encrypted_channel ---
    encrypted_traffic(Host, Protocol) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        Protocol = 'encrypted'.

    compromise(Host, user) :-
        encrypted_traffic(Host, Protocol).

% --- T1522_cloud_instance_metadata_api ---
    metadata_access(Host, provider) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromise(Host, user).

    compromise(Host, user) :-
        metadata_access(Host, provider),
        cloud_cred(User, type).

% --- T1525_implant_internal_image ---
    implant_internal_image(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        cloud_account(Account, Provider),
        cloud_cred(Principal, Type),
        iam_policy(Principal, 'list_images', Resource, 'allow'),
        compromise(Host, user),
        persistence(Host, 'implanted_image').

% --- T1526_cloud_service_discovery ---
    cloud_service_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        cloud_account(Account, Provider),
        cloud_cred(Principal, Type),
        valid_account(User, Host),
        user_on_host(User, Host).

    compromise(Host, user) :-
        cloud_service_discovery(Host).

% --- T1527_application_access_token ---
    compromised_access_token(User, Token) :-
        attacker_at(net),
        stolen_token(User, Token).

compromise(Host, user) :-
    compromised_access_token(User, Token),
    user_on_host(User, Host),
    auth_usable_cred(User, Host, oauth_token).

% --- T1529_system_shutdown_reboot ---
    system_shutdown_reboot(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        perm(Host, User, admin).

    impact(Host) :-
        system_shutdown_reboot(Host).

% --- T1530_data_from_cloud_storage ---
    cloud_data_accessible(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        misconfig(Host, public_access).

    compromise(Host, user) :-
        cloud_data_accessible(Host).

% --- T1532_archive_collected_data ---
    archive_collected_data(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        (compress_data(Host, DataKind); encrypt_data(Host, DataKind)),
        exfiltrated(Host, DataKind, Channel).

    compress_data(Host, DataKind) :-
        % Logic to determine if data is compressed
        true.

    encrypt_data(Host, DataKind) :-
        % Logic to determine if data is encrypted
        true.

% --- T1533_data_from_local_system ---
    data_from_local_system(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        priv_escalation(Host, low, high),
        mobile_perm(App, storage).

    compromise(Host, user) :-
        data_from_local_system(Host).

% --- T1534_internal_spearphishing ---
    internal_spearphishing(Host, User) :-
        compromised_device(Host),
        valid_account(User, Host),
        user_phishes_success(User).

    compromise(Host, user) :-
        internal_spearphishing(Host, User).

% --- T1535_unused_unsupported_cloud_regions ---
    unused_cloud_region_exploitation(Host, Region) :-
        attacker_at(net),
        cloud_account(User, Provider),
        cloud_cred(User, type),
        connects(net, Host, _Port),
        not(monitored_region(Provider, Region)),
        creates_instance_in_region(User, Provider, Region).

compromise(Host, user) :-
    unused_cloud_region_exploitation(Host, _Region).

% --- T1536_revert_cloud_instance ---
    revert_cloud_instance(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        cloud_account(Account, Provider),
        cloud_cred(User, 'admin'),
        compromise(Host, user),
        event(Host, 'defense-evasion', 'T1536', EvidenceId).

    % Rule to indicate that an attacker can revert a cloud instance to evade detection
    can_revert_instance(Host) :-
        revert_cloud_instance(Host),
        iam_policy(User, 'revert', Host, 'allow').

% --- T1537_transfer_data_to_cloud_account ---
    exfiltrated(Host, data, cloud_account) :-
        attacker_at(net),
        connects(net, Host, _Port),
        cloud_account(AttackerAccount, Provider),
        has_data(Host, data),
        cloud_cred(AttackerAccount, _Type).

% --- T1538_cloud_service_dashboard ---
    cloud_dashboard_access(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        auth_usable_cred(User, Host, cloud_dashboard),
        cloud_account(User, _Provider).

    compromise(Host, user) :-
        cloud_dashboard_access(Host, User).

% --- T1539_steal_web_session_cookie ---
    steal_web_session_cookie(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, session_cookie),
        user_on_host(User, Host).

    compromise(Host, user) :-
        steal_web_session_cookie(Host, User).

% --- T1540_code_injection ---
    code_injection_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        vulnerable_app(App, Version).

    compromise(Host, elevated_privileges) :-
        code_injection_possible(Host),
        exec(Host, elevated_privileges).

% --- T1541_foreground_persistence ---
    malicious_app_installed(Host, App) :-
        app_installed(Host, App, _Version),
        mobile_perm(App, sensor_access),
        attacker_at(net),
        connects(net, Host, _Port).

    persistence(Host, 'Foreground Persistence') :-
        malicious_app_installed(Host, App),
        mobile_perm(App, foreground_service).

% --- T1542_pre-os_boot ---
    persistence(Host, pre_os_boot) :-
        attacker_at(net),
        connects(net, Host, _Port),
        firmware_accessible(Host).

    compromise(Host, kernel) :-
        persistence(Host, pre_os_boot).

% --- T1543_create_or_modify_system_process ---
    persistence(Host, system_process) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, admin),
        perm(Host, admin, root),
        execCode(Host, root).

    priv_escalation(Host, admin, root) :-
        persistence(Host, system_process).

% --- T1544_ingress_tool_transfer ---
    ingress_tool_transfer(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        c2_channel(Host, _Proto).

    compromise(Host, user) :-
        ingress_tool_transfer(Host).

% --- T1548_abuse_elevation_control_mechanism ---
    priv_escalation(Host, user, root) :-
        attacker_at(net),
        connects(net, Host, _Port),
        defense_disabled(Host, elevation_control).
    compromise(Host, root) :-
        priv_escalation(Host, user, root).

% --- T1550_use_alternate_authentication_material ---
    stolen_auth_material(User, Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        auth_usable_cred(User, Host, method),
        auth_possible(Host, User, method).

    compromise(Host, user) :-
        stolen_auth_material(User, Host).

% --- T1552_unsecured_credentials ---
    unsecured_credentials(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        has_data(Host, credentials),
        loot(Host, credentials, _Id).

    compromise(Host, user) :-
        unsecured_credentials(Host, User).

% --- T1553_subvert_trust_controls ---
    subvert_trust_controls(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        (defense_disabled(Host, 'code_signing') ;
         defense_disabled(Host, 'internet_download_warning') ;
         defense_disabled(Host, 'untrusted_site_warning')).

    compromise(Host, user) :-
        subvert_trust_controls(Host).

% --- T1554_compromise_host_software_binary ---
    compromised_binary(Host, Binary) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, Binary, _Version),
        malicious_app_installed(Host, Binary).

    persistence(Host, 'Compromise Host Software Binary') :-
        compromised_binary(Host, _Binary).

% --- T1555_credentials_from_password_stores ---
    credentials_from_password_stores(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, password_manager, _Version),
        user_on_host(User, Host),
        cred(User, Host, password).

    compromise(Host, user) :-
        credentials_from_password_stores(Host, User).

% --- T1556_modify_authentication_process ---
    modified_auth_process(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        auth_possible(Host, User, modified_method),
        not(valid_account(User, Host)).

    compromise(Host, user) :-
        modified_auth_process(Host).

% --- T1558_steal_or_forge_kerberos_tickets ---
    kerberos_ticket_stolen_or_forged(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        auth_usable_cred(User, Host, kerberos).

    compromise(Host, user) :-
        kerberos_ticket_stolen_or_forged(Host, User).

% --- T1559_inter-process_communication ---
    ipc_abuse(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        os(Host, OS),
        member(OS, [windows, macos, linux]).

    compromise(Host, user) :-
        ipc_abuse(Host).

% --- T1560_archive_collected_data ---
    archive_collected_data(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        (compress_data(Host, DataKind); encrypt_data(Host, DataKind)).

    exfiltrated(Host, DataKind, Channel) :-
        archive_collected_data(Host),
        c2_channel(Host, Channel).

% --- T1561_disk_wipe ---
    disk_wipe(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        perm(Host, User, root).

    compromise(Host, user) :-
        disk_wipe(Host).

% --- T1562_impair_defenses ---
    defense_disabled(Host, Control) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromised_device(Host),
        (Control = firewall; Control = antivirus; Control = detection_system).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        weakened_defense(Host).

% --- T1563_remote_service_session_hijacking ---
    session_hijack_possible(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        exposed(Host, Port),
        service(Host, Port, Proto, Service, Version),
        member(Service, [telnet, ssh, rdp]).

    compromise(Host, user) :-
        session_hijack_possible(Host, User).

% --- T1565_data_manipulation ---
    data_manipulation(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        compromised_device(Host).

    compromise(Host, user) :-
        data_manipulation(Host).

% --- T1566_phishing ---
    phishing_attempt(User, Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        user_phishes_success(User).

    compromise(Host, user) :-
        phishing_attempt(User, Host).

% --- T1567_exfiltration_over_web_service ---
    exfiltration_over_web_service(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, sensitive),
        exfiltrated(Host, sensitive, web_service),
        encrypted_traffic(Host, web_service).

% --- T1568_dynamic_resolution ---
    dynamic_resolution(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol).

    compromise(Host, user) :-
        dynamic_resolution(Host).

% --- T1569_system_services ---
    exec(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, _Proto, Service, _Version),
        user_on_host(User, Host),
        compromise_from_exec(Host, user).

% --- T1570_lateral_tool_transfer ---
    lateral_tool_transfer(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        valid_account(User, Host),
        auth_usable_cred(User, Host, Method),
        auth_possible(Host, User, Method),
        file_transfer_tool_installed(Host, Tool).

    compromise(Host, user) :-
        lateral_tool_transfer(Host).

% --- T1571_non-standard_port ---
    non_standard_port_usage(Host, Proto, Port) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, Proto, _, _),
        \+ standard_port(Proto, Port).

    compromise(Host, user) :-
        non_standard_port_usage(Host, _, _).

    standard_port(https, 443).
    standard_port(http, 80).
    standard_port(ftp, 21).
    standard_port(smtp, 25).
    standard_port(pop3, 110).
    standard_port(imap, 143).

% --- T1572_protocol_tunneling ---
    protocol_tunneling(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        encrypted_traffic(Host, Protocol),
        c2_channel(Host, Protocol).

    compromise(Host, user) :-
        protocol_tunneling(Host).

% --- T1573_encrypted_channel ---
    encrypted_c2_channel(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        encrypted_traffic(Host, Protocol).

    compromise(Host, user) :-
        encrypted_c2_channel(Host).

% --- T1574_hijack_execution_flow ---
    hijack_execution_flow(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, OS),
        (OS = 'Linux'; OS = 'macOS'; OS = 'Windows'),
        (exec_possible_remote(Host, User);
         exec_possible_user(Host, User)),
        (persistence(Host, 'execution_flow_hijack');
         priv_escalation(Host, 'user', 'admin');
         bypasses_app_control(Host)).

    compromise(Host, user) :-
        hijack_execution_flow(Host).

% --- T1575_native_api ---
    native_api_execution(Host) :-
        app_installed(Host, App, Version),
        os(Host, android),
        exec(Host, user),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        native_api_execution(Host).

% --- T1576_uninstall_malicious_application ---
    uninstall_malicious_app(Host) :-
        malicious_app_installed(Host, App),
        (mobile_perm(App, 'device_owner') ;
         mobile_perm(App, 'root') ;
         mobile_perm(App, 'accessibility_service')),
        compromised_device(Host).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port).

% --- T1577_compromise_application_executable ---
    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, Version),
        (vuln(Host, 'CVE-Janus', App);
         decompiled_and_rebuilt(App, Host)),
        malicious_app_installed(Host, App).

    decompiled_and_rebuilt(App, Host) :-
        app_installed(Host, App, _),
        attacker_modifies_app(App, Host).

    attacker_modifies_app(App, Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _).

% --- T1578_modify_cloud_compute_infrastructure ---
    modify_cloud_infrastructure(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        cloud_account(Host, Provider),
        cloud_cred(User, Type),
        iam_policy(User, modify, infrastructure, allow).

    compromise(Host, user) :-
        modify_cloud_infrastructure(Host).

% --- T1579_keychain ---
    compromised_device(Host) :-
        device(Host),
        os(Host, ios),
        (priv_escalation(Host, _, root); compromised_device(Host)),
        has_data(Host, keychain),
        attacker_at(net),
        connects(net, Host, _Port).

    loot(Host, keychain, T1579) :-
        compromised_device(Host),
        has_data(Host, keychain).

% --- T1580_cloud_infrastructure_discovery ---
    cloud_infrastructure_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        cloud_cred(User, 'access_key'),
        user_on_host(User, Host),
        cloud_account(Account, Provider),
        iam_policy(User, 'DescribeInstances', Resource, 'Allow'),
        iam_policy(User, 'ListBuckets', Resource, 'Allow'),
        iam_policy(User, 'DescribeDBInstances', Resource, 'Allow').

% --- T1581_geofencing ---
    geofencing_exploit(Host) :-
        app_installed(Host, App, Version),
        mobile_perm(App, 'ACCESS_FINE_LOCATION'),
        mobile_perm(App, 'ACCESS_BACKGROUND_LOCATION'),
        attacker_at(net),
        connects(net, Host, _Port).

    compromise(Host, user) :-
        geofencing_exploit(Host).

% --- T1582_sms_control ---
    sms_control_attack(Host) :-
        app_installed(Host, App, _Version),
        mobile_perm(App, 'RECEIVE_SMS'),
        mobile_perm(App, 'SEND_SMS'),
        malicious_app_installed(Host, App).

    compromise(Host, user) :-
        sms_control_attack(Host).

% --- T1583_acquire_infrastructure ---
    infrastructure_acquired(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _, _, _).

    compromise(Host, user) :-
        infrastructure_acquired(Host).

% --- T1584_compromise_infrastructure ---
    compromised_infrastructure(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        compromised_infrastructure(Host).

% --- T1585_establish_accounts ---
    establish_account(User, Service) :-
        attacker_at(net),
        valid_account(User, Service),
        connects(net, Service, _Port).

    compromise(Service, user) :-
        establish_account(User, Service).

% --- T1587_develop_capabilities ---
    capability_developed(Adversary, Capability) :-
        attacker_at(Adversary),
        identifies_development_requirements(Adversary, Requirements),
        builds_solution(Adversary, Capability, Requirements).

    supports_operations(Capability, Phase) :-
        capability_developed(_, Capability),
        used_in_phase(Capability, Phase).

% --- T1588_obtain_capabilities ---
    obtained_capabilities(Attacker) :-
        attacker_at(Attacker),
        (purchased_capability(Attacker, Capability);
         downloaded_capability(Attacker, Capability);
         stolen_capability(Attacker, Capability)).

    purchased_capability(Attacker, Capability) :-
        marketplace(Market),
        sells(Market, Capability),
        transaction(Attacker, Market, Capability).

    downloaded_capability(Attacker, Capability) :-
        free_resource(Resource),
        hosts(Resource, Capability),
        access(Attacker, Resource).

    stolen_capability(Attacker, Capability) :-
        third_party(Party),
        possesses(Party, Capability),
        theft(Attacker, Party, Capability).

% --- T1589_gather_victim_identity_information ---
    gather_victim_identity_info(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        has_data(Host, identity_info).

    compromise(Host, user) :-
        gather_victim_identity_info(Host).

% --- T1590_gather_victim_network_information ---
    gather_victim_network_info(Network) :-
        attacker_at(Network),
        exposed(Host, Port),
        connects(Network, Host, Port).

    reconnaissance_opportunity(Network) :-
        gather_victim_network_info(Network).

% --- T1591_gather_victim_org_information ---
    gather_victim_org_info(Attacker, Org) :-
        attacker_at(Attacker),
        exposed(Org, _Port),
        has_data(Org, org_info).

    reconnaissance(Attacker, Org) :-
        gather_victim_org_info(Attacker, Org).

% --- T1592_gather_victim_host_information ---
    gather_victim_host_info(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exposed(Host, _Port).

    reconnaissance(Host) :-
        gather_victim_host_info(Host).

% --- T1593_search_open_websites_domains ---
    gather_victim_org_info(Attacker, Victim) :-
        attacker_at(Attacker),
        user_accesses_content(Victim, 'open_website'),
        connects(_, Victim, _Port).

compromise(Victim, user) :-
    gather_victim_org_info(_, Victim).

% --- T1594_search_victim-owned_websites ---
    gather_victim_org_info(Host, InfoType) :-
        attacker_at(net),
        connects(net, Host, _Port),
        has_data(Host, InfoType).

    compromise(Host, user) :-
        gather_victim_org_info(Host, _).

% --- T1595_active_scanning ---
    active_scanning(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    reconnaissance(Host) :-
        active_scanning(Host).

% --- T1596_search_open_technical_databases ---
    gather_victim_org_info(Attacker, InfoSource) :-
        attacker_at(Attacker),
        open_technical_database(InfoSource),
        reconnaissance(Attacker, InfoSource).

reconnaissance(Attacker, InfoSource) :-
    attacker_at(Attacker),
    open_technical_database(InfoSource).

open_technical_database(Source) :-
    member(Source, [whois, dns_dumpster, circl_passive_dns, medium_ssl_cert, sslshopper_lookup, digitalshadows_cdn, shodan]).

% --- T1597_search_closed_sources ---
    gather_victim_org_info(Attacker, Information) :-
        attacker_at(Attacker),
        closed_source(Information),
        reconnaissance_tactic(Attacker, Information).

    reconnaissance_tactic(Attacker, Information) :-
        purchase_information(Attacker, Information, Source),
        closed_source(Source).

    closed_source(Source) :-
        reputable_private_source(Source);
        dark_web(Source);
        cybercrime_blackmarket(Source).

    purchase_information(Attacker, Information, Source) :-
        attacker_at(Attacker),
        closed_source(Source),
        information_available(Source, Information).

    information_available(Source, Information) :-
        subscription_feed(Source, Information);
        blackmarket_listing(Source, Information).

% --- T1598_phishing_for_information ---
    phishing_for_information(User) :-
        attacker_at(net),
        user_on_host(User, Host),
        connects(net, Host, _Port),
        user_phishes_success(User).

compromise(Host, user) :-
    phishing_for_information(User),
    user_on_host(User, Host).

% --- T1599_network_boundary_bridging ---
    compromised_device(Device) :-
        attacker_at(Network),
        connects(Network, Device, Port),
        role(Device, boundary_device),
        compromise(Device, admin).

    bypass_policy_enforcement(Device) :-
        compromised_device(Device),
        role(Device, boundary_device),
        allows_traffic(Device, restricted_traffic).

    lateral_movement_possible(Device, TargetNetwork) :-
        bypass_policy_enforcement(Device),
        connects(Device, TargetNetwork, _).

    exfiltration_possible(Device, DataKind) :-
        bypass_policy_enforcement(Device),
        has_data(Device, DataKind).

% --- T1600_weaken_encryption ---
    weakened_defense(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        device(Host),
        defense_disabled(Host, encryption).

    compromise(Host, user) :-
        weakened_defense(Host).

% --- T1601_modify_system_image ---
    modify_system_image(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, embedded),
        defense_disabled(Host, os_integrity),
        compromised_device(Host).

    compromised_device(Host) :-
        modify_system_image(Host).

% --- T1602_data_from_configuration_repository ---
    data_from_config_repo(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        has_data(Host, config_data).

    compromise(Host, user) :-
        data_from_config_repo(Host).

% --- T1604_proxy_through_victim ---
    proxy_through_victim(Host) :-
        compromised_device(Host),
        c2_channel(Host, socks_proxy),
        os(Host, android).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port).

% --- T1605_command-line_interface ---
    exec(Host, user) :-
        attacker_at(net),
        connects(net, Host, _Port),
        (rooted(Host); jailbroken(Host)),
        user_on_host(user, Host),
        perm(Host, user, root).

    compromise(Host, root) :-
        exec(Host, user).

% --- T1606_forge_web_credentials ---
    forged_web_credential(User, Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        auth_possible(Host, User, web),
        auth_usable_cred(User, Host, forged).

    compromise(Host, user) :-
        forged_web_credential(User, Host).

% --- T1608_stage_capabilities ---
    stage_capabilities(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (purchased_infrastructure(Host);
         compromised_infrastructure(Host);
         web_service_staging(Host);
         paas_staging(Host)).

    purchased_infrastructure(Host) :-
        acquire_infrastructure(Host).

    compromised_infrastructure(Host) :-
        compromise_infrastructure(Host).

    web_service_staging(Host) :-
        web_service(Host).

    paas_staging(Host) :-
        paas_service(Host).

    acquire_infrastructure(Host) :-
        % Logic to determine if infrastructure was acquired
        true.

    compromise_infrastructure(Host) :-
        % Logic to determine if infrastructure was compromised
        true.

    web_service(Host) :-
        % Logic to determine if web service is used for staging
        true.

    paas_service(Host) :-
        % Logic to determine if PaaS is used for staging
        true.

% --- T1609_container_administration_command ---
    container_admin_command_exec(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, tcp, 'container_admin_service', _Version).

    compromise(Host, user) :-
        container_admin_command_exec(Host).

% --- T1610_deploy_container ---
    deploy_container(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        service(Host, Port, _, 'docker', _).

    compromise(Host, user) :-
        deploy_container(Host).

% --- T1611_escape_to_host ---
    escape_to_host(Host) :-
        attacker_at(Container),
        connects(Container, Host, _Port),
        (misconfig(Container, 'bind_mount') ;
         priv_escalation(Container, 'user', 'root') ;
         local_vuln(Container, CVE),
         exploit_possible(CVE)).

    compromise(Host, root) :-
        escape_to_host(Host).

% --- T1612_build_image_on_host ---
    build_image_on_host(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, tcp, docker, _Version),
        c2_channel(Host, http),
        defense_disabled(Host, docker_api_access).

    compromise(Host, user) :-
        build_image_on_host(Host).

% --- T1613_container_and_resource_discovery ---
    container_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        service(Host, Port, _, 'docker', _).

    compromise(Host, user) :-
        container_discovery(Host).

% --- T1614_system_location_discovery ---
    system_location_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (setting(Host, timezone, _);
         setting(Host, keyboard_layout, _);
         setting(Host, language, _);
         metadata_access(Host, _);
         online_geolocation_service(Host, _)).

% --- T1615_group_policy_discovery ---
    group_policy_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        os(Host, windows),
        role(Host, domain_controller).

    compromise(Host, user) :-
        group_policy_discovery(Host).

% --- T1616_call_control ---
    call_control_compromise(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, _Version),
        mobile_perm(App, 'ANSWER_PHONE_CALLS'),
        mobile_perm(App, 'CALL_PHONE'),
        mobile_perm(App, 'PROCESS_OUTGOING_CALLS'),
        mobile_perm(App, 'MANAGE_OWN_CALLS'),
        mobile_perm(App, 'BIND_TELECOM_CONNECTION_SERVICE'),
        mobile_perm(App, 'WRITE_CALL_LOG').

    compromise(Host, user) :-
        call_control_compromise(Host).

% --- T1617_hooking ---
    hooking_attack(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        rooted(Host),
        app_installed(Host, 'Xposed', _Version);
        app_installed(Host, 'Magisk', _Version).

    compromise(Host, user) :-
        hooking_attack(Host).

% --- T1618_user_evasion ---
    user_evasion(Host) :-
        app_installed(Host, App, Version),
        sensor(Host, motion),
        malicious_app_installed(Host, App),
        device(Host).

    compromise(Host, user) :-
        user_evasion(Host).

% --- T1619_cloud_storage_object_discovery ---
    cloud_storage_object_discovery(Host, Account) :-
        attacker_at(net),
        connects(net, Host, _Port),
        cloud_account(Account, Provider),
        cloud_cred(Principal, type),
        iam_policy(Principal, 'ListObjects', Resource, 'Allow'),
        user_on_host(User, Host),
        valid_account(User, Host).

    compromise(Host, user) :-
        cloud_storage_object_discovery(Host, Account).

% --- T1620_reflective_code_loading ---
    reflective_code_loading(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exec(Host, user),
        not(file_created(Host)).

    compromise(Host, user) :-
        reflective_code_loading(Host).

% --- T1621_multi-factor_authentication_request_generation ---
    mfa_request_generation_attack(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        valid_account(User, Host),
        auth_possible(Host, User, mfa),
        auth_usable_cred(User, Host, password),
        user_mfa_fatigue(User).

compromise(Host, user) :-
    mfa_request_generation_attack(Host, User).

% --- T1622_debugger_evasion ---
    debugger_evasion_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        analysis_env_detected(Host).

    compromise(Host, user) :-
        debugger_evasion_detected(Host).

% --- T1623_command_and_scripting_interpreter ---
    exec_possible_remote(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        os(Host, android),
        user_on_host(User, Host).

    exec_possible_remote(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        os(Host, ios),
        user_on_host(User, Host).

    compromise_from_exec(Host, user) :-
        exec_possible_remote(Host, User).

% --- T1624_event_triggered_execution ---
    event_triggered_execution(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, android),
        persistence(Host, event_triggered).

    compromise(Host, user) :-
        event_triggered_execution(Host).

% --- T1625_hijack_execution_flow ---
    hijack_execution_flow(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        os(Host, android),
        persistence(Host, hijack_execution_flow).

    compromise(Host, user) :-
        hijack_execution_flow(Host).

% --- T1626_abuse_elevation_control_mechanism ---
    priv_escalation(Host, low, high) :-
        attacker_at(net),
        connects(net, Host, _Port),
        defense_disabled(Host, elevation_control),
        user_on_host(User, Host),
        perm(Host, User, low).

    compromise(Host, high) :-
        priv_escalation(Host, low, high).

% --- T1627_execution_guardrails ---
    execution_guardrails(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        expected_environment(Host, Environment),
        matches_environment(Host, Environment).

compromise(Host, user) :-
    execution_guardrails(Host).

% --- T1628_hide_artifacts ---
    hide_artifacts(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        mobile_perm(App, 'hide_launcher_icon'),
        os(Host, 'Android').

    compromise(Host, user) :-
        hide_artifacts(Host).

% --- T1629_impair_defenses ---
    defense_disabled(Host, Control) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        compromised_device(Host),
        app_installed(Host, Control, _Version),
        Control \= "anti-virus",
        Control \= "detection-capability".

% --- T1630_indicator_removal_on_host ---
    indicator_removal_on_host(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        compromised_device(Host),
        defense_disabled(Host, 'event_collection'),
        defense_disabled(Host, 'reporting'),
        defense_disabled(Host, 'notification_system').

% --- T1631_process_injection ---
    process_injection_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        rooted(Host).

    compromise(Host, elevated_privileges) :-
        process_injection_possible(Host).

% --- T1632_subvert_trust_controls ---
    subvert_trust_controls(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        defense_disabled(Host, trust_mechanism),
        os(Host, mobile_os),
        (mobile_os = android; mobile_os = ios).

    compromise(Host, user) :-
        subvert_trust_controls(Host).

% --- T1633_virtualization_sandbox_evasion ---
    analysis_env_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port).

    evasion_attempt(Host) :-
        analysis_env_detected(Host),
        compromise(Host, user).

% --- T1634_credentials_from_password_store ---
    credentials_from_password_store(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'password_manager', Version),
        user_on_host(User, Host),
        loot(Host, 'credentials', Id),
        auth_usable_cred(User, Host, 'password').
    compromise(Host, user) :-
        credentials_from_password_store(Host, User).

% --- T1635_steal_application_access_token ---
    steal_application_access_token(User, Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_phishes_success(User),
        app_installed(Host, app, _Version),
        auth_usable_cred(User, Host, oauth_token).

compromise(Host, user) :-
    steal_application_access_token(User, Host).

% --- T1636_protected_user_data ---
    access_protected_user_data(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        (jailbroken(Host); rooted(Host)),
        has_data(Host, protected_user_data).

    compromise(Host, user) :-
        access_protected_user_data(Host).

% --- T1637_dynamic_resolution ---
    dynamic_resolution(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        dynamic_parameters(Host, Domain, IP, Port).

    compromise(Host, user) :-
        dynamic_resolution(Host).

% --- T1638_adversary-in-the-middle ---
    adversary_in_the_middle(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (malicious_app_installed(Host, app_vpn);
         mobile_perm(app, 'READ_EXTERNAL_STORAGE');
         mobile_perm(app, 'WRITE_EXTERNAL_STORAGE');
         rogue_access_point(Network)).

    compromise(Host, user) :-
        adversary_in_the_middle(Host).

% --- T1639_exfiltration_over_alternative_protocol ---
    exfiltration_over_alternative_protocol(Host, DataKind, AltProto) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        \+ c2_channel(Host, AltProto),
        exfiltrated(Host, DataKind, AltProto).

% --- T1640_account_access_removal ---
    account_access_removal(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        modify_account(User, Host).

    compromise(Host, user) :-
        account_access_removal(Host, User).

% --- T1641_data_manipulation ---
    data_manipulation(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        has_data(Host, DataKind),
        specialized_software_access(Host, Software),
        prolonged_information_gathering(Host).

    compromise(Host, user) :-
        data_manipulation(Host).

% --- T1642_endpoint_denial_of_service ---
    endpoint_dos(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        (os(Host, android), app_installed(Host, device_admin_app, Version), Version < 7;
         os(Host, ios), jailbroken(Host), malicious_app_installed(Host, keyraider)).

% --- T1643_generate_traffic_from_victim ---
    generate_traffic_from_victim(Host) :-
        compromised_device(Host),
        app_installed(Host, App, _Version),
        mobile_perm(App, 'SEND_SMS'),
        os(Host, 'Android').

    generate_traffic_from_victim(Host) :-
        compromised_device(Host),
        app_installed(Host, App, _Version),
        os(Host, 'iOS'),
        \+ mobile_perm(App, 'SEND_SMS').

% --- T1644_out_of_band_data ---
    c2_channel(Host, Channel) :-
        compromised_device(Host),
        out_of_band_channel(Channel),
        mobile_perm(App, notification_access),
        app_installed(Host, App, _Version).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port).

    out_of_band_channel(sms).
    out_of_band_channel(nfc).
    out_of_band_channel(bluetooth).

% --- T1645_compromise_client_software_binary ---
    compromised_device(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        malicious_app_installed(Host, App),
        exec(Host, User),
        persistence(Host, 'Compromise Client Software Binary').

% --- T1646_exfiltration_over_c2_channel ---
    exfiltration_over_c2_channel(Host, DataKind) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        has_data(Host, DataKind),
        exfiltrated(Host, DataKind, Protocol).

% --- T1647_plist_file_modification ---
    plist_file_modified(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, macOS),
        setting(Host, plist_file, modified).

    compromise(Host, user) :-
        plist_file_modified(Host).

% --- T1648_serverless_execution ---
    serverless_execution(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        cloud_account(User, Provider),
        cloud_cred(User, CredType),
        exec_possible_remote(Host, User),
        compromise_from_exec(Host, User).

    compromise(Host, user) :-
        serverless_execution(Host, user).

% --- T1649_steal_or_forge_authentication_certificates ---
    steal_or_forge_certificates(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        auth_usable_cred(User, Host, certificate).

    compromise(Host, user) :-
        steal_or_forge_certificates(Host, User).

% --- T1650_acquire_access ---
    acquired_access(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        valid_account(User, Host).

    compromise(Host, user) :-
        acquired_access(Host).

% --- T1651_cloud_administration_command ---
    exec(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        cloud_account(User, Provider),
        cloud_cred(User, admin),
        perm(Host, User, admin),
        exec_possible_remote(Host, User).

    compromise(Host, admin) :-
        exec(Host, User).

% --- T1652_device_driver_discovery ---
    device_driver_discovery(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        os(Host, _OS),
        exec(Host, user),
        compromise(Host, user).

    compromise(Host, user) :-
        device_driver_discovery(Host).

% --- T1653_power_settings ---
    persistence(Host, power_settings) :-
        attacker_at(net),
        connects(net, Host, _Port),
        setting(Host, powercfg, _Value).

    compromise(Host, user) :-
        persistence(Host, power_settings).

% --- T1654_log_enumeration ---
    log_enumeration(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        exposed(Host, _Port),
        app_installed(Host, 'log_enumeration_tool', _Version).

    compromise(Host, user) :-
        log_enumeration(Host).

% --- T1655_masquerading ---
    masquerading_detected(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        misleading_name(Host).

    compromise(Host, user) :-
        masquerading_detected(Host).

% --- T1656_impersonation ---
    impersonation_attack(User, Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        trust(User, Host, impersonation),
        exec_possible_user(Host, User).

    compromise(Host, user) :-
        impersonation_attack(User, Host).

% --- T1657_financial_theft ---
    financial_theft(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        compromise(Host, user),
        has_data(Host, financial_information),
        exfiltrated(Host, financial_information, Channel).

    compromise(Host, user) :-
        user_phishes_success(User),
        user_on_host(User, Host).

    exfiltrated(Host, financial_information, Channel) :-
        c2_channel(Host, Channel).

% --- T1658_exploitation_for_client_execution ---
    exec_possible_remote(Host, user) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port),
        remote_service_vulnerable(Host, Port, CVE, Service).

    compromise(Host, user) :-
        exec_possible_remote(Host, user).

% --- T1659_content_injection ---
    content_injection_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exposed(Host, Port).

    compromise(Host, user) :-
        content_injection_possible(Host).

% --- T1660_phishing ---
    phishing_attempt(Host, User) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        user_on_host(User, Host),
        exec_possible_user(Host, User).

    compromise(Host, user) :-
        phishing_attempt(Host, User),
        user_phishes_success(User).

% --- T1661_application_versioning ---
    malicious_app_installed(Device, App) :-
        attacker_at(net),
        app_installed(Device, App, Version),
        trusted_app_store(App),
        app_update_malicious(App, NewVersion),
        user_on_host(User, Device),
        mobile_perm(App, Permission),
        grants_permission(User, App, Permission).

    compromise(Device, user) :-
        malicious_app_installed(Device, App).

% --- T1662_data_destruction ---
    data_destruction(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        exec(Host, User),
        app_installed(Host, App, Version),
        (App = 'pm' ; App = 'rm'),
        compromised_device(Host).

    compromised_device(Host) :-
        data_destruction(Host).

% --- T1663_remote_access_software ---
    c2_channel(Host, Protocol) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, App, Version),
        member(App, ['VNC', 'TeamViewer', 'AirDroid', 'AirMirror']),
        Protocol = 'remote_access'.

    compromise(Host, user) :-
        c2_channel(Host, 'remote_access').

% --- T1664_exploitation_for_initial_access ---
    exploit_vulnerability(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        remote_service_vulnerable(Host, Port, CVE, Service).

    compromise(Host, user) :-
        exploit_vulnerability(Host).

% --- T1665_hide_infrastructure ---
    hide_infrastructure(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        c2_channel(Host, Protocol),
        defense_disabled(Host, 'network_monitoring'),
        allows_traffic(Host, 'malicious_domain').

    compromise(Host, user) :-
        hide_infrastructure(Host).

% --- T1666_modify_cloud_resource_hierarchy ---
    modify_cloud_hierarchy(Account, Provider) :-
        cloud_account(Account, Provider),
        cloud_cred(Principal, admin),
        attacker_at(net),
        connects(net, Account, _Port),
        iam_policy(Principal, modify, hierarchy, allow).

    compromise(Account, admin) :-
        modify_cloud_hierarchy(Account, Provider).

% --- T1667_email_bombing ---
    email_bombing(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, _, email, _),
        overload_inbox(Host).

    overload_inbox(Host) :-
        service(Host, _Port, _, email, _),
        sends_mass_emails(Host).

    sends_mass_emails(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, _, email, _).

% --- T1668_exclusive_control ---
    exclusive_control(Host) :-
        compromised_device(Host),
        (disable_vulnerable_service(Host);
         patch_vulnerable_system(Host);
         strip_privileges(Host);
         remove_other_malware(Host)).

    compromised_device(Host) :-
        attacker_at(net),
        connects(net, Host, _Port).

    disable_vulnerable_service(Host) :-
        defense_disabled(Host, service).

    patch_vulnerable_system(Host) :-
        not(remote_service_vulnerable(Host, _Port, _CVE, _Service)).

    strip_privileges(Host) :-
        perm(Host, User, _Priv),
        not(perm(Host, User, admin)).

    remove_other_malware(Host) :-
        not(malicious_app_installed(Host, _App)).

% --- T1669_wi-fi_networks ---
    wifi_access(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        (open_wifi(Host, Port); valid_account(User, Host)),
        reachable(Network, Host, Port).

    compromise(Host, user) :-
        wifi_access(Host).

% --- T1670_virtualization_solution ---
    virtualization_solution_exploit(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, 'Android Virtualization Framework', _Version),
        malicious_app_installed(Host, 'BankingAppMimic'),
        exec(Host, User),
        has_data(Host, 'credentials'),
        exfiltrated(Host, 'credentials', 'virtual_env').

    compromise(Host, user) :-
        virtualization_solution_exploit(Host).

% --- T1671_cloud_application_integration ---
    persistence(Host, 'OAuthAppIntegration') :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, App, 'OAuth'),
        grants_permission(App, User, 'high_privilege'),
        cloud_account(User, Provider),
        cloud_cred(User, 'service_principal').

    compromise(Host, user) :-
        persistence(Host, 'OAuthAppIntegration').

% --- T1672_email_spoofing ---
    email_spoofing_possible(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        misconfig(Host, 'SPF_DKIM_DMARC'),
        weakened_defense(Host).

    compromise(Host, user) :-
        email_spoofing_possible(Host).

% --- T1673_virtual_machine_discovery ---
    virtual_machine_discovery(Host) :-
        attacker_at(Network),
        connects(Network, Host, Port),
        app_installed(Host, "Hypervisor CLI", _Version).

    compromise(Host, user) :-
        virtual_machine_discovery(Host).

% --- T1674_input_injection ---
    input_injection(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        user_on_host(User, Host),
        (connected_removable_media(Host, _Device);
         vulnerable_browser(Host)),
        exec(Host, User).

    compromise(Host, user) :-
        input_injection(Host).

% --- T1675_esxi_administration_command ---
    esxi_admin_command_execution(Host) :-
        attacker_at(net),
        connects(net, Host, _Port),
        service(Host, _Port, tcp, 'vmware-tools-daemon', _Version).

    compromise(Host, user) :-
        esxi_admin_command_execution(Host).

interaction_rule( ( compromise(Host, user) :-         gather_process_state_info(Host) ), rule_desc('T0801_monitor_process_state - Monitor Process State', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         automated_collection(Host) ), rule_desc('T0802_automated_collection - Automated Collection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         brute_force_io(Host) ), rule_desc('T0806_brute_force_i_o - Brute Force I/O', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(Network),         connects(Network, Host, Port),         exposed(Host, Port),         auth_possible(Host, User, Method),         auth_usable_cred(User, Host, Method) ), rule_desc('T0807_command-line_interface - Command-Line Interface', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         control_device_identification(Host) ), rule_desc('T0808_control_device_identification - Control Device Identification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_destruction(Host) ), rule_desc('T0809_data_destruction - Data Destruction', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_collected_from_repo(Host, _DataKind) ), rule_desc('T0811_data_from_information_repositories - Data from Information Repositories', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         default_credentials_exploitable(Host) ), rule_desc('T0812_default_credentials - Default Credentials', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         denial_of_control(Host) ), rule_desc('T0813_denial_of_control - Denial of Control', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         denial_of_service(Host) ), rule_desc('T0814_denial_of_service - Denial of Service', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         denial_of_view(Host) ), rule_desc('T0815_denial_of_view - Denial of View', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         device_restart_shutdown(Host) ), rule_desc('T0816_device_restart_shutdown - Device Restart/Shutdown', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         watering_hole_attack(Host) ), rule_desc('T0817_drive-by_compromise - Drive-by Compromise', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remote_service_vulnerable(Host, _Port, _CVE, _Service) ), rule_desc('T0819_exploit_public-facing_application - Exploit Public-Facing Application', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_for_evasion(Host) ), rule_desc('T0820_exploitation_for_evasion - Exploitation for Evasion', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modify_controller_tasking(Host) ), rule_desc('T0821_modify_controller_tasking - Modify Controller Tasking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         potential_t0822(Host) ), rule_desc('T0822_external_remote_services - External Remote Services', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     gui_access_possible(Host) ), rule_desc('T0823_graphical_user_interface - Graphical User Interface', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         io_module_discovery(Host) ), rule_desc('T0824_i_o_module_discovery - I/O Module Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         location_identification_possible(Host) ), rule_desc('T0825_location_identification - Location Identification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         loss_of_availability(Host) ), rule_desc('T0826_loss_of_availability - Loss of Availability', 1.0) ).
interaction_rule( ( compromise(Host, control_system) :-         loss_of_control(Host) ), rule_desc('T0827_loss_of_control - Loss of Control', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         loss_of_view(Host) ), rule_desc('T0829_loss_of_view - Loss of View', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         manipulation_of_control(Host) ), rule_desc('T0831_manipulation_of_control - Manipulation of Control', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         manipulated_view(Host) ), rule_desc('T0832_manipulation_of_view - Manipulation of View', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modify_control_logic(Host) ), rule_desc('T0833_modify_control_logic - Modify Control Logic', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         native_api_access(Host) ), rule_desc('T0834_native_api - Native API', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     manipulate_io_image(Host) ), rule_desc('T0835_manipulate_i_o_image - Manipulate I/O Image', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modify_parameter(Host) ), rule_desc('T0836_modify_parameter - Modify Parameter', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_connection_enumeration(Host) ), rule_desc('T0840_network_connection_enumeration - Network Connection Enumeration', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_service_scanning(Host) ), rule_desc('T0841_network_service_scanning - Network Service Scanning', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_sniffing_possible(Host),         has_data(Host, credentials),         exfiltrated(Host, credentials, network) ), rule_desc('T0842_network_sniffing - Network Sniffing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         program_download_possible(Host) ), rule_desc('T0843_program_download - Program Download', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         infected_pou(Host) ), rule_desc('T0844_program_organization_units - Program Organization Units', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         program_upload_possible(Host) ), rule_desc('T0845_program_upload - Program Upload', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remote_system_discovery(Host) ), rule_desc('T0846_remote_system_discovery - Remote System Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         removable_media_infection(Host) ), rule_desc('T0847_replication_through_removable_media - Replication Through Removable Media', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         rogue_master_setup(Host) ), rule_desc('T0848_rogue_master - Rogue Master', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         masquerading_detected(Host) ), rule_desc('T0849_masquerading - Masquerading', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         role_identification(Host) ), rule_desc('T0850_role_identification - Role Identification', 1.0) ).
interaction_rule( ( compromise(Host, kernel) :-         rootkit_installed(Host) ), rule_desc('T0851_rootkit - Rootkit', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         screen_capture_possible(Host) ), rule_desc('T0852_screen_capture - Screen Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         scripting_executed(Host, user) ), rule_desc('T0853_scripting - Scripting', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         unauthorized_command_message(Host) ), rule_desc('T0855_unauthorized_command_message - Unauthorized Command Message', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         spoof_reporting_message(Host) ), rule_desc('T0856_spoof_reporting_message - Spoof Reporting Message', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         change_operating_mode(Host) ), rule_desc('T0858_change_operating_mode - Change Operating Mode', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         valid_account_access(Host, User) ), rule_desc('T0859_valid_accounts - Valid Accounts', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         wireless_compromise(Host) ), rule_desc('T0860_wireless_compromise - Wireless Compromise', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         collected_point_tag_values(Host) ), rule_desc('T0861_point_tag_identification - Point & Tag Identification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         supply_chain_compromise(Host) ), rule_desc('T0862_supply_chain_compromise - Supply Chain Compromise', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     user_execution_possible(Host, User) ), rule_desc('T0863_user_execution - User Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         transient_asset_compromise(Host) ), rule_desc('T0864_transient_cyber_asset - Transient Cyber Asset', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         spearphishing_attachment(User, Host),         exec_possible_user(Host, User) ), rule_desc('T0865_spearphishing_attachment - Spearphishing Attachment', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_remote_service(Host) ), rule_desc('T0866_exploitation_of_remote_services - Exploitation of Remote Services', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         lateral_tool_transfer(Host) ), rule_desc('T0867_lateral_tool_transfer - Lateral Tool Transfer', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         detect_operating_mode(Host) ), rule_desc('T0868_detect_operating_mode - Detect Operating Mode', 1.0) ).
interaction_rule( ( c2_channel(Host, Proto) :-         attacker_at(Network),         connects(Network, Host, Port),         protocol(Proto, Port),         member(Proto, [http, https, opc, rdp, telnet, dnp3, modbus]) ), rule_desc('T0869_standard_application_layer_protocol - Standard Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, _Proto) ), rule_desc('T0869_standard_application_layer_protocol - Standard Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         gather_program_state_info(Host) ), rule_desc('T0870_detect_program_state - Detect Program State', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         infected_project_file(Host) ), rule_desc('T0873_project_file_infection - Project File Infection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hooking_possible(Host) ), rule_desc('T0874_hooking - Hooking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         change_program_state(Host) ), rule_desc('T0875_change_program_state - Change Program State', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         alarm_suppression(Host) ), rule_desc('T0878_alarm_suppression - Alarm Suppression', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         loss_of_safety(Host) ), rule_desc('T0880_loss_of_safety - Loss of Safety', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         service_stopped(Host, _Service) ), rule_desc('T0881_service_stop - Service Stop', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         theft_of_operational_info(Host) ), rule_desc('T0882_theft_of_operational_information - Theft of Operational Information', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exposed(Host, Port),         auth_possible(Host, user, method),         auth_usable_cred(user, Host, method) ), rule_desc('T0883_internet_accessible_device - Internet Accessible Device', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         connection_proxy(Host) ), rule_desc('T0884_connection_proxy - Connection Proxy', 1.0) ).
interaction_rule( ( c2_channel(Host, Port) :-         attacker_at(Network),         connects(Network, Host, Port),         commonly_used_port(Port) ), rule_desc('T0885_commonly_used_port - Commonly Used Port', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, _Port) ), rule_desc('T0885_commonly_used_port - Commonly Used Port', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remote_service_accessible(Host, _),         valid_account(User, Host) ), rule_desc('T0886_remote_services - Remote Services', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         wireless_sniffing_possible(Host) ), rule_desc('T0887_wireless_sniffing - Wireless Sniffing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modify_program(Host) ), rule_desc('T0889_modify_program - Modify Program', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, root) :-         attacker_at(net),         connects(net, Host, Port),         local_pe_vulnerable(Host, CVE),         exec(Host, user),         compromise_from_pe(Host, root) ), rule_desc('T0890_exploitation_for_privilege_escalation - Exploitation for Privilege Escalation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_collected(Host, _) ), rule_desc('T0893_data_from_local_system - Data from Local System', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         proxy_execution_possible(Host) ), rule_desc('T0894_system_binary_proxy_execution - System Binary Proxy Execution', 1.0) ).
interaction_rule( ( exec(Host, user) :-         autorun_vulnerability(Host) ), rule_desc('T0895_autorun_image - Autorun Image', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         credential_dumping(Host, User) ), rule_desc('T1003_os_credential_dumping - OS Credential Dumping', 1.0) ).
interaction_rule( ( persistence(Host, winlogon_helper_dll) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, windows),         setting(Host, 'HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\Notify', _),         setting(Host, 'HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\Userinit', _),         setting(Host, 'HKLM\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Winlogon\\Shell', _) ), rule_desc('T1004_winlogon_helper_dll - Winlogon Helper DLL', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, winlogon_helper_dll) ), rule_desc('T1004_winlogon_helper_dll - Winlogon Helper DLL', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_collection(Host) ), rule_desc('T1005_data_from_local_system - Data from Local System', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         direct_volume_access(Host) ), rule_desc('T1006_direct_volume_access - Direct Volume Access', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         system_service_discovery(Host) ), rule_desc('T1007_system_service_discovery - System Service Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         binary_padding_evasion(Host) ), rule_desc('T1009_binary_padding - Binary Padding', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         application_window_discovery(Host) ), rule_desc('T1010_application_window_discovery - Application Window Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     query_registry(Host) ), rule_desc('T1012_query_registry - Query Registry', 1.0) ).
interaction_rule( ( persistence(Host, 'port_monitor') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, 'Windows'),         setting(Host, 'HKLM\\SYSTEM\\CurrentControlSet\\Control\\Print\\Monitors', _),         exec(Host, 'SYSTEM') ), rule_desc('T1013_port_monitors - Port Monitors', 1.0) ).
interaction_rule( ( compromise(Host, 'SYSTEM') :-         persistence(Host, 'port_monitor') ), rule_desc('T1013_port_monitors - Port Monitors', 1.0) ).
interaction_rule( ( compromise(Host, kernel) :-         rootkit_installed(Host) ), rule_desc('T1014_rootkit - Rootkit', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_config_discovery(Host) ), rule_desc('T1016_system_network_configuration_discovery - System Network Configuration Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec_possible_remote(Host, admin) ), rule_desc('T1017_application_deployment_software - Application Deployment Software', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remote_system_discovery(Host) ), rule_desc('T1018_remote_system_discovery - Remote System Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         firmware_compromise(Host) ), rule_desc('T1019_system_firmware - System Firmware', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         automated_exfiltration(Host) ), rule_desc('T1020_automated_exfiltration - Automated Exfiltration', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remote_access_possible(Host, User) ), rule_desc('T1021_remote_services - Remote Services', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         encrypted_exfiltration(Host, _, _) ), rule_desc('T1022_data_encrypted - Data Encrypted', 1.0) ).
interaction_rule( ( c2_channel(Host, custom_crypto) :-         attacker_at(Network),         connects(Network, Host, Port),         service(Host, Port, _, _, _),         compromised_device(Host) ), rule_desc('T1024_custom_cryptographic_protocol - Custom Cryptographic Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         multiband_communication(Host) ), rule_desc('T1026_multiband_communication - Multiband Communication', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         obfuscated_payload_detected(Host) ), rule_desc('T1027_obfuscated_files_or_information - Obfuscated Files or Information', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec_possible_remote(Host, User) ), rule_desc('T1028_windows_remote_management - Windows Remote Management', 1.0) ).
interaction_rule( ( exfiltrated(Host, data_chunk, network_channel) :-         attacker_at(Network),         connects(Network, Host, Port),         has_data(Host, sensitive),         not(alert_triggered(Host, data_transfer)) ), rule_desc('T1030_data_transfer_size_limits - Data Transfer Size Limits', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         encrypted_c2_traffic(Host) ), rule_desc('T1032_standard_cryptographic_protocol - Standard Cryptographic Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         user_discovery(Host, _User) ), rule_desc('T1033_system_owner_user_discovery - System Owner/User Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         path_interception_vulnerable(Host) ), rule_desc('T1034_path_interception - Path Interception', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         masquerading_detected(Host) ), rule_desc('T1036_masquerading - Masquerading', 1.0) ).
interaction_rule( ( persistence(Host, 'boot_or_logon_initialization_scripts') :-         attacker_at(net),         connects(net, Host, _Port),         (valid_account(User, Host); perm(Host, User, admin)),         exec(Host, User) ), rule_desc('T1037_boot_or_logon_initialization_scripts - Boot or Logon Initialization Scripts', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         persistence(Host, 'boot_or_logon_initialization_scripts'),         perm(Host, user, user) ), rule_desc('T1037_boot_or_logon_initialization_scripts - Boot or Logon Initialization Scripts', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         dll_search_order_hijacking(Host),         perm(Host, user, admin) ), rule_desc('T1038_dll_search_order_hijacking - DLL Search Order Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_from_network_shared_drive(Host) ), rule_desc('T1039_data_from_network_shared_drive - Data from Network Shared Drive', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_sniffing(Host) ), rule_desc('T1040_network_sniffing - Network Sniffing', 1.0) ).
interaction_rule( ( persistence(Host, 'Change Default File Association') :-         registry_key_modified(Host, 'HKEY_CLASSES_ROOT', _Extension) ), rule_desc('T1042_change_default_file_association - Change Default File Association', 1.0) ).
interaction_rule( ( c2_channel(Host, Proto) :-         attacker_at(Network),         connects(Network, Host, Port),         member(Port, [80, 443, 25, 53, 135, 22, 3389]),         protocol(Proto, _, Port) ), rule_desc('T1043_commonly_used_port - Commonly Used Port', 1.0) ).
interaction_rule( ( compromise(Host, high) :-         file_system_permissions_weakness(Host) ), rule_desc('T1044_file_system_permissions_weakness - File System Permissions Weakness', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         packed_software_detected(Host) ), rule_desc('T1045_software_packing - Software Packing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_service_discovery(Host) ), rule_desc('T1046_network_service_discovery - Network Service Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         wmi_execution_possible(Host) ), rule_desc('T1047_windows_management_instrumentation - Windows Management Instrumentation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     network_discovery(Host) ), rule_desc('T1049_system_network_connections_discovery - System Network Connections Discovery', 1.0) ).
interaction_rule( ( priv_escalation(Host, administrator, system) :-         new_service_installed(Host) ), rule_desc('T1050_new_service - New Service', 1.0) ).
interaction_rule( ( persistence(Host, new_service) :-         new_service_installed(Host) ), rule_desc('T1050_new_service - New Service', 1.0) ).
interaction_rule( ( compromise(Host, 'system') :-         shared_webroot_attack(Host),         priv_escalation(Host, 'web_server_user', 'system') ), rule_desc('T1051_shared_webroot - Shared Webroot', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         scheduled_task_abuse(Host, User) ), rule_desc('T1053_scheduled_task_job - Scheduled Task/Job', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         input_capture(Host, User) ), rule_desc('T1056_input_capture - Input Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         process_discovery(Host) ), rule_desc('T1057_process_discovery - Process Discovery', 1.0) ).
interaction_rule( ( persistence(Host, 'ServiceRegistryPermissions') :-         misconfig(Host, 'ServiceRegistryPermissions'),         exec(Host, 'attacker_controlled_program') ), rule_desc('T1058_service_registry_permissions_weakness - Service Registry Permissions Weakness', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(Network),         connects(Network, Host, Port),         app_installed(Host, 'CommandInterpreter', _Version),         user_on_host(User, Host) ), rule_desc('T1059_command_and_scripting_interpreter - Command and Scripting Interpreter', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec(Host, User) ), rule_desc('T1059_command_and_scripting_interpreter - Command and Scripting Interpreter', 1.0) ).
interaction_rule( ( persistence(Host, 'Registry Run Keys / Startup Folder') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, windows),         user_on_host(User, Host),         exec(Host, User) ), rule_desc('T1060_registry_run_keys_startup_folder - Registry Run Keys / Startup Folder', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, 'Registry Run Keys / Startup Folder') ), rule_desc('T1060_registry_run_keys_startup_folder - Registry Run Keys / Startup Folder', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hypervisor_persistence(Host) ), rule_desc('T1062_hypervisor - Hypervisor', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         security_software_discovery(Host) ), rule_desc('T1063_security_software_discovery - Security Software Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     scripting_execution(Host, user) ), rule_desc('T1064_scripting - Scripting', 1.0) ).
interaction_rule( ( c2_channel(Host, proto) :-         attacker_at(net),         connects(net, Host, Port),         exposed(Host, Port),         misconfig(Host, 'improperly configured firewall or proxy') ), rule_desc('T1065_uncommonly_used_port - Uncommonly Used Port', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         indicator_removal_from_tools(Host) ), rule_desc('T1066_indicator_removal_from_tools - Indicator Removal from Tools', 1.0) ).
interaction_rule( ( compromise(Host, kernel) :-         bootkit_persistence(Host) ), rule_desc('T1067_bootkit - Bootkit', 1.0) ).
interaction_rule( ( priv_escalation(Host, low_priv, high_priv) :-         attacker_at(net),         connects(net, Host, _Port),         local_pe_vulnerable(Host, _CVE),         exec(Host, low_priv),         perm(Host, low_priv, user),         perm(Host, high_priv, root) ), rule_desc('T1068_exploitation_for_privilege_escalation - Exploitation for Privilege Escalation', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         priv_escalation(Host, low_priv, high_priv) ), rule_desc('T1068_exploitation_for_privilege_escalation - Exploitation for Privilege Escalation', 1.0) ).
interaction_rule( ( c2_channel(Host, Protocol) :-         attacker_at(Network),         connects(Network, Host, Port),         protocol(Host, Protocol, Port) ), rule_desc('T1071_application_layer_protocol - Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, _) ), rule_desc('T1071_application_layer_protocol - Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         exec_possible_remote(Host, user) ), rule_desc('T1072_software_deployment_tools - Software Deployment Tools', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         dll_side_loading_vulnerable(Host),         attacker_at(net),         connects(net, Host, _Port) ), rule_desc('T1073_dll_side-loading - DLL Side-Loading', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         pth_feasible(Host, User) ), rule_desc('T1075_pass_the_hash - Pass the Hash', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         rdp_compromise(Host, _User) ), rule_desc('T1076_remote_desktop_protocol - Remote Desktop Protocol', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         rdp_session_hijack(Host, _User) ), rule_desc('T1076_remote_desktop_protocol - Remote Desktop Protocol', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         windows_admin_shares_accessible(Host, User) ), rule_desc('T1077_windows_admin_shares - Windows Admin Shares', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         valid_account_compromise(Host, User) ), rule_desc('T1078_valid_accounts - Valid Accounts', 1.0) ).
interaction_rule( ( c2_channel(Host, Protocol) :-         attacker_at(Network),         connects(Network, Host, Port),         service(Host, Port, Protocol, _, _),         (Protocol = https; Protocol = smtps),         compromised_device(Host) ), rule_desc('T1079_multilayer_encryption - Multilayer Encryption', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         taint_shared_content(Host) ), rule_desc('T1080_taint_shared_content - Taint Shared Content', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         credentials_in_files(Host, User),         auth_usable_cred(User, Host, password) ), rule_desc('T1081_credentials_in_files - Credentials in Files', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         file_and_directory_discovery(Host) ), rule_desc('T1083_file_and_directory_discovery - File and Directory Discovery', 1.0) ).
interaction_rule( ( persistence(Host, wmi_event_subscription) :-         attacker_at(Network),         connects(Network, Host, Port),         os(Host, windows),         exec(Host, User),         user_on_host(User, Host) ), rule_desc('T1084_windows_management_instrumentation_event_subscription - Windows Management Instrumentation Event Subscription', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, wmi_event_subscription) ), rule_desc('T1084_windows_management_instrumentation_event_subscription - Windows Management Instrumentation Event Subscription', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         account_discovery(Host) ), rule_desc('T1087_account_discovery - Account Discovery', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         bypass_uac(Host) ), rule_desc('T1088_bypass_user_account_control - Bypass User Account Control', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         proxy_used(Host) ), rule_desc('T1090_proxy - Proxy', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         process_hollowing_detected(Host) ), rule_desc('T1093_process_hollowing - Process Hollowing', 1.0) ).
interaction_rule( ( c2_channel(Host, Protocol) :-         attacker_at(Network),         connects(Network, Host, Port),         protocol(Host, Protocol, Port),         non_application_layer_protocol(Protocol) ), rule_desc('T1095_non-application_layer_protocol - Non-Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, Protocol) ), rule_desc('T1095_non-application_layer_protocol - Non-Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         ntfs_file_attribute_attack(Host) ), rule_desc('T1096_ntfs_file_attributes - NTFS File Attributes', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         pass_the_ticket_feasible(Host, User) ), rule_desc('T1097_pass_the_ticket - Pass the Ticket', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     account_manipulation(Host, User) ), rule_desc('T1098_account_manipulation - Account Manipulation', 1.0) ).
interaction_rule( ( persistence(Host, "web_shell") :-         web_shell_installed(Host) ), rule_desc('T1100_web_shell - Web Shell', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         web_shell_installed(Host) ), rule_desc('T1100_web_shell - Web Shell', 1.0) ).
interaction_rule( ( persistence(Host, 'Security Support Provider') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, 'Windows'),         setting(Host, 'HKLM\\SYSTEM\\CurrentControlSet\\Control\\Lsa\\Security Packages', _),         setting(Host, 'HKLM\\SYSTEM\\CurrentControlSet\\Control\\Lsa\\OSConfig\\Security Packages', _) ), rule_desc('T1101_security_support_provider - Security Support Provider', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, 'Security Support Provider') ), rule_desc('T1101_security_support_provider - Security Support Provider', 1.0) ).
interaction_rule( ( c2_channel(Host, web_service) :-         attacker_at(net),         connects(net, Host, _Port),         service(Host, _Port, https, web_service, _Version) ), rule_desc('T1102_web_service - Web Service', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, web_service) ), rule_desc('T1102_web_service - Web Service', 1.0) ).
interaction_rule( ( persistence(Host, appinit_dlls) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, windows),         setting(Host, 'HKEY_LOCAL_MACHINE\\Software\\Microsoft\\Windows NT\\CurrentVersion\\Windows', AppInitDLLs),         setting(Host, 'HKEY_LOCAL_MACHINE\\Software\\Wow6432Node\\Microsoft\\Windows NT\\CurrentVersion\\Windows', AppInitDLLs),         exec(Host, user),         not(defense_disabled(Host, secure_boot)) ), rule_desc('T1103_appinit_dlls - AppInit DLLs', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         persistence(Host, appinit_dlls) ), rule_desc('T1103_appinit_dlls - AppInit DLLs', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         ingress_tool_transfer(Host) ), rule_desc('T1105_ingress_tool_transfer - Ingress Tool Transfer', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         native_api_execution(Host) ), rule_desc('T1106_native_api - Native API', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         file_deletion(Host) ), rule_desc('T1107_file_deletion - File Deletion', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         redundant_access(Host) ), rule_desc('T1108_redundant_access - Redundant Access', 1.0) ).
interaction_rule( ( persistence(Host, firmware) :-         compromised_firmware(Host) ), rule_desc('T1109_component_firmware - Component Firmware', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         brute_force_possible(Host, User) ), rule_desc('T1110_brute_force - Brute Force', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         mfa_interception_possible(Host, User),         auth_usable_cred(User, Host, mfa) ), rule_desc('T1111_multi-factor_authentication_interception - Multi-Factor Authentication Interception', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modify_registry(Host) ), rule_desc('T1112_modify_registry - Modify Registry', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         attacker_at(net),         connects(net, Host, _Port) ), rule_desc('T1113_screen_capture - Screen Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         email_collection(Host, User) ), rule_desc('T1114_email_collection - Email Collection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         clipboard_data_access(Host) ), rule_desc('T1115_clipboard_data - Clipboard Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     signed_malware(Host) ), rule_desc('T1116_code_signing - Code Signing', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(Network),         connects(Network, Host, Port),         app_installed(Host, 'regsvr32.exe', _Version),         os(Host, 'Windows'),         user_on_host(User, Host),         exposed(Host, Port) ), rule_desc('T1117_regsvr32 - Regsvr32', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec(Host, User) ), rule_desc('T1117_regsvr32 - Regsvr32', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(net),         connects(net, Host, _Port),         app_installed(Host, 'InstallUtil', _Version),         os(Host, 'Windows'),         user_on_host(User, Host) ), rule_desc('T1118_installutil - InstallUtil', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         automated_collection(Host) ), rule_desc('T1119_automated_collection - Automated Collection', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(net),         connects(net, Host, _Port),         app_installed(Host, 'Regsvcs', _Version),         perm(Host, User, _Privilege) ), rule_desc('T1121_regsvcs_regasm - Regsvcs/Regasm', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         com_hijack_persistence(Host) ), rule_desc('T1122_component_object_model_hijacking - Component Object Model Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         audio_capture_possible(Host) ), rule_desc('T1123_audio_capture - Audio Capture', 1.0) ).
interaction_rule( ( exfiltrated(Host, 'audio_files', 'network') :-         audio_capture_possible(Host) ), rule_desc('T1123_audio_capture - Audio Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         system_time_discovery(Host) ), rule_desc('T1124_system_time_discovery - System Time Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         video_capture_possible(Host) ), rule_desc('T1125_video_capture - Video Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_share_connection_removed(Host) ), rule_desc('T1126_network_share_connection_removal - Network Share Connection Removal', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         trusted_dev_util_proxy_exec(Host) ), rule_desc('T1127_trusted_developer_utilities_proxy_execution - Trusted Developer Utilities Proxy Execution', 1.0) ).
interaction_rule( ( persistence(Host, netsh_helper_dll) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, windows),         app_installed(Host, netsh, _Version),         setting(Host, 'HKLM\\SOFTWARE\\Microsoft\\Netsh', _Value) ), rule_desc('T1128_netsh_helper_dll - Netsh Helper DLL', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, netsh_helper_dll) ), rule_desc('T1128_netsh_helper_dll - Netsh Helper DLL', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     compromised_certificate(Host) ), rule_desc('T1130_install_root_certificate - Install Root Certificate', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_encoded_c2(Host) ), rule_desc('T1132_data_encoding - Data Encoding', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         external_remote_service_access(Host),         valid_account(User, Host) ), rule_desc('T1133_external_remote_services - External Remote Services', 1.0) ).
interaction_rule( ( compromise(Host, system) :-         privilege_escalation(Host, User) ), rule_desc('T1134_access_token_manipulation - Access Token Manipulation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_share_discovery(Host) ), rule_desc('T1135_network_share_discovery - Network Share Discovery', 1.0) ).
interaction_rule( ( persistence(Host, create_account) :-         create_account(Host, _User) ), rule_desc('T1136_create_account - Create Account', 1.0) ).
interaction_rule( ( persistence(Host, 'Office Application Startup') :-         app_installed(Host, 'Microsoft Office', _Version),         os(Host, 'Windows'),         attacker_at(net),         connects(net, Host, _Port) ), rule_desc('T1137_office_application_startup - Office Application Startup', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, 'Office Application Startup') ), rule_desc('T1137_office_application_startup - Office Application Startup', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         app_shimming_possible(Host),         exec(Host, user) ), rule_desc('T1138_application_shimming - Application Shimming', 1.0) ).
interaction_rule( ( persistence(Host, app_shimming) :-         app_shimming_possible(Host) ), rule_desc('T1138_application_shimming - Application Shimming', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         bash_history_access(Host, User),         loot(Host, credentials, bash_history) ), rule_desc('T1139_bash_history - Bash History', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         deobfuscate_decode_files_or_information(Host) ), rule_desc('T1140_deobfuscate_decode_files_or_information - Deobfuscate/Decode Files or Information', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     fake_input_prompt(Host, User) ), rule_desc('T1141_input_prompt - Input Prompt', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         keychain_access(Host, User) ), rule_desc('T1142_keychain - Keychain', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hidden_window_exploit(Host) ), rule_desc('T1143_hidden_window - Hidden Window', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         gatekeeper_bypass(Host) ), rule_desc('T1144_gatekeeper_bypass - Gatekeeper Bypass', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     hidden_user_account(Host, User) ), rule_desc('T1147_hidden_users - Hidden Users', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         defense_disabled(Host, histcontrol) ), rule_desc('T1148_histcontrol - HISTCONTROL', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hijack_entry_point(Host) ), rule_desc('T1149_lc_main_hijacking - LC_MAIN Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, elevated_privileges) :-         plist_modification(Host) ), rule_desc('T1150_plist_modification - Plist Modification', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, macOS),         app_installed(Host, launchctl, _Version),         perm(Host, User, admin) ), rule_desc('T1152_launchctl - Launchctl', 1.0) ).
interaction_rule( ( persistence(Host, launchctl) :-         exec(Host, User) ), rule_desc('T1152_launchctl - Launchctl', 1.0) ).
interaction_rule( ( persistence(Host, 'trap_command') :-         trap_execution(Host, User) ), rule_desc('T1154_trap - Trap', 1.0) ).
interaction_rule( ( persistence(Host, 'malicious_shell_modification') :-         attacker_at(Network),         connects(Network, Host, Port),         os(Host, 'Linux'),         compromised_device(Host) ), rule_desc('T1156_malicious_shell_modification - Malicious Shell Modification', 1.0) ).
interaction_rule( ( persistence(Host, 'malicious_shell_modification') :-         attacker_at(Network),         connects(Network, Host, Port),         os(Host, 'macOS'),         compromised_device(Host) ), rule_desc('T1156_malicious_shell_modification - Malicious Shell Modification', 1.0) ).
interaction_rule( ( persistence(Host, dylib_hijacking) :-         dylib_hijacking_possible(Host) ), rule_desc('T1157_dylib_hijacking - Dylib Hijacking', 1.0) ).
interaction_rule( ( persistence(Host, hidden_files_and_directories) :-     hidden_files_and_directories(Host) ), rule_desc('T1158_hidden_files_and_directories - Hidden Files and Directories', 1.0) ).
interaction_rule( ( persistence(Host, launch_agent) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, macos),         user_on_host(User, Host),         exec(Host, User),         perm(Host, User, user) ), rule_desc('T1159_launch_agent - Launch Agent', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, launch_agent) ), rule_desc('T1159_launch_agent - Launch Agent', 1.0) ).
interaction_rule( ( persistence(Host, launch_daemon) :-         launch_daemon_persistence(Host) ), rule_desc('T1160_launch_daemon - Launch Daemon', 1.0) ).
interaction_rule( ( priv_escalation(Host, admin, root) :-         launch_daemon_persistence(Host) ), rule_desc('T1160_launch_daemon - Launch Daemon', 1.0) ).
interaction_rule( ( persistence(Host, 'LC_LOAD_DYLIB Addition') :-         attacker_at(Network),         connects(Network, Host, Port),         os(Host, 'macOS'),         compromised_device(Host) ), rule_desc('T1161_lc_load_dylib_addition - LC_LOAD_DYLIB Addition', 1.0) ).
interaction_rule( ( persistence(Host, 'login_item') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, 'macOS'),         user_on_host(User, Host),         exec(Host, User) ), rule_desc('T1162_login_item - Login Item', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, 'login_item') ), rule_desc('T1162_login_item - Login Item', 1.0) ).
interaction_rule( ( persistence(Host, 'reopened_applications') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, 'macOS'),         file_modified(Host, '~/Library/Preferences/com.apple.loginwindow.plist'),         file_modified(Host, '~/Library/Preferences/ByHost/com.apple.loginwindow.*.plist') ), rule_desc('T1164_re-opened_applications - Re-opened Applications', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     persistence(Host, 'reopened_applications') ), rule_desc('T1164_re-opened_applications - Re-opened Applications', 1.0) ).
interaction_rule( ( persistence(Host, startup_items) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, macos),         exec(Host, root) ), rule_desc('T1165_startup_items - Startup Items', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, root) :-         persistence(Host, startup_items) ), rule_desc('T1165_startup_items - Startup Items', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, macOS),         perm(Host, User, root),         user_on_host(User, Host) ), rule_desc('T1167_securityd_memory - Securityd Memory', 1.0) ).
interaction_rule( ( loot(Host, keychain_passwords, T1167) :-         compromise(Host, root),         os(Host, macOS) ), rule_desc('T1167_securityd_memory - Securityd Memory', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         job_scheduling_persistence(Host) ), rule_desc('T1168_local_job_scheduling - Local Job Scheduling', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         llmnr_nbt_ns_poisoning_and_relay(Host),         auth_possible(Host, User, ntlmv2),         accepts_hash(Host, User) ), rule_desc('T1171_llmnr_nbt-ns_poisoning_and_relay - LLMNR/NBT-NS Poisoning and Relay', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         domain_fronting_possible(Host) ), rule_desc('T1172_domain_fronting - Domain Fronting', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         dde_execution_possible(Host) ), rule_desc('T1173_dynamic_data_exchange - Dynamic Data Exchange', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_password_filter_installed(Host) ), rule_desc('T1174_password_filter_dll - Password Filter DLL', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec_possible_remote(Host, user) ), rule_desc('T1175_component_object_model_and_distributed_com - Component Object Model and Distributed COM', 1.0) ).
interaction_rule( ( persistence(Host, 'software_extension') :-         malicious_extension_installed(Host) ), rule_desc('T1176_software_extensions - Software Extensions', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         execCode(Host, priv) ), rule_desc('T1177_lsass_driver - LSASS Driver', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hooking_detected(Host) ), rule_desc('T1179_hooking - Hooking', 1.0) ).
interaction_rule( ( persistence(Host, screensaver) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, windows),         setting(Host, 'HKCU\\Control Panel\\Desktop\\SCRNSAVE.exe', MaliciousPEPath),         setting(Host, 'HKCU\\Control Panel\\Desktop\\ScreenSaveActive', '1'),         setting(Host, 'HKCU\\Control Panel\\Desktop\\ScreenSaverIsSecure', '0'),         setting(Host, 'HKCU\\Control Panel\\Desktop\\ScreenSaveTimeout', _Timeout) ), rule_desc('T1180_screensaver - Screensaver', 1.0) ).
interaction_rule( ( compromise(Host, elevated_privileges) :-         ewm_injection_possible(Host) ), rule_desc('T1181_extra_window_memory_injection - Extra Window Memory Injection', 1.0) ).
interaction_rule( ( persistence(Host, 'AppCertDLLs') :-         setting(Host, 'HKEY_LOCAL_MACHINE\\System\\CurrentControlSet\\Control\\Session Manager\\AppCertDLLs', DLL),         malicious_dll(DLL) ), rule_desc('T1182_appcert_dlls - AppCert DLLs', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         persistence(Host, 'AppCertDLLs'),         exec(Host, user) ), rule_desc('T1182_appcert_dlls - AppCert DLLs', 1.0) ).
interaction_rule( ( persistence(Host, "IFEO") :-         ifeo_injection(Host) ), rule_desc('T1183_image_file_execution_options_injection - Image File Execution Options Injection', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         ifeo_injection(Host) ), rule_desc('T1183_image_file_execution_options_injection - Image File Execution Options Injection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         browser_session_hijacking(Host) ), rule_desc('T1185_browser_session_hijacking - Browser Session Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         process_doppelgaenging(Host) ), rule_desc('T1186_process_doppelgänging - Process Doppelgänging', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         multi_hop_proxy_detected(Host) ), rule_desc('T1188_multi-hop_proxy - Multi-hop Proxy', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remote_service_vulnerable(Host, _Port, _CVE, _Service) ), rule_desc('T1190_exploit_public-facing_application - Exploit Public-Facing Application', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         cmstp_exploit(Host) ), rule_desc('T1191_cmstp - CMSTP', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         spearphishing_link(User, Host) ), rule_desc('T1192_spearphishing_link - Spearphishing Link', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         spearphishing_attachment(Host, User) ), rule_desc('T1193_spearphishing_attachment - Spearphishing Attachment', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         spearphishing_via_service(Host, User) ), rule_desc('T1194_spearphishing_via_service - Spearphishing via Service', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         supply_chain_compromise(Host) ), rule_desc('T1195_supply_chain_compromise - Supply Chain Compromise', 1.0) ).
interaction_rule( ( persistence(Host, bits_job) :-         bits_job_abuse(Host) ), rule_desc('T1197_bits_jobs - BITS Jobs', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         bits_job_abuse(Host) ), rule_desc('T1197_bits_jobs - BITS Jobs', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         trusted_relationship_access(Host) ), rule_desc('T1199_trusted_relationship - Trusted Relationship', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     hardware_addition_access(Host) ), rule_desc('T1200_hardware_additions - Hardware Additions', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         password_policy_discovery(Host) ), rule_desc('T1201_password_policy_discovery - Password Policy Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         indirect_command_execution(Host) ), rule_desc('T1202_indirect_command_execution - Indirect Command Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec_possible_remote(Host, user) ), rule_desc('T1203_exploitation_for_client_execution - Exploitation for Client Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     traffic_signaling_detected(Host) ), rule_desc('T1205_traffic_signaling - Traffic Signaling', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, root) :-         sudo_caching_vulnerable(Host),         attacker_at(net),         connects(net, Host, _Port) ), rule_desc('T1206_sudo_caching - Sudo Caching', 1.0) ).
interaction_rule( ( compromise(Host, domain) :-         rogue_domain_controller(Host) ), rule_desc('T1207_rogue_domain_controller - Rogue Domain Controller', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         kerberoasting_possible(Host, User) ), rule_desc('T1208_kerberoasting - Kerberoasting', 1.0) ).
interaction_rule( ( persistence(Host, 'T1209_Time_Providers') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, windows),         perm(Host, admin, _),         exec(Host, 'LocalService') ), rule_desc('T1209_time_providers - Time Providers', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec_possible_remote(Host, user) ), rule_desc('T1210_exploitation_of_remote_services - Exploitation of Remote Services', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_defense_evasion(Host) ), rule_desc('T1211_exploitation_for_defense_evasion - Exploitation for Defense Evasion', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_credential_access(Host, user) ), rule_desc('T1212_exploitation_for_credential_access - Exploitation for Credential Access', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         compromised_data(Host, _) ), rule_desc('T1213_data_from_information_repositories - Data from Information Repositories', 1.0) ).
interaction_rule( ( persistence(Host, kernel_module) :-         kernel_module_loaded(Host) ), rule_desc('T1215_kernel_modules_and_extensions - Kernel Modules and Extensions', 1.0) ).
interaction_rule( ( persistence(Host, kernel_extension) :-         kernel_extension_loaded(Host) ), rule_desc('T1215_kernel_modules_and_extensions - Kernel Modules and Extensions', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         proxy_execution_possible(Host) ), rule_desc('T1216_system_script_proxy_execution - System Script Proxy Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         browser_info_discovery(Host) ), rule_desc('T1217_browser_information_discovery - Browser Information Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         proxy_execution_possible(Host) ), rule_desc('T1218_system_binary_proxy_execution - System Binary Proxy Execution', 1.0) ).
interaction_rule( ( c2_channel(Host, protocol) :-         remote_access_tool_installed(Host),         attacker_at(Network),         connects(Network, Host, Port) ), rule_desc('T1219_remote_access_tools - Remote Access Tools', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, protocol) ), rule_desc('T1219_remote_access_tools - Remote Access Tools', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         potential_xsl_script_processing(Host) ), rule_desc('T1220_xsl_script_processing - XSL Script Processing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         template_injection_possible(Host) ), rule_desc('T1221_template_injection - Template Injection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         file_permission_modified(Host, _File) ), rule_desc('T1222_file_and_directory_permissions_modification - File and Directory Permissions Modification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         potential_t1223(Host),         exec_possible_user(Host, user) ), rule_desc('T1223_compiled_html_file - Compiled HTML File', 1.0) ).
interaction_rule( ( persistence(Host, 'boot_or_logon_initialization_scripts') :-         attacker_at(Network),         connects(Network, Host, Port),         (rooted(Host) ; jailbroken(Host)) ), rule_desc('T1398_boot_or_logon_initialization_scripts - Boot or Logon Initialization Scripts', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, 'boot_or_logon_initialization_scripts') ), rule_desc('T1398_boot_or_logon_initialization_scripts - Boot or Logon Initialization Scripts', 1.0) ).
interaction_rule( ( persistence(Host, system_partition) :-         modify_system_partition(Host) ), rule_desc('T1400_modify_system_partition - Modify System Partition', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         device_admin_privilege_escalation(Host) ), rule_desc('T1401_device_administrator_permissions - Device Administrator Permissions', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     broadcast_receiver_malware(Host) ), rule_desc('T1402_broadcast_receivers - Broadcast Receivers', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_tee_vulnerability(Host) ), rule_desc('T1405_exploit_tee_vulnerability - Exploit TEE Vulnerability', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         obfuscated_payload_detected(Host) ), rule_desc('T1406_obfuscated_files_or_information - Obfuscated Files or Information', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         dynamic_code_downloaded(Host) ), rule_desc('T1407_download_new_code_at_runtime - Download New Code at Runtime', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         disguise_root_jailbreak_indicators(Host) ), rule_desc('T1408_disguise_root_jailbreak_indicators - Disguise Root/Jailbreak Indicators', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         stored_app_data_accessible(Host) ), rule_desc('T1409_stored_application_data - Stored Application Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_traffic_capture_or_redirection(Host) ), rule_desc('T1410_network_traffic_capture_or_redirection - Network Traffic Capture or Redirection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_prompt(Host) ), rule_desc('T1411_input_prompt - Input Prompt', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         access_sensitive_data_in_device_logs(Host) ), rule_desc('T1413_access_sensitive_data_in_device_logs - Access Sensitive Data in Device Logs', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         clipboard_data_access(Host, _App) ), rule_desc('T1414_clipboard_data - Clipboard Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         uri_hijacking_possible(Host, Uri),         has_data(Host, oauth_tokens),         exfiltrated(Host, oauth_tokens, MaliciousApp) ), rule_desc('T1416_uri_hijacking - URI Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         input_capture(Host) ), rule_desc('T1417_input_capture - Input Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         software_discovery(Host) ), rule_desc('T1418_software_discovery - Software Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     network_discovery(Host) ), rule_desc('T1421_system_network_connections_discovery - System Network Connections Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_config_discovery(Host) ), rule_desc('T1422_system_network_configuration_discovery - System Network Configuration Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         network_service_scanning(Host) ), rule_desc('T1423_network_service_scanning - Network Service Scanning', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         process_discovery(Host) ), rule_desc('T1424_process_discovery - Process Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         insecure_third_party_library(Host, _, _) ), rule_desc('T1425_insecure_third-party_libraries - Insecure Third-Party Libraries', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         system_info_discovery(Host) ), rule_desc('T1426_system_information_discovery - System Information Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         compromised_device(Device),         connected_removable_media(Device, Host) ), rule_desc('T1427_attack_pc_via_usb_connection - Attack PC via USB Connection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         audio_capture_possible(Host) ), rule_desc('T1429_audio_capture - Audio Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         location_tracking(Host) ), rule_desc('T1430_location_tracking - Location Tracking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     malicious_app_installed(Host, App) ), rule_desc('T1431_app_delivered_via_web_download - App Delivered via Web Download', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         access_contact_list(Host) ), rule_desc('T1432_access_contact_list - Access Contact List', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_app_installed(Host, _App) ), rule_desc('T1434_app_delivered_via_email_attachment - App Delivered via Email Attachment', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         access_calendar_entries(Host) ), rule_desc('T1435_access_calendar_entries - Access Calendar Entries', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     uses_common_port(Host, _Port) ), rule_desc('T1436_commonly_used_port - Commonly Used Port', 1.0) ).
interaction_rule( ( c2_channel(Host, Protocol) :-         attacker_at(Network),         connects(Network, Host, Port),         service(Host, Port, Protocol, _, _),         protocol_in_use(Protocol) ), rule_desc('T1437_application_layer_protocol - Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, _) ), rule_desc('T1437_application_layer_protocol - Application Layer Protocol', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         detect_app_analysis_env(Host) ), rule_desc('T1440_detect_app_analysis_environment - Detect App Analysis Environment', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         fake_developer_account_compromise(Host) ), rule_desc('T1442_fake_developer_accounts - Fake Developer Accounts', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         remotely_install_app(Host) ), rule_desc('T1443_remotely_install_application - Remotely Install Application', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_app_installed(Host, App),         mobile_perm(App, accessibility_service) ), rule_desc('T1444_masquerade_as_legitimate_application - Masquerade as Legitimate Application', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         malicious_app_installed(Device, _App) ), rule_desc('T1445_abuse_of_ios_enterprise_app_signing_key - Abuse of iOS Enterprise App Signing Key', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         device_lockout(Host) ), rule_desc('T1446_device_lockout - Device Lockout', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         delete_device_data(Host) ), rule_desc('T1447_delete_device_data - Delete Device Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         carrier_billing_fraud(Host) ), rule_desc('T1448_carrier_billing_fraud - Carrier Billing Fraud', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         exploit_ss7_tracking(Device) ), rule_desc('T1450_exploit_ss7_to_track_device_location - Exploit SS7 to Track Device Location', 1.0) ).
interaction_rule( ( compromise(User, mobile_data) :-     sim_card_swap_attack(User, MobileCarrier),     receives_mobile_data(MobileCarrier, User) ), rule_desc('T1451_sim_card_swap - SIM Card Swap', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_app_installed(Host, App),         mobile_perm(App, accessibility_features) ), rule_desc('T1453_abuse_accessibility_features - Abuse Accessibility Features', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         vulnerable_baseband(Host) ), rule_desc('T1455_exploit_baseband_vulnerability - Exploit Baseband Vulnerability', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         potential_drive_by_compromise(Host),         exec(Host, user) ), rule_desc('T1456_drive-by_compromise - Drive-By Compromise', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_media_content(Host) ), rule_desc('T1457_malicious_media_content - Malicious Media Content', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         removable_media_infection(Host) ), rule_desc('T1458_replication_through_removable_media - Replication Through Removable Media', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         device_unlock_code_guessing(Host) ), rule_desc('T1459_device_unlock_code_guessing_or_brute_force - Device Unlock Code Guessing or Brute Force', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     biometric_spoofing_possible(Host) ), rule_desc('T1460_biometric_spoofing - Biometric Spoofing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         lockscreen_bypass(Host) ), rule_desc('T1461_lockscreen_bypass - Lockscreen Bypass', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_software_development_tools(Host) ), rule_desc('T1462_malicious_software_development_tools - Malicious Software Development Tools', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         manipulate_device_communication(Host) ), rule_desc('T1463_manipulate_device_communication - Manipulate Device Communication', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         rogue_wifi_access_point(Host) ), rule_desc('T1465_rogue_wi-fi_access_points - Rogue Wi-Fi Access Points', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         downgrade_to_insecure_protocols(Host) ), rule_desc('T1466_downgrade_to_insecure_protocols - Downgrade to Insecure Protocols', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         rogue_base_station_detected(Device) ), rule_desc('T1467_rogue_cellular_base_station - Rogue Cellular Base Station', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         unauthorized_wipe(Device) ), rule_desc('T1469_remotely_wipe_data_without_authorization - Remotely Wipe Data Without Authorization', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_encrypted_for_impact(Host) ), rule_desc('T1471_data_encrypted_for_impact - Data Encrypted for Impact', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         fraudulent_ad_revenue(Host) ), rule_desc('T1472_generate_fraudulent_advertising_revenue - Generate Fraudulent Advertising Revenue', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         supply_chain_compromise(Host) ), rule_desc('T1474_supply_chain_compromise - Supply Chain Compromise', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         malicious_app_installed(Device, _App) ), rule_desc('T1475_deliver_malicious_app_via_authorized_app_store - Deliver Malicious App via Authorized App Store', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         malicious_app_installed(Device, _App) ), rule_desc('T1476_deliver_malicious_app_via_other_means - Deliver Malicious App via Other Means', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_via_radio_interface(Host) ), rule_desc('T1477_exploit_via_radio_interfaces - Exploit via Radio Interfaces', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     execution_guardrails(Host) ), rule_desc('T1480_execution_guardrails - Execution Guardrails', 1.0) ).
interaction_rule( ( c2_channel(Host, web_service) :-         attacker_at(net),         connects(net, Host, _Port),         encrypted_traffic(Host, ssl_tls) ), rule_desc('T1481_web_service - Web Service', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, web_service) ), rule_desc('T1481_web_service - Web Service', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         domain_trust_discovery(Host) ), rule_desc('T1482_domain_trust_discovery - Domain Trust Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     dga_c2_channel(Host) ), rule_desc('T1483_domain_generation_algorithms - Domain Generation Algorithms', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         domain_policy_modified(Host) ), rule_desc('T1484_domain_or_tenant_policy_modification - Domain or Tenant Policy Modification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_encrypted_for_impact(Host) ), rule_desc('T1486_data_encrypted_for_impact - Data Encrypted for Impact', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         disk_content_wipe(Host) ), rule_desc('T1488_disk_content_wipe - Disk Content Wipe', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     service_stopped(Host, _Service) ), rule_desc('T1489_service_stop - Service Stop', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         defacement_possible(Host) ), rule_desc('T1491_defacement - Defacement', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_manipulation(Host) ), rule_desc('T1492_stored_data_manipulation - Stored Data Manipulation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_manipulation(Host) ), rule_desc('T1493_transmitted_data_manipulation - Transmitted Data Manipulation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         runtime_data_manipulation(Host) ), rule_desc('T1494_runtime_data_manipulation - Runtime Data Manipulation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         resource_hijacking(Host) ), rule_desc('T1496_resource_hijacking - Resource Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         analysis_env_detected(Host) ), rule_desc('T1497_virtualization_sandbox_evasion - Virtualization/Sandbox Evasion', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     compile_after_delivery(Host) ), rule_desc('T1500_compile_after_delivery - Compile After Delivery', 1.0) ).
interaction_rule( ( persistence(Host, systemd_service) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, linux),         (perm(Host, root, _); perm(Host, user, _)) ), rule_desc('T1501_systemd_service - Systemd Service', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, systemd_service) ), rule_desc('T1501_systemd_service - Systemd Service', 1.0) ).
interaction_rule( ( compromise(Host, system) :-         ppid_spoofing_possible(Host),         priv_escalation(Host, admin, system) ), rule_desc('T1502_parent_pid_spoofing - Parent PID Spoofing', 1.0) ).
interaction_rule( ( persistence(Host, 'PowerShellProfile') :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, 'Windows'),         app_installed(Host, 'PowerShell', _Version) ), rule_desc('T1504_powershell_profile - PowerShell Profile', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, admin) :-         persistence(Host, 'PowerShellProfile'),         exec(Host, admin) ), rule_desc('T1504_powershell_profile - PowerShell Profile', 1.0) ).
interaction_rule( ( persistence(Host, 'Server Software Component') :-         malicious_component_installed(Host) ), rule_desc('T1505_server_software_component - Server Software Component', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         session_cookie_stolen(Host, user) ), rule_desc('T1506_web_session_cookie - Web Session Cookie', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         network_info_discovery(Device) ), rule_desc('T1507_network_information_discovery - Network Information Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         malicious_app_installed(Host, _App) ), rule_desc('T1508_suppress_application_icon - Suppress Application Icon', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         non_standard_port_usage(Host, Port) ), rule_desc('T1509_non-standard_port - Non-Standard Port', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         clipboard_modification(Host) ), rule_desc('T1510_clipboard_modification - Clipboard Modification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         video_capture_possible(Host),         (jailbroken(Host); rooted(Host)) ), rule_desc('T1512_video_capture - Video Capture', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         screen_capture_possible(Host) ), rule_desc('T1513_screen_capture - Screen Capture', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, root) :-         attacker_at(net),         connects(net, Host, _Port),         os(Host, macOS),         user_on_host(User, Host),         exec_possible_user(Host, User),         auth_usable_cred(User, Host, prompt),         app_installed(Host, 'AuthorizationExecuteWithPrivileges', _Version) ), rule_desc('T1514_elevated_execution_with_prompt - Elevated Execution with Prompt', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         priv_escalation(Host, user, root) ), rule_desc('T1514_elevated_execution_with_prompt - Elevated Execution with Prompt', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         input_injection_possible(Host) ), rule_desc('T1516_input_injection - Input Injection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         notification_data_collected(Host) ), rule_desc('T1517_access_notifications - Access Notifications', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         software_discovery(Host),         local_pe_vulnerable(Host, CVE) ), rule_desc('T1518_software_discovery - Software Discovery', 1.0) ).
interaction_rule( ( persistence(Host, emond) :-         app_installed(Host, emond, _Version),         os(Host, macos),         attacker_at(net),         connects(net, Host, _Port) ), rule_desc('T1519_emond - Emond', 1.0) ).
interaction_rule( ( priv_escalation(Host, admin, root) :-         persistence(Host, emond) ), rule_desc('T1519_emond - Emond', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         dga_used_for_c2(Host) ), rule_desc('T1520_domain_generation_algorithms - Domain Generation Algorithms', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         encrypted_traffic(Host, Protocol) ), rule_desc('T1521_encrypted_channel - Encrypted Channel', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         metadata_access(Host, provider),         cloud_cred(User, type) ), rule_desc('T1522_cloud_instance_metadata_api - Cloud Instance Metadata API', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         cloud_service_discovery(Host) ), rule_desc('T1526_cloud_service_discovery - Cloud Service Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     compromised_access_token(User, Token),     user_on_host(User, Host),     auth_usable_cred(User, Host, oauth_token) ), rule_desc('T1527_application_access_token - Application Access Token', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         cloud_data_accessible(Host) ), rule_desc('T1530_data_from_cloud_storage - Data from Cloud Storage', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_from_local_system(Host) ), rule_desc('T1533_data_from_local_system - Data from Local System', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         internal_spearphishing(Host, User) ), rule_desc('T1534_internal_spearphishing - Internal Spearphishing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     unused_cloud_region_exploitation(Host, _Region) ), rule_desc('T1535_unused_unsupported_cloud_regions - Unused/Unsupported Cloud Regions', 1.0) ).
interaction_rule( ( exfiltrated(Host, data, cloud_account) :-         attacker_at(net),         connects(net, Host, _Port),         cloud_account(AttackerAccount, Provider),         has_data(Host, data),         cloud_cred(AttackerAccount, _Type) ), rule_desc('T1537_transfer_data_to_cloud_account - Transfer Data to Cloud Account', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         cloud_dashboard_access(Host, User) ), rule_desc('T1538_cloud_service_dashboard - Cloud Service Dashboard', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         steal_web_session_cookie(Host, User) ), rule_desc('T1539_steal_web_session_cookie - Steal Web Session Cookie', 1.0) ).
interaction_rule( ( compromise(Host, elevated_privileges) :-         code_injection_possible(Host),         exec(Host, elevated_privileges) ), rule_desc('T1540_code_injection - Code Injection', 1.0) ).
interaction_rule( ( persistence(Host, 'Foreground Persistence') :-         malicious_app_installed(Host, App),         mobile_perm(App, foreground_service) ), rule_desc('T1541_foreground_persistence - Foreground Persistence', 1.0) ).
interaction_rule( ( persistence(Host, pre_os_boot) :-         attacker_at(net),         connects(net, Host, _Port),         firmware_accessible(Host) ), rule_desc('T1542_pre-os_boot - Pre-OS Boot', 1.0) ).
interaction_rule( ( compromise(Host, kernel) :-         persistence(Host, pre_os_boot) ), rule_desc('T1542_pre-os_boot - Pre-OS Boot', 1.0) ).
interaction_rule( ( persistence(Host, system_process) :-         attacker_at(net),         connects(net, Host, _Port),         exec(Host, admin),         perm(Host, admin, root),         execCode(Host, root) ), rule_desc('T1543_create_or_modify_system_process - Create or Modify System Process', 1.0) ).
interaction_rule( ( priv_escalation(Host, admin, root) :-         persistence(Host, system_process) ), rule_desc('T1543_create_or_modify_system_process - Create or Modify System Process', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         ingress_tool_transfer(Host) ), rule_desc('T1544_ingress_tool_transfer - Ingress Tool Transfer', 1.0) ).
interaction_rule( ( priv_escalation(Host, user, root) :-         attacker_at(net),         connects(net, Host, _Port),         defense_disabled(Host, elevation_control) ), rule_desc('T1548_abuse_elevation_control_mechanism - Abuse Elevation Control Mechanism', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         priv_escalation(Host, user, root) ), rule_desc('T1548_abuse_elevation_control_mechanism - Abuse Elevation Control Mechanism', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         stolen_auth_material(User, Host) ), rule_desc('T1550_use_alternate_authentication_material - Use Alternate Authentication Material', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         unsecured_credentials(Host, User) ), rule_desc('T1552_unsecured_credentials - Unsecured Credentials', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         subvert_trust_controls(Host) ), rule_desc('T1553_subvert_trust_controls - Subvert Trust Controls', 1.0) ).
interaction_rule( ( persistence(Host, 'Compromise Host Software Binary') :-         compromised_binary(Host, _Binary) ), rule_desc('T1554_compromise_host_software_binary - Compromise Host Software Binary', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         credentials_from_password_stores(Host, User) ), rule_desc('T1555_credentials_from_password_stores - Credentials from Password Stores', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modified_auth_process(Host) ), rule_desc('T1556_modify_authentication_process - Modify Authentication Process', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         kerberos_ticket_stolen_or_forged(Host, User) ), rule_desc('T1558_steal_or_forge_kerberos_tickets - Steal or Forge Kerberos Tickets', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         ipc_abuse(Host) ), rule_desc('T1559_inter-process_communication - Inter-Process Communication', 1.0) ).
interaction_rule( ( exfiltrated(Host, DataKind, Channel) :-         archive_collected_data(Host),         c2_channel(Host, Channel) ), rule_desc('T1560_archive_collected_data - Archive Collected Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         disk_wipe(Host) ), rule_desc('T1561_disk_wipe - Disk Wipe', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         session_hijack_possible(Host, User) ), rule_desc('T1563_remote_service_session_hijacking - Remote Service Session Hijacking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_manipulation(Host) ), rule_desc('T1565_data_manipulation - Data Manipulation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         phishing_attempt(User, Host) ), rule_desc('T1566_phishing - Phishing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         dynamic_resolution(Host) ), rule_desc('T1568_dynamic_resolution - Dynamic Resolution', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(net),         connects(net, Host, _Port),         service(Host, _Port, _Proto, Service, _Version),         user_on_host(User, Host),         compromise_from_exec(Host, user) ), rule_desc('T1569_system_services - System Services', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         lateral_tool_transfer(Host) ), rule_desc('T1570_lateral_tool_transfer - Lateral Tool Transfer', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         non_standard_port_usage(Host, _, _) ), rule_desc('T1571_non-standard_port - Non-Standard Port', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         protocol_tunneling(Host) ), rule_desc('T1572_protocol_tunneling - Protocol Tunneling', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         encrypted_c2_channel(Host) ), rule_desc('T1573_encrypted_channel - Encrypted Channel', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hijack_execution_flow(Host) ), rule_desc('T1574_hijack_execution_flow - Hijack Execution Flow', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         native_api_execution(Host) ), rule_desc('T1575_native_api - Native API', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         modify_cloud_infrastructure(Host) ), rule_desc('T1578_modify_cloud_compute_infrastructure - Modify Cloud Compute Infrastructure', 1.0) ).
interaction_rule( ( loot(Host, keychain, T1579) :-         compromised_device(Host),         has_data(Host, keychain) ), rule_desc('T1579_keychain - Keychain', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         geofencing_exploit(Host) ), rule_desc('T1581_geofencing - Geofencing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         sms_control_attack(Host) ), rule_desc('T1582_sms_control - SMS Control', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         infrastructure_acquired(Host) ), rule_desc('T1583_acquire_infrastructure - Acquire Infrastructure', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         compromised_infrastructure(Host) ), rule_desc('T1584_compromise_infrastructure - Compromise Infrastructure', 1.0) ).
interaction_rule( ( compromise(Service, user) :-         establish_account(User, Service) ), rule_desc('T1585_establish_accounts - Establish Accounts', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         gather_victim_identity_info(Host) ), rule_desc('T1589_gather_victim_identity_information - Gather Victim Identity Information', 1.0) ).
interaction_rule( ( compromise(Victim, user) :-     gather_victim_org_info(_, Victim) ), rule_desc('T1593_search_open_websites_domains - Search Open Websites/Domains', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         gather_victim_org_info(Host, _) ), rule_desc('T1594_search_victim-owned_websites - Search Victim-Owned Websites', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     phishing_for_information(User),     user_on_host(User, Host) ), rule_desc('T1598_phishing_for_information - Phishing for Information', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         weakened_defense(Host) ), rule_desc('T1600_weaken_encryption - Weaken Encryption', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_from_config_repo(Host) ), rule_desc('T1602_data_from_configuration_repository - Data from Configuration Repository', 1.0) ).
interaction_rule( ( exec(Host, user) :-         attacker_at(net),         connects(net, Host, _Port),         (rooted(Host); jailbroken(Host)),         user_on_host(user, Host),         perm(Host, user, root) ), rule_desc('T1605_command-line_interface - Command-Line Interface', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         exec(Host, user) ), rule_desc('T1605_command-line_interface - Command-Line Interface', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         forged_web_credential(User, Host) ), rule_desc('T1606_forge_web_credentials - Forge Web Credentials', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         container_admin_command_exec(Host) ), rule_desc('T1609_container_administration_command - Container Administration Command', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         deploy_container(Host) ), rule_desc('T1610_deploy_container - Deploy Container', 1.0) ).
interaction_rule( ( compromise(Host, root) :-         escape_to_host(Host) ), rule_desc('T1611_escape_to_host - Escape to Host', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         build_image_on_host(Host) ), rule_desc('T1612_build_image_on_host - Build Image on Host', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         container_discovery(Host) ), rule_desc('T1613_container_and_resource_discovery - Container and Resource Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         group_policy_discovery(Host) ), rule_desc('T1615_group_policy_discovery - Group Policy Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         call_control_compromise(Host) ), rule_desc('T1616_call_control - Call Control', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hooking_attack(Host) ), rule_desc('T1617_hooking - Hooking', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         user_evasion(Host) ), rule_desc('T1618_user_evasion - User Evasion', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         cloud_storage_object_discovery(Host, Account) ), rule_desc('T1619_cloud_storage_object_discovery - Cloud Storage Object Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         reflective_code_loading(Host) ), rule_desc('T1620_reflective_code_loading - Reflective Code Loading', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     mfa_request_generation_attack(Host, User) ), rule_desc('T1621_multi-factor_authentication_request_generation - Multi-Factor Authentication Request Generation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         debugger_evasion_detected(Host) ), rule_desc('T1622_debugger_evasion - Debugger Evasion', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         event_triggered_execution(Host) ), rule_desc('T1624_event_triggered_execution - Event Triggered Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hijack_execution_flow(Host) ), rule_desc('T1625_hijack_execution_flow - Hijack Execution Flow', 1.0) ).
interaction_rule( ( priv_escalation(Host, low, high) :-         attacker_at(net),         connects(net, Host, _Port),         defense_disabled(Host, elevation_control),         user_on_host(User, Host),         perm(Host, User, low) ), rule_desc('T1626_abuse_elevation_control_mechanism - Abuse Elevation Control Mechanism', 1.0) ).
interaction_rule( ( compromise(Host, high) :-         priv_escalation(Host, low, high) ), rule_desc('T1626_abuse_elevation_control_mechanism - Abuse Elevation Control Mechanism', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     execution_guardrails(Host) ), rule_desc('T1627_execution_guardrails - Execution Guardrails', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hide_artifacts(Host) ), rule_desc('T1628_hide_artifacts - Hide Artifacts', 1.0) ).
interaction_rule( ( compromise(Host, elevated_privileges) :-         process_injection_possible(Host) ), rule_desc('T1631_process_injection - Process Injection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         subvert_trust_controls(Host) ), rule_desc('T1632_subvert_trust_controls - Subvert Trust Controls', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         credentials_from_password_store(Host, User) ), rule_desc('T1634_credentials_from_password_store - Credentials from Password Store', 1.0) ).
interaction_rule( ( compromise(Host, user) :-     steal_application_access_token(User, Host) ), rule_desc('T1635_steal_application_access_token - Steal Application Access Token', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         access_protected_user_data(Host) ), rule_desc('T1636_protected_user_data - Protected User Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         dynamic_resolution(Host) ), rule_desc('T1637_dynamic_resolution - Dynamic Resolution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         adversary_in_the_middle(Host) ), rule_desc('T1638_adversary-in-the-middle - Adversary-in-the-Middle', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         account_access_removal(Host, User) ), rule_desc('T1640_account_access_removal - Account Access Removal', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         data_manipulation(Host) ), rule_desc('T1641_data_manipulation - Data Manipulation', 1.0) ).
interaction_rule( ( c2_channel(Host, Channel) :-         compromised_device(Host),         out_of_band_channel(Channel),         mobile_perm(App, notification_access),         app_installed(Host, App, _Version) ), rule_desc('T1644_out_of_band_data - Out of Band Data', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         plist_file_modified(Host) ), rule_desc('T1647_plist_file_modification - Plist File Modification', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         serverless_execution(Host, user) ), rule_desc('T1648_serverless_execution - Serverless Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         steal_or_forge_certificates(Host, User) ), rule_desc('T1649_steal_or_forge_authentication_certificates - Steal or Forge Authentication Certificates', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         acquired_access(Host) ), rule_desc('T1650_acquire_access - Acquire Access', 1.0) ).
interaction_rule( ( exec(Host, User) :-         attacker_at(Network),         connects(Network, Host, Port),         cloud_account(User, Provider),         cloud_cred(User, admin),         perm(Host, User, admin),         exec_possible_remote(Host, User) ), rule_desc('T1651_cloud_administration_command - Cloud Administration Command', 1.0) ).
interaction_rule( ( compromise(Host, admin) :-         exec(Host, User) ), rule_desc('T1651_cloud_administration_command - Cloud Administration Command', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         device_driver_discovery(Host) ), rule_desc('T1652_device_driver_discovery - Device Driver Discovery', 1.0) ).
interaction_rule( ( persistence(Host, power_settings) :-         attacker_at(net),         connects(net, Host, _Port),         setting(Host, powercfg, _Value) ), rule_desc('T1653_power_settings - Power Settings', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, power_settings) ), rule_desc('T1653_power_settings - Power Settings', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         log_enumeration(Host) ), rule_desc('T1654_log_enumeration - Log Enumeration', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         masquerading_detected(Host) ), rule_desc('T1655_masquerading - Masquerading', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         impersonation_attack(User, Host) ), rule_desc('T1656_impersonation - Impersonation', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         user_phishes_success(User),         user_on_host(User, Host) ), rule_desc('T1657_financial_theft - Financial Theft', 1.0) ).
interaction_rule( ( exfiltrated(Host, financial_information, Channel) :-         c2_channel(Host, Channel) ), rule_desc('T1657_financial_theft - Financial Theft', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exec_possible_remote(Host, user) ), rule_desc('T1658_exploitation_for_client_execution - Exploitation for Client Execution', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         content_injection_possible(Host) ), rule_desc('T1659_content_injection - Content Injection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         phishing_attempt(Host, User),         user_phishes_success(User) ), rule_desc('T1660_phishing - Phishing', 1.0) ).
interaction_rule( ( compromise(Device, user) :-         malicious_app_installed(Device, App) ), rule_desc('T1661_application_versioning - Application Versioning', 1.0) ).
interaction_rule( ( c2_channel(Host, Protocol) :-         attacker_at(Network),         connects(Network, Host, Port),         app_installed(Host, App, Version),         member(App, ['VNC', 'TeamViewer', 'AirDroid', 'AirMirror']),         Protocol = 'remote_access' ), rule_desc('T1663_remote_access_software - Remote Access Software', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         c2_channel(Host, 'remote_access') ), rule_desc('T1663_remote_access_software - Remote Access Software', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         exploit_vulnerability(Host) ), rule_desc('T1664_exploitation_for_initial_access - Exploitation for Initial Access', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         hide_infrastructure(Host) ), rule_desc('T1665_hide_infrastructure - Hide Infrastructure', 1.0) ).
interaction_rule( ( compromise(Account, admin) :-         modify_cloud_hierarchy(Account, Provider) ), rule_desc('T1666_modify_cloud_resource_hierarchy - Modify Cloud Resource Hierarchy', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         wifi_access(Host) ), rule_desc('T1669_wi-fi_networks - Wi-Fi Networks', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         virtualization_solution_exploit(Host) ), rule_desc('T1670_virtualization_solution - Virtualization Solution', 1.0) ).
interaction_rule( ( persistence(Host, 'OAuthAppIntegration') :-         attacker_at(net),         connects(net, Host, _Port),         app_installed(Host, App, 'OAuth'),         grants_permission(App, User, 'high_privilege'),         cloud_account(User, Provider),         cloud_cred(User, 'service_principal') ), rule_desc('T1671_cloud_application_integration - Cloud Application Integration', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         persistence(Host, 'OAuthAppIntegration') ), rule_desc('T1671_cloud_application_integration - Cloud Application Integration', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         email_spoofing_possible(Host) ), rule_desc('T1672_email_spoofing - Email Spoofing', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         virtual_machine_discovery(Host) ), rule_desc('T1673_virtual_machine_discovery - Virtual Machine Discovery', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         input_injection(Host) ), rule_desc('T1674_input_injection - Input Injection', 1.0) ).
interaction_rule( ( compromise(Host, user) :-         esxi_admin_command_execution(Host) ), rule_desc('T1675_esxi_administration_command - ESXi Administration Command', 1.0) ).
