% NOTE: Autogenerated stub. Adjust predicates/logic to your KB.
% technique_id: T1028
% technique_name: Windows Remote Management
% domain: 
% tactic(s): execution;lateral-movement
% platforms: Windows
% url: https://attack.mitre.org/techniques/T1028
% is_subtechnique: no
% stix_id: attack-pattern--c3bce4f4-9795-46c6-976e-8676300bbc39
% description: Windows Remote Management (WinRM) is the name of both a Windows service and a protocol that allows a user to interact with a remote system (e.g., run an executable, modify the Registry, modify services). (Citation: Microsoft WinRM) It may be called with the <code>winrm</code> command or by any number of programs such as PowerShell. (Citation: Jacobsen 2014)
% generated_by: notebook_generate_rules

% ---- RULE STUB START (network/service exploit) ----
potential_t1028_remote(Host,Port) :-
    attacker_at(net),
    connects(net, Host, Port),
    service(Host, Port, Proto, Svc, Version),
    vuln(Host, CVE, Svc),
    exploit_possible(CVE),
    service_matches(CVE, Svc).

exec(Host, www_user) :-
    potential_t1028_remote(Host,_).

compromise(Host, user) :-
    exec(Host, _).
% ---- RULE STUB END ----
