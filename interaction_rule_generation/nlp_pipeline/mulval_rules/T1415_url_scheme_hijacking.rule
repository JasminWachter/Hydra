% NOTE: Autogenerated stub. Adjust predicates/logic to your KB.
% technique_id: T1415
% technique_name: URL Scheme Hijacking
% domain: 
% tactic(s): credential-access
% platforms: iOS
% url: https://attack.mitre.org/techniques/T1415
% is_subtechnique: no
% stix_id: attack-pattern--8f142a25-f6c3-4520-bd50-2ae3ab50ed3e
% description: An iOS application may be able to maliciously claim a URL scheme, allowing it to intercept calls that are meant for a different application(Citation: FireEye-Masque2)(Citation: Dhanjani-URLScheme). This technique, for example, could be used to capture OAuth authorization codes(Citation: IETF-PKCE) or to phish user credentials(Citation: MobileIron-XARA).
% generated_by: notebook_generate_rules

% ---- RULE STUB START (network/service exploit) ----
potential_t1415_remote(Host,Port) :-
    attacker_at(net),
    connects(net, Host, Port),
    service(Host, Port, Proto, Svc, Version),
    vuln(Host, CVE, Svc),
    exploit_possible(CVE),
    service_matches(CVE, Svc).

exec(Host, www_user) :-
    potential_t1415_remote(Host,_).

compromise(Host, user) :-
    exec(Host, _).
% ---- RULE STUB END ----
