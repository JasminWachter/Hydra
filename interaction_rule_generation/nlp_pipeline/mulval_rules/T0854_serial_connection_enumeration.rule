% NOTE: Autogenerated stub. Adjust predicates/logic to your KB.
% technique_id: T0854
% technique_name: Serial Connection Enumeration
% domain: 
% tactic(s): discovery
% platforms: Windows;Input/Output Server;Field Controller/RTU/PLC/IED
% url: https://attack.mitre.org/techniques/T0854
% is_subtechnique: no
% stix_id: attack-pattern--5f3da2f3-91c8-4d8b-a02f-bf43a11def55
% description: Adversaries may perform serial connection enumeration to gather situational awareness after gaining access to devices in the OT network. Control systems devices often communicate to each other via various types of serial communication mediums. These serial communications are used to facilitate informational communication, as well as commands.  Serial Connection Enumeration differs from I/O Module Discovery, as I/O modules are auxiliary systems to the main system, and devices that are connected via serial connection are normally discrete systems.  While IT and OT networks may work in tandem, the exact structure of the OT network may not be discernible from the IT network alone. After gaining access to a device on the OT network, an adversary may be able to enumerate the serial connection...
% generated_by: notebook_generate_rules

% ---- RULE STUB START (network/service exploit) ----
potential_t0854_remote(Host,Port) :-
    attacker_at(net),
    connects(net, Host, Port),
    service(Host, Port, Proto, Svc, Version),
    vuln(Host, CVE, Svc),
    exploit_possible(CVE),
    service_matches(CVE, Svc).

exec(Host, www_user) :-
    potential_t0854_remote(Host,_).

compromise(Host, user) :-
    exec(Host, _).
% ---- RULE STUB END ----
