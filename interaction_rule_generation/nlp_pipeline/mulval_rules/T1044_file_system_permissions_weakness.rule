% NOTE: Autogenerated stub. Adjust predicates/logic to your KB.
% technique_id: T1044
% technique_name: File System Permissions Weakness
% domain: 
% tactic(s): persistence;privilege-escalation
% platforms: Windows;Linux;macOS
% url: https://attack.mitre.org/techniques/T1044
% is_subtechnique: no
% stix_id: attack-pattern--0ca7beef-9bbc-4e35-97cf-437384ddce6a
% description: Processes may automatically execute specific binaries as part of their functionality or to perform other actions. If the permissions on the file system directory containing a target binary, or permissions on the binary itself, are improperly set, then the target binary may be overwritten with another binary using user-level permissions and executed by the original process. If the original process and thread are running under a higher permissions level, then the replaced binary will also execute under higher-level permissions, which could include SYSTEM.  Adversaries may use this technique to replace legitimate binaries with malicious ones as a means of executing code at a higher permissions level. If the executing process is set to run at a specific time or during a certain event (e.g.,...
% generated_by: notebook_generate_rules

% ---- RULE STUB START (network/service exploit) ----
potential_t1044_remote(Host,Port) :-
    attacker_at(net),
    connects(net, Host, Port),
    service(Host, Port, Proto, Svc, Version),
    vuln(Host, CVE, Svc),
    exploit_possible(CVE),
    service_matches(CVE, Svc).

exec(Host, www_user) :-
    potential_t1044_remote(Host,_).

compromise(Host, user) :-
    exec(Host, _).
% ---- RULE STUB END ----
