% NOTE: Autogenerated stub. Adjust predicates/logic to your KB.
% technique_id: T1121
% technique_name: Regsvcs/Regasm
% domain: enterprise
% tactic(s): defense-evasion;execution
% platforms: Windows
% url: https://attack.mitre.org/techniques/T1121
% is_subtechnique: no
% stix_id: attack-pattern--215190a9-9f02-4e83-bb5f-e0589965a302
% description: Regsvcs and Regasm are Windows command-line utilities that are used to register .NET Component Object Model (COM) assemblies. Both are digitally signed by Microsoft. (Citation: MSDN Regsvcs) (Citation: MSDN Regasm)  Adversaries can use Regsvcs and Regasm to proxy execution of code through a trusted Windows utility. Both utilities may be used to bypass process whitelisting through use of attributes within the binary to specify code that should be run before registration or unregistration: <code>[ComRegisterFunction]</code> or <code>[ComUnregisterFunction]</code> respectively. The code with the registration and unregistration attributes will be executed even if the process is run under insufficient privileges and fails to execute. (Citation: LOLBAS Regsvcs)(Citation: LOLBAS Regasm)
% generated_by: notebook_generate_rules

% ---- RULE LLM START (minimal) ----
    exec(Host, User) :-
        attacker_at(net),
        connects(net, Host, _Port),
        app_installed(Host, 'Regsvcs', _Version),
        perm(Host, User, _Privilege).

    compromise_from_exec(Host, user) :-
        exec(Host, User).
% ---- RULE LLM END ----
