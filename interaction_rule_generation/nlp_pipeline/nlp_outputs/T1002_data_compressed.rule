% NOTE: Autogenerated stub. Adjust predicates/logic to your KB.
% technique_id: T1002
% technique_name: Data Compressed
% domain: enterprise
% tactic(s): exfiltration
% platforms: Linux;Windows;macOS
% url: https://attack.mitre.org/techniques/T1002
% is_subtechnique: no
% stix_id: attack-pattern--b9f5dbe2-4c55-4fc5-af2e-d42c1d182ec4
% description: An adversary may compress data (e.g., sensitive documents) that is collected prior to exfiltration in order to make it portable and minimize the amount of data sent over the network. The compression is done separately from the exfiltration channel and is performed using a custom program or algorithm, or a more common compression library or utility such as 7zip, RAR, ZIP, or zlib.
% generated_by: notebook_generate_rules

% ---- RULE LLM START (minimal) ----
    data_compressed(Host) :-
        has_data(Host, sensitive),
        app_installed(Host, compression_tool, _Version),
        attacker_at(net),
        connects(net, Host, _Port).

    exfiltration_possible(Host) :-
        data_compressed(Host),
        exfiltrated(Host, compressed_data, network_channel).
% ---- RULE LLM END ----
